# 20260831-2 — Checklist: Pass 2b hello quiet

**Purpose:** Score the named Pass 2 FAIL fix. After the first hello, same pair / same room stays quiet until leave. Sitting long talks must not restart as a new id. Occupancy at **11:00** / **20:00** is a **hold**.

**Prior:** `20260831-1` Pass 2 **FAIL talk** @260. Card: `20260831_checklist.md` §8. **Do not resume.**

**This tip:** `ivan/pass-2-hello-quiet` (on 1-B / Pass 2 line). Fast-forward `railway` to this tip (never rebase / force-push `railway`).

**Score sim:** `20260831-2`  
**Match 1-B / Pass 2 exactly:** `soul15_seed_20260224` · `copy_memories=true` · `copy_coords=false` · sprint · **`skip_premiere: true`** · `max_steps: 2400` · **`diagnostic_mode: false`** · `HEADLESS_TAB_REUSE=false` · `TASK_DECOMP_CONTEXTUAL_ENABLED=true`

**Primary bet:** talk. Pass 2 mill was **77** re-opens inside 30 by step 260 (**54** at Hobbs cafe).

**Out:** seek, cafe `study`, restore lock, longer linger/pins, fire-when-12, Option 2 (hellos through ConversationManager).

---

### When to look

Quote `conversation_id`, first line, `act_address`, and names **verbatim**.

| # | Step | What you read | If it's wrong |
|---|---|---|---|
| 1 | **~5** | Skip-premiere jump; tip is this branch | Wrong tip / premiere → tear down |
| 2 | **~60** | No crash. Persist over-6 = **0** | Box sick → stop |
| 3 | **First cafe sits** (~120–240) | Same pair still at `the Ville:Hobbs Cafe:cafe`: **no** second `👋 GREETING TRIGGERED` inside **30** steps. `🛋️ SOFA QUIET` present. `reason=desync_scratch_cleared` **0** (or near-zero) | **FAIL mill / desync — stop.** Do not wait for 11:00 |
| 4 | **305** | Occupancy **hold** 12/15 | Dip → stop and split. Do not restore the lock |

**Pass talk** = greeting re-opens inside 30 fall hard vs 77, and the worst same-pair mill is gone. First hello of a sit is still allowed. Leave then return can hello again.

---

### Start payload (VPS, after pull)

```bash
curl -s -X POST http://127.0.0.1:8001/api/simulations/fork \
  -H "Content-Type: application/json" \
  -d '{"sim_code":"20260831-2","baseline":"soul15_seed_20260224","description":"Pass 2b: hello quiet until leave + persist partner keep","copy_memories":true,"copy_coords":false}'

curl -s -X POST http://127.0.0.1:8001/api/simulations/20260831-2/start \
  -H "Content-Type: application/json" \
  -d '{"action":"start","parameters":{"max_steps":2400,"generation_mode":"sprint","skip_premiere":true,"diagnostic_mode":false}}'
```
