# 20260724-2 — Checklist: score re-prove after headless ready fix

**Purpose:** Full Survival score run after P0 headless fix. Re-prove hygiene + path persist with **real** headless acceptance; finish open gates from `20260723-1` / `20260720-1`.

1. **No screenshots** on the hot path (default)  
2. **Chrome recycle every 500** successful headless renders  
3. **Headless validation real** — sustained `15/15` ACCEPTED (not blind intent-only)  
4. **Reverie RSS creep** time series (`scripts/monitor_reverie_rss.sh`)  
5. **Path persist (slice A)** ≥95% usable `actual_path` on travel steps  
6. **Path playback (slice B)** — live + CDN tween (FE; after §11)  
7. **Self-serve epic** dormant (D7) — remaining signed-in / Look / Join items  
8. **Carry-over natural movement + Survival** (§12) — open human / analyzer gates  

**Context:** `done/20260723-1_checklist.md` · `done/20260723-1_RCA.md` · `20260723_reverie_rss_creep.md` · `20260723_eng-request_path-animation.md` · `20260720-1_RCA.md`  
**Prior score (failed headless):** `20260723-1` — soak `accepted=0/15`, path coverage **0%**, wall clips from intent clamps.  
**P0 smoke (PASS):** `20260724-1` — 100 steps, **15/15** every step, path coverage **100%**, human visual OK.  
**Posture:** No landing CTA; open a QA world only for optional §9.

**Score sim:** `20260724-2`  
**Prior score sims:** `20260723-1` (hygiene paper; headless FAIL) · `20260720-1` (analyzers ~7532; human A/B residue)  
**BE deploy / commit:** `railway` @ `553d6e81` (sim UUID → headless collision override)  
**FE (VPS headless):** rebuilt against BE Supabase `kkjhsozszgoorwehhsdg`; `FRONTEND_URL=http://localhost:3000`  
**FE viewers (slice B):** `________` (promote status)  
**Signed-in account:** `________`  
**RSS CSV path:** `/tmp/reverie_rss_20260724-2.csv` → copy to: `________`  
**Start:** 2026-07-24T20:14:04Z · **7600** steps · sprint · `diagnostic_mode=false` · runner PID **1661266**  
**Env:** `HEADLESS_MOVEMENT_ENABLED=true` · `HEADLESS_STRICT_ABORT=true`  
**Auto-watch:** **ON** — `ops/ops_watch_20260724-2.py` · state `ops/20260724-2_watch_state.json` · Cursor loop **15m**  
**RSS monitor:** nohup `SIM=20260724-2` (started with run)

> **Do not close the run without §10b** (copy CSV + one-paragraph read).  
> **Do not declare path/movement ship without §3 + §11 green on this sim.**

---

### 0. Before the run — env armed?

- [x] Deployed code includes screenshot gate + browser recycle + **headless sim_id override** — VPS `railway` @ `553d6e81`
- [x] Screenshot default **off**: `HEADLESS_SCREENSHOT_EVERY_N=0` (or unset)
- [x] `HEADLESS_BROWSER_RECYCLE_EVERY_N=500` (or unset — default `500`)
- [x] Phaser loop on: `HEADLESS_MOVEMENT_ENABLED=true`
- [x] `HEADLESS_STRICT_ABORT=true` (abort on bad acceptance — keep for this score)
- [x] `diagnostic_mode=false` · sprint · target **7600**
- [x] FE headless host on correct Supabase (not stale `jawqll…`)
- [x] BE self-serve stack present; `CHAT_REQUIRE_AUTH` false/unset; public sims `self_serve_access=closed`
- [x] RSS monitor scripts on VPS: `scripts/monitor_reverie_rss.sh`, `scripts/snapshot_sim_chrome_mem.sh`
- [x] Note: `SIM=20260724-2` · start step `0` · UUID `736e241a-8917-4a4b-828a-01dd6c93d929`
- [x] P0 smoke `20260724-1` already proved ready + path coverage before this score

```bash
grep -E 'HEADLESS_SCREENSHOT_EVERY_N|HEADLESS_BROWSER_RECYCLE|HEADLESS_MOVEMENT|HEADLESS_STRICT|FRONTEND_URL' \
  /var/www/generative_agents/.env.local
```

---

### 1. Screenshots stopped

**Pass:** `rendered/` does not grow with per-step PNGs.

- [ ] Midflight (~100+ steps): no new `step-*.png` (unless `EVERY_N>0` on purpose)
- [ ] End of run: no unbounded `rendered/` growth (contrast: `20260720-1` ~269 MiB)

```bash
SIM=20260724-2
ls -lt /var/www/generative_agents/environment/frontend_server/storage/$SIM/rendered 2>/dev/null | head
du -sh /var/www/generative_agents/environment/frontend_server/storage/$SIM/rendered 2>/dev/null
```

**Fail:** new PNG every step with default env → wrong code/flag on VPS.

---

### 2. Chrome recycle every 500 successful renders

**Pass:** recycle after ~500 / ~1000 **successful headless completions**; Chrome count not unbounded; steps keep advancing.

**Note:** Counter is **completions in the current process**, not sim step alone. Clean start from 0 ≈ steps 500/1000. Log `step=` = sim step at recycle time. If literal log missing, **RSS proxy** (`chrome_rss_mb` drop ≥40 MB near 500/1000/2000) is acceptable — as on `20260723-1`.

- [ ] ~500 completions: recycle log **or** RSS chrome drop
- [ ] ~1000 completions: second recycle
- [ ] ~2000 completions (bonus): third recycle
- [ ] `backend_process_active=true` and `current_step` still climbing after recycle
- [ ] Chrome/Chromium count does not grow without bound
- [ ] Post-recycle: movement reports / coords still update

```bash
curl -sk https://127.0.0.1:8001/api/simulations/$SIM/status \
  | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('current_step'),d.get('backend_process_active'),d.get('last_generated_at'))"
ps aux | grep -E '[c]hrome|[c]hromium' | wc -l
```

**Fail:** no recycle after ~500 completions · Chrome pile-up · freeze / no movement after recycle.

---

### 3. Regression — validation still real (P0 for this paper)

*Was FAIL on `20260723-1` (headless never ready). Smoke `20260724-1` PASS — re-prove on long run.*

- [ ] Soak / probe: sustained **15/15** `[CP-8] ACCEPTED` (or agreed floor); **0** `HEADLESS STRICT FAILURE`
- [ ] Log shows collision sim_id override for this UUID at start
- [ ] Steps advance; coords in Supabase for new steps; travel steps have non-empty paths (ties to §11)
- [ ] Not a silent “headless disabled” path — Playwright/Chrome under runner
- [ ] Soft: multi-hour / multi-day without OOM kill of `double-api`

```bash
# Local evaluate (preferred)
python ops/ops_watch_20260724-2.py evaluate
# Or on VPS:
grep -ac 'HEADLESS STRICT FAILURE' /var/log/soak/20260724-2.log
grep -aoE 'accepted=[0-9]+/[0-9]+' /var/log/soak/20260724-2.log | sort | uniq -c | sort -rn | head
```

**Fail:** `accepted=0/15` · `headless_ready_false` · `collision_manager_bound=False` · strict abort storm.

---

### 4. Viewer coexistence

- [ ] Gateway up — `GET /health` healthy; status API OK
- [ ] Open **`20260724-2`** (or known prod sim) **without login** — playback/sprites normal
- [ ] Cast / Survival chat still works without forcing login (gate off)

**FE/Landing follow-ups (chat bubbles — not hygiene; carry from `20260720-1`):**  
minimize button no-op · click-drag selects text instead of moving bubble · dynamic tip-to-head does not stretch to speaker. Route to FE + Landing.

---

### 5. Auth + ownership — **remaining from `20260723-1`**

- [x] No JWT → `GET /api/me/double/avatar` (or `/double`) → **401** — proven on prior score / deploy
- [ ] Signed in → `GET /api/me/double` resolves owned Double + host — needs signed-in token / human
- [ ] Soft: `GET /api/me/doubles` returns this user’s agent link — needs signed-in token / human

```bash
curl -sk https://127.0.0.1:8001/api/me/double/avatar -o /dev/null -w "%{http_code}\n"
# With Bearer $TOKEN:
curl -sk https://127.0.0.1:8001/api/me/double -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

---

### 6. Assess → Talk — **remaining**

*Skip if already proven for this account on prod and you’re only re-checking deploy.*

- [ ] Quiz OK (or profile already has Big-5)
- [ ] Interview / `prediction_ready` true for owned Double
- [ ] Talk: 1–2 messages (`/onboarding/talk` or `POST /api/me/double/chat`)
- [ ] End session → assessment stored, **no silent ISS apply**
- [ ] Soft: post-chat review confirm/dismiss works

---

### 7. Look (D8) — **remaining**

- [ ] `GET /api/me/double/avatar` returns catalog (+ current if set)
- [ ] **Change look** / `POST` `atlas_code` persists (sprite-manifest / active row)
- [ ] Soft: Double not stuck in `missing_personas`

---

### 8. Join World — dormant — **remaining**

- [ ] Signed-in `GET /api/me/double/bind/sims` → **`sims: []`**
- [ ] FE Join World empty state is OK (not an error)

```bash
curl -sk https://127.0.0.1:8001/api/me/double/bind/sims \
  -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

---

### 9. Optional punchline (demo only)

*Open one QA world deliberately, then close it. Skip for dormant D7.*

- [ ] Set one QA world `private` + allowlist (or `open`) briefly
- [ ] Look → Join → job/home → finalize
- [ ] Roster + job/home in scratch; atlas present; map coords seeded at latest step
- [ ] Set world back to **`closed`**

---

### 10. Reverie RSS creep — start monitor + check results

**Pass:** CSV from near step 0 → end (≥1500 steps preferred). Headless stays ON; observe-only.  
Details: `done/20260723_reverie_rss_creep.md` (or sibling doc if still at repo root).

#### 10a. START — within ~5 min of sim start

```bash
cd /var/www/generative_agents
SIM=20260724-2 INTERVAL_SEC=300 bash scripts/monitor_reverie_rss.sh
# nohup / tmux; leave for full sprint
tail -f /tmp/reverie_rss_20260724-2.csv
```

- [x] **Started** with sim — first sample **2026-07-24T20:14:52Z** (step blank, reverie **188.3**, chrome **0.0**, host_avail **2931**)
- [ ] Confirmed CSV appending every ~5 min through midflight
- [ ] Soft mid-run: `SIM=20260724-2 bash scripts/snapshot_sim_chrome_mem.sh` if Chrome looks weird

#### 10b. AFTER RUN — check results (before declaring hygiene/RSS done)

- [ ] Monitor still had rows through stop (or note last step in CSV)
- [ ] Copied `/tmp/reverie_rss_20260724-2.csv` off-box **or** into `storage/20260724-2/analysis/`
- [ ] Filled **RSS CSV path** at top of this checklist
- [ ] One-paragraph read written below:
  - `reverie_rss_mb` vs step → linear / plateau / bands?
  - `chrome_rss_mb` flat or climbing? recycle drops near 500/1000/2000?
  - `assoc_disk_mb` flat while RSS climbs (or tracking)?
  - `png_count` / `rendered_disk_mb` flat with screenshots off?

**RSS read (fill in):**  
_TBD after stop._

**Fail:** forgot to start monitor · started late mid-run only · no CSV at end · no read after stop.

---

### 11. Path persist (slice A) — **must re-prove** (was 0% on `20260723-1`)

**Pass:** travel steps have usable `actual_path` (or array `path`) in Supabase. Does **not** prove FE tween (slice B).  
**Smoke:** `20260724-1` → **100%** (228/228) — expect similar on this score.

```bash
python scripts/analyze_travel_path_coverage.py 20260724-2 --min-pct 95
```

- [x] Deploy includes path-persist migration + gateway `actual_path` readback
- [x] Headless ON for this score sim (FE emits `actual_path` when ready)
- [ ] Coverage script PASS (≥95% of travel steps) — midflight + end
- [ ] Spot-check one commute step in DB: `movement.actual_path` is a waypoint list (not null)
- [ ] Soft: gateway step API returns `actual_path` for that step
- [ ] Soft: contrast only — `20260723-1` remains 0% (no retroactive fix expected)

**Fail:** coverage below bar · `actual_path` still absent on moving steps after headless reports.

---

### 11b. Path playback (slice B) — **remaining / blocked until §11 + FE promote**

**FE status (as of 2026-07-23):** Slice B on `double-front` `local` — clamp keeps walked prefix; CDN maps multi-waypoint `actual_path`. Scrub **drag** may snap; continuous walk = **play / LIVE**.  
**Depends on:** §11 PASS on **`20260724-2`** + FE build on the score stack viewers.

- [ ] FE `local` (or promoted) build with emit + replay harden is what viewers use
- [ ] **Live gateway / websocket viewer:** 2–3 dorm↔cafe / campus↔Hobbs walks look continuous (tween within step)
- [ ] **CDN / replay viewer:** same stored steps also animate (no strip of `actual_path`)
- [ ] Missing-path steps fall back honestly (snap OK) — no fake invented path
- [ ] Soft: scrub **drag** may teleport (documented); play/LIVE must walk

**Fail:** §11 green but both viewers still snap · CDN drops `actual_path` · live OK / CDN broken (or reverse).

---

### 12. Carry-over — open gates from `20260720-1` / `20260723-1`

**Source:** `20260720-1_RCA.md` §A–H · leftover rows in `done/20260723-1_checklist.md`.  
**Prefer validate on `20260724-2`.** Analyzer baselines from `20260720-1` (~7532) still useful:

| Counter | `20260720-1` | vs `20260717-1` |
|---------|--------------|-----------------|
| TELEPORT @ sr1 | **0** | ~1818 |
| Gap1 / Gap2 non-worker | **28 / 17** | 676 / 599 |
| Address divergence | **46** | 3154 |
| Piano false / real | **297 / 73** | want ≈0 false |
| Wait-wrap / `<waiting>` | **0** | — |
| CHAT_MISSING_PAYLOAD (R8) | **0** | — |
| Coord jump max / step | **6 tiles** | clamp OK |

#### 12A. Walk feel / no blink (R3) — **P0 human**

- [x] Prior human (`20260720-1` / smoke `20260724-1`): continuous travel, no town hop / no ping-pong
- [ ] **This score sim:** 2–3 building changes — continuous multi-minute travel, not map hops
- [ ] No arrive-one-tick / leave-next ping-pong
- [ ] **No wall clips** (was FAIL on `20260723-1` while headless blind — should clear with §3 green)
- [ ] Soft after §11 + **11b**: path tween visible (else snap-per-step until FE slice B)

#### 12B. Hobbs staff zones (R1) — **P0 human — remaining**

- [x] Prior: busy cafe crowd in customer seating / public (not kitchen invasion) — PASS on `20260720-1`
- [ ] Spot-check **Max Shoemaker** (and other non-workers) not living on cooking / **behind counter** — prior soft FAIL: Irene then Olivia behind counter; soft FAIL again on `20260723-1`
- [x] Soft: full house = shared seats OK, not musical-chairs teleports — PASS prior
- [ ] Optional: re-run Gap1/2 on **this** score sim — expect still ≪ 676 / 599

#### 12C. Honest place language (R2) — **P0 soft — remaining FAIL**

- [ ] Walking: `heading to` + `@` = presence (not already far destination) — prior FAIL: Diana / Alex / Vince status≠place windows; FE continuous-play status freeze risk
- [x] Stationary: `@` and `address_label` agree — PASS prior
- [x] No clear place fights (e.g. work-at-Willows `@ Hobbs`) on prior spot-check
- [ ] Re-check on **`20260724-2`** midflight / human pass

#### 12D. Piano only when playing (R6) — **P2 FAIL carry**

- [ ] Non-play acts **not** parked on `:piano` (was **297** false on `20260720-1`)
- [ ] Real play still lands on piano
- [ ] Re-measure V2 on **this** sim after any affordance fix; bar ≈0 false

#### 12E. Prop / leaf stability (R7) — **P2**

- [x] Classroom teach stays on podium/board — PASS soft prior
- [x] Cafe coffee stays on a seat while task stable — PASS soft prior
- [ ] Soft re-spot on this sim (optional)

#### 12F. Analyzer truth (R4 / R5 / R8) — **P1 — remaining**

- [ ] R8: no `(from description)` chat-shell flood on new sim
- [ ] R5: Apartment-N noise — only wrong-home / foreign apartment matters
- [ ] R4: if playback still feels deep-chat ping-pong, open **separate** social-policy task — **do not** ship gather pair-mute; cafe chatty is OK

#### 12G. Survival regression — **this run is Survival — remaining**

- [ ] Gather **spatial_gate** still fires Day 2+ chal/vote
- [ ] No sleep-stuck / sleeping-1440 / empty-tally elim regression
- [x] `BACKEND_INTENT_ONLY_PATH` still default — confirm on VPS `.env.local`
- [ ] Wait-wrap / nested `<waiting>` still ~0
- [ ] Workers still allowed on staff leaves in work area

#### 12H. Light soul carry-over (optional)

Only if product wants Light ship on this long Survival (`max_steps` ≥ ~7500–9000).

- [ ] Light soul reasons skim (Day 4+ IDs that ran)
- [ ] Blind triad Ivan · Diana · Mike after `challenge_blind_choice` package
- [ ] Attraction Day 4+ soft
- [ ] Full fidelity / Light ship call only after above

**Skip 12H** if Light stays deferred.

---

### Ops / stop hygiene

- [ ] Auto-watch loop left on until terminal **or** explicitly stopped
- [ ] Prestop harvest (RSS CSV + soak snippets + chrome snapshot) before stop — adapt `scripts/harvest_prestop_20260723-1.sh` for `SIM=20260724-2` or run by hand
- [ ] Stop with `{"action":"stop","parameters":{"force":true}}` if graceful hang
- [ ] Confirm status **`stopped`/`completed`**, runner gone, Chrome drained

---

### Verdict (fill as gates close)

| Gate | Result | Notes |
|------|--------|-------|
| **0 env / deploy** | PASS | `553d6e81` · strict abort · FE Supabase aligned |
| **1 screenshots off** | | |
| **2 recycle @ 500** | | |
| **3 validation real** | | Must stay 15/15; was FAIL on `20260723-1` |
| **4 viewer** | | |
| **5 auth / ownership** | PARTIAL | 401 done; signed-in open |
| **6 assess → Talk** | OPEN | |
| **7 look (D8)** | OPEN | |
| **8 join dormant** | OPEN | |
| **9 punchline** | SKIP/OPEN | dormant D7 |
| **10a RSS started** | PASS | with start @ 20:14Z |
| **10b RSS CSV + read** | | |
| **11 path persist** | | Smoke 100% on `20260724-1`; score TBD |
| **11b path playback** | BLOCKED | until §11 + FE promote |
| **12A walk / walls** | | Re-human on this sim |
| **12B Hobbs counter** | OPEN | residue |
| **12C place copy** | OPEN | residue FAIL |
| **12D piano** | OPEN | |
| **12E sticky leaf** | PASS* prior | |
| **12F analyzer** | OPEN | |
| **12G Survival** | OPEN | |
| **12H Light** | SKIP? | |
| **Ship hygiene?** | | 1+2+3+10 |
| **Path persist A?** | | |
| **Natural movement ship?** | | 12A–C + §11/11b |

**Human visual (`20260724-1` smoke, 2026-07-24):** PASS — looks great (spot-check before score start).

**Human visual (`20260724-2` score):** _TBD midflight / end._
