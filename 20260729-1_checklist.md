# 20260729-1 — Checklist: place-language honesty ship + leftover Talk gate

**Purpose:** Prove today’s deploy (`6057e10d`) closes the **soft place-language residuals** left yellow on `20260728-2`, and finish the **Talk auto_apply** gate that was still HOLD.

**Already proven (do not re-litigate):** classic Hobbs@Park named-travel (§12C ship-blocker) on `20260728-2` / P1; empty-assessor fix; skip reason + messages intact; path persist / headless smoke shape from prior runs.

**Shipped today (must re-score):**
- **P2-SOT-1 / RC3:** API prefers emit `description`/`intent` (`PLACE_LANGUAGE_API_PREFER_EMIT=true`); nested progress aligned to travel-safe copy
- **Emit forensics:** `realism_trace.emit_honesty` `{rewrote, cause, raw_act, emitted_act}` — raw act kept for rot measurement
- **PR2 / RC2 at source:** travel + light home stretch coerced off `exercise` → `relax` (prompts + validator + inference) so whitelist does not invent Johnson Park

**Still open from `20260728-2`:**
- ~~Soft A / Soft B / Soft C~~ — cleared on this run (§2 PASS)
- Talk Path A — `auto_applied: true` not proven (`validation_failed` / rate limit)
- Talk FE skip copy / idle close (FE branches — optional this paper)
- Stop hygiene for this run (sim still live)

**BE:** `origin/railway` @ **`6057e10d`**  
**FE (Talk):** still optional — `ivan/post-chat-session-end-skip-ux` / `ivan/talk-session-auto-close` if promoted  
**Posture:** Survival sprint; do **not** restart `double-api` while this sim is live.

**Score sim:** `20260729-1`  
**Baseline / fork from:** `soul15_seed_20260224`  
**Target steps:** `2600` (min useful call ~1600–1800 through Survival Day 1 ~09:00)  
**Start:** `2026-07-29 06:30` · runner PID `2087583` · UUID `b6536b03-20a3-44d1-9b2a-18d89ea423ea`  
**Env:** `HEADLESS_MOVEMENT_ENABLED=true` · `HEADLESS_STRICT_ABORT=true` · `INTENT_PERSIST_HARD_FAIL=true` · `PLACE_LANGUAGE_API_PREFER_EMIT=true` (default)

> **Ship rule:** Natural-movement ship needs **§2 green** (classic + soft A/B cleared or explicitly accepted). Talk learning ship needs **§3 auto_apply green**.

**Clock note:** Expect day boundary ~**step 1050 @ 00:00** (confirm early). Survival Day 1 **07:00–09:00 ≈ steps 1470–1590**.

**Probe tool:** `generative_agents/scripts/score_sim_probe.py` only (no one-off `_probe_*`).  
Example: `python scripts/score_sim_probe.py 20260729-1 --morning-day1`

**RCA:** `20260729-1_RCA.md` (full-pipeline inquiry: Class D display vs Class P wrong destination pick). Prior: `done/20260724-2_rca_12c_place_language.md`.

---

### 0. Preflight

- [x] `20260728-2` stopped; runner PID gone
- [x] VPS `git pull` → tip **`6057e10d`**
- [x] `systemctl restart double-api` after pull
- [x] Prefer-emit live on VPS (`prefer_emit True`; normalize returns travel copy over nested “order…”)
- [x] Fork + start `20260729-1`; Survival ON (Day 1 / 15 players); UUID / PID recorded

---

### 1. Early smoke (~steps 50–100) — expect PASS

- [x] Tip still `6057e10d`; `double-api` active; runner PID alive
- [x] Headless acceptance alive/alive — no `HEADLESS_STRICT_ABORT`
- [x] Soft: at least one travel step with usable `actual_path` (path_len ≥ 2)
- [x] Soft: soak has no crash/traceback flood in first ~100 steps
- [x] Note `curr_time` / step for day-boundary estimate

**Result:** **PASS** @ step **54** (`2026-07-29T07:25`, Premiere). Tip `6057e10d`; API active; PID `2087583` alive (~56m wall). `HEADLESS_STRICT_ABORT=0`, traceback=0. Travel paths seen (e.g. Max→Hobbs path_len=7; Vince breakfast commute path_len=5). Midnight still ~step **1050**; Survival Day 1 07:00–09:00 ≈ **1470–1590**.

---

### 2. Place language — **primary gate (today’s ship)**

**Pass bar (stronger than `20260728-2` soft yellow):**
1. Classic named Hobbs/Oak Hill @ Park still **absent** (P1 regression).
2. Soft A gone: no “stretch… main room” (or similar home stretch) assigned / displayed at Johnson Park.
3. Soft B gone **on player-facing fields**: step API `description`/`intent` must not show “order/eat…” while clearly `travel_to_zone` with long path. Raw planner may still diverge — that is OK if `emit_honesty.rewrote` and player fields are travel-safe.
4. Soft: `emit_honesty_summary` present on morning probe; rewrite causes explain any residual raw/player split.

**Human / probe (Day 1 morning ≈ 1470–1590):**
- [x] Vince: no Hobbs-label @ Park-addr assigns (classic)
- [x] Vince: no stretch/home place-claim → Park cascade (soft A)
- [x] Vince: no player-facing “ordering…” with path_len ≥ 4 while heading to Hobbs (soft B)
- [x] Soft: coffee/study remaps (soft C) — note only; not ship-blocker alone
- [x] Soft peers (Diana / Alex): no Hobbs@Park

**Soak / analyzer:**
- [x] `POST-VALIDATE keep named dest` still fires (P1 healthy)
- [x] Soft: Tier-2 `exercise`→Park on travel/stretch rows ↓ vs `20260728-2` (decomp coerce working)
- [x] Probe `emit_honesty_summary.rewrote` / `causes` recorded; `raw_vs_player_divergent` may be >0 (forensics) while `api_fail_candidates` for ordering+long-path is **0**

**Fail →** keep NM ship HOLD; file residual against RCA (do not invent keyword patches).

**Result:** **PASS** — scored @ live step **~1635** (Survival Day 1 ~09:45) via `score_sim_probe.py 20260729-1 --morning-day1` (window 1410–1590). Tip still `6057e10d`.
1. Classic: soak `bad_count=0`; Vince walk/order/eat all resolve to **Hobbs Cafe** (not Johnson Park).
2. Soft A: morning stretch is `waking up and stretching in bed` @ **Apartment 5:main room:bed** — no home-stretch→Park cascade; soak greps for stretch/exercise→Park in morning sample empty.
3. Soft B: `ordering breakfast` / `ordering and starting…` at path_len=1 with Hobbs addr (arrived); `api_fail_candidates=[]` (no ordering+path_len≥4).
4. Soft C: coffee stay at Hobbs seating; later `walking to Oak Hill…` with Oak Hill library addr (honest travel, not coffee@classroom).
5. Peers @ ~08:00: Diana dorm / Alex kitchen / Irene Hobbs — no Hobbs@Park.
6. Forensics: `emit_honesty_summary` present (13 rows); `rewrote=0`, `raw_vs_player_divergent=0` this morning (aligned; rewrites not required). `POST-VALIDATE keep named dest=13`.

---

### 3. Talk / post-chat learning — **remaining from 20260728-2**

*Account: `ipistsov@gmail.com` · owned host `owned-c2677e73`. Consent ON.*

**BE (can run while sim live — do not restart API):**
- [ ] End session after ≥3 user turns / ≥120 chars → patches proposed → `auto_applied: true`, `apply_status: confirmed`, life-chapter updated (Path A) — **primary leftover**
- [ ] Soft: skip path still returns usable `reason` + `skipped: true` (regression)
- [ ] Soft: messages intact after end/skip
- [ ] Soft: idle sweep ~16+ min (optional)

**FE (only if Talk host has the FE branches):**
- [ ] Skip → honest “No update proposals (reason)” — no fake CTA
- [ ] Leave / reopen Talk → new `thread_id` after idle close

**Smoke:** `SMOKE_GATEWAY_URL=https://api.ondouble.com:8001 python scripts/smoke_talk_session_end_live.py --live`  
Watch owned-host **2 threads/hour** rate limit — space retries.

**Result:** _(fill)_

---

### 4. Natural-movement spot-check (regression — soft)

- [x] Walk / walls OK on morning window used for §2
- [x] Soft: Hobbs counter — non-staff not invading kitchen (spot only)

**Notes:** Morning probe `path_spot` shows multi-tile travel (path_len 5–7) with no `HEADLESS_STRICT_ABORT`. Kitchen spot @ 1485–1560: **0** personas on Hobbs `kitchen` addr (no non-staff invasion).

---

### 5. Stop hygiene

- [ ] Status `stopped`/`completed`; runner gone; Chrome drained
- [ ] One-line note: tip `6057e10d` + §2 / §3 verdict → NM ship? Talk ship?

*Sim still running @ ~1635 — defer stop hygiene.*

---

### Verdict

| Gate | Result | Notes |
|------|--------|-------|
| **0 preflight / deploy** | PASS | tip `6057e10d`; prefer-emit verified |
| **1 early smoke** | PASS | step 54; abort/TB 0; paths OK |
| **2 place language** | **PASS** | Classic + soft A/B cleared; soft C clean; emit_honesty present; api_fail=0 |
| **3 Talk / post-chat** | OPEN | Need live Path A `auto_applied: true` |
| **4 NM soft** | PASS | Walk OK; kitchen soft clean |
| **Natural movement ship?** | **YES** | §2 + §4 green on `20260729-1` |
| **Talk learning ship?** | HOLD | §3 still open |

**Deferred:** Join punchline, Light §12H, deep R4 chat policy, PM-OWN-1 editor UI, PR3 sector-claim guard (only if soft A survives this run).
