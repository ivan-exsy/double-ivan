# Launch plan — village MVP + parallel tracks

**Locked:** 2026-08-27 · **Updated:** 2026-08-28 (pass 1 scored; 1-A added)  
**Role of this file:** high-level order of work for you. Chunk-level plans stay in the papers listed at the bottom.

---

## MVP gate

A first-time viewer of one Survival day. Public MVP needs **gather and town talk**. Walk honesty is already in.

| Layer | Today | Gate |
|---|---|---|
| Bodies stay on the last honest tile | **Pass** (keep `HEADLESS_TAB_REUSE=false`) | Hold |
| Challenge at **11:00** looks like a show (~80% at Hobbs) | **Fail** — **3/15** then **6/14** on `20260827-1`; board still runs | Must fix (**1-A**) |
| Vote at **20:00** | **Fail** — scored at the real clock at last: **8/15**, reached 12 at **20:15**. Old ~19:48 passes were a 19:00 spatial fire and do **not** carry | Must fix (**1-A**). The restored 19:00 vote invitation overlaps dinner's last 30 min — accepted, that is the 23-2 / 25-1 config that passed |
| Action text matches the body | **Fail** — 930 off-cafe “heading to Hobbs” lines. **Mostly the same bug as gather** — closes when the lock goes | Must fix (inside **1-A**) |
| Cafe talk looks like people | **Fail** on sofa re-greets (72 inside 30 steps). Fourth wall is now **Pass** — **0** lines name the simulation | Must fix (**second pass**) |
| Present-only / overlay / absentees cannot win | **Pass** | Hold |
| “That’s me” (quiz twin) | Not this gate | Post-MVP |

Score **tiles**, not labels. Premiere (engine day 1) is grace — do not score gather there.

**Already decided out of the gate (band-aids):** dest-rewrite never-sits (H3), fail-closed if the room is thin, longer pins / all-morning curfew, fire challenge or vote early when 12 sit.

---

## How to size a pass

The old rule was “one branch, one deploy, one bet.” It came from Pass 1, which shipped the gather lock and the invitation cut in the same commit and then needed a full RCA to work out which one caused the 3/15.

**1-A disproved the rule as written.** It bundled *five* fixes and every one was attributed cleanly — because each had its own runtime signal, checked at **step 5** before a single occupancy number was read. Deleting the lock, the invitation, the identity seed, the travel anchor and the ritual gate all show up in different places.

The real rule:

> **Bundle freely when each fix has an independent observable. Serialize only when two fixes move the same number.**

Corollaries:

- Before bundling, name the observable for each fix and the step you can read it at. No observable → it does not go in the run.
- Gather and sofa were separated because both move **occupancy**. That held up: chat was tested as the remover on `20260828-1` and cleared, so they are two bugs and the split was right. Re-check the assumption whenever two deferred items touch the same metric.
- A run is not “one bet.” It is “one bet **per shared number**.”

This is what makes the step-5 gate worth more than the run: it converts a 27-hour experiment into a ten-minute one for anything with an early observable.

---

## First pass — pre-MVP (DONE — shipped, scored **FAIL** on `20260827-1`)

One branch, one deploy, **one** skip-Premiere score sim. Do not mix a second village bet onto this run.

- **Gather:** walk-in + pin only the last ~30 min for **both** appointments (challenge **10:30→11:00**, vote **19:30→20:00**). Challenge **fires at 11:00**, vote **at 20:00** — not when the room fills. Dinner 18:00–19:30 stays free. Honest dest text. Challenge/vote verbs only on Hobbs tiles. Skip still costs the round.
- **Thin town talk:** one sofa = one talk (no re-greet); full talk only if they want it; never name Doubland / simulation / backend. Linger +10 min once if both still want it. **No seek** (walking across town can empty Hobbs).
- **Test harness:** skip-Premiere start (engine day 2 ~05:55; Survival on at 06:00). Live/production start must refuse this flag.
- **After a green score:** update SOT §4.7 (written 25-tile bar → live 6-tile reject). Not before.

**Not this pass:** seek, FE home-snap, Talk-with-Double cadence, personality compile, drain/reboot-safe stop, RLS/disk, video auto-gen.

---

## First pass A — pre-MVP (code **DONE** 2026-08-28; awaiting deploy + score)

Branch `ivan/gather-1a` @ **`acf744b8`**, four commits over `266aa54f`. Not deployed — the deploy restarts the gateway and would kill `20260827-1` before its day-2 vote. Score checklist: `20260828-1_checklist.md`.

### What `20260827-1` said

| Scored on tiles at the declared clock | Result |
|---|---|
| Day-1 challenge 11:00 | **3/15** (bar 12) — worst first morning of the three score sims |
| Day-1 vote 20:00 | **8/15** (bar 12); room reached 12 at **20:15**, fifteen minutes late |
| Day-2 challenge 11:00 | **6/14** (bar 11) |
| Start-jump · spoken fourth wall | **Pass** — 0 over-6 in 26,819 persona-steps; 0 meta lines |
| Honest caption · sofa | **Fail** both |

Day-2 vote (step 2285) still to score. Runner stays up. Full paper: `20260827_RCA+recommendations.md`.

**Why it missed:** the 30-min lock writes the on-screen sentence but never the field that moves the body, so people stand at work while the subtitle says they are walking to Hobbs. Worse, while the lock is on it re-stamps each agent's activity timer every step, so the daily plan cannot advance and the room stops changing until the lock releases — then four people arrive five to ten minutes too late. Pass 1 also cut the planner's invitation from 60 to 30 min in the same commit, which is why the first morning collapsed to 3/15.

### What 1-A is for

**Make the room fill on its own.** Less coercion, not more. One branch, one deploy, one score sim — same discipline as pass 1.

- **Delete the gather lock.** Removed outright, not no-opped. Kills the viewer lie and the arrival freeze in one move. Opposite of a pin.
- **Restore the invitation to an hour**, decoupled from the lock so the two can never be cut together again. It was **two** prompt sites, not one, and the second — the daily-plan cue — is the one that writes the late cafe block. Both appointments move back: challenge invite to **10:00**, vote to **19:00**.
- **Anchor travel tasks to the named destination** — “walking to Hobbs” cannot resolve to library / park / desk.
- **Refresh the `currently` identity seed** per survival day. It still told the daily-plan prompt it was premiere day with fifteen people, contradicting the invitation inside the same call. Wrong on 23-2 and 25-1 too. Side benefit: the LLM identity rewrite survival was overwriting anyway now stops running — ~60 fewer calls per day rollover.
- **Gate ritual verbs on where the body is.** Dropping the lock clears ~930 of ~950 dishonest lines; the residual ~13 “casting vote” lines are a second gate, cheap but not free.
- **Test the movement field.** The 18 lock tests never mentioned `target_zone` — that is why this shipped green three times. They retire with the lock, so the rule now binds the *next* gather test.

**Bar:** unchanged — tiles at the declared clock, **80%** (12/15, then 11/14), challenge **and** vote, on a competitive day.

**Not this pass:** sofa / re-greet, and everything already deferred from pass 1. Sofa is a live gate row, but chat state moves bodies, so mixing it in makes the occupancy number unreadable — the same reason seek was cut from pass 1.

**Honest caption is not a separate ticket.** Ninety-eight percent of it is the same bug as gather and closes with the lock; the rest is one extra gate inside 1-A. So the gate's four must-fix rows are **two** pieces of work — 1-A, then sofa — not four. Do not budget the caption twice.

**Still out (band-aids, unchanged):** dest-rewrite never-sits, fail-closed, longer pins, fire-when-12.

**Found, deliberately not touched:** the flag that switches on all of the travel-anchor machinery defaults to *off* in code but is *on* on the box. The run must score the deployed config; align the two afterwards.

---

## Second pass — pre-MVP (after **1-A** scores green)

Only what still blocks a public watch or a sane deploy. Pick from this list; do not open all of it at once.

- **Sofa / re-greet** (gate row, deferred out of 1-A) — one sofa = one talk. Pass 1 did not hold it: 917 chat opens, 72 greeting re-opens inside 30 steps, some one minute apart. Build it **on top of a green 1-A tip** so this run doubles as the combined confirm — not in parallel.
- **Seek** — walk toward a named person in everyday hours, more than once a day, with satiation. Pause in the gather window. Was cut from pass 1 so occupancy stays readable.
- **Drain / continue** — finish the current step, persist, then reboot/update, then the **same** sim continues. Today: `/pause` dies on reboot; `/stop` then `/start` continues from the last **complete** Supabase step, but stop waits only ~10 s and can kill a long step. Needed before the next multi-day score if you will deploy mid-run.
- **FE home-snap** — sprites start each headless step on the backend tile (so tab reuse can go back on). Workaround already works; playback does not need it.
- **Public-internet hygiene** — RLS enable + RPC lockdown on the live project (repo already has the pattern). Disk cleanup so Pro does not jump 12→18 GB. Not village UX; do before the product is on the open web.
- **Green the test suite** — **76** standing failures today, none from 1-A. A real regression can hide in that noise, and proving innocence costs three full suite runs per change. That tax lands on every fix from here. Triage plan in `20260828-1_checklist.md` §8; do it after 1-A scores, not before.
- **Video (parallel, not a village slice)** — Episode 1 closer auto-gen is **locked** and **is the short** (2026-08-27; `video/TODO_video.md`). Remaining video (later nights, viral-grammar, longer informative cut) is **Post-MVP**. Does not wait on gather, and does not replace it.

If **1-A** still misses 80% at 11:00: *then* argue a dest-yank in the open — do not slip it in as “debug the pin,” and do not answer with a longer curfew or fail-closed. Any lock that comes back must be tested on the movement field, not the sentence.

---

## Post-MVP

- **“That’s me”** — compile quiz/interview into short if–then scene lines; measure express *and* suppress; interview as evidence; human recognition before any public “scientifically you.” Charter: `TODO_realism_matriAIx.md`. Research KB is not an implement brief. No paper we reviewed is a proven twin.
- **Talk Path A** — Talk tab (you ↔ one Double): cadence, anti-loop. Village talk does not prove this.
- **Acting-model swap** — only if pass 1–2 still sound like one writer after compiled lines.
- Official Naturalness Gate pair, display-honesty probe, MatrAIx catalog / MBTI / fine-tune on private messages — stay parked or banned as in the charter.

---

## Detail (do not copy into this file)

| Topic | Where |
|---|---|
| **Pass 1 score + why it missed + the 1-A fix list** | `20260827_RCA+recommendations.md` (1-A plan and per-chunk findings in §7A, code map in §11) · score `20260827_checklist.md` |
| **1-A score checklist** (preflight, fix-took-effect checks, test-suite cleanup) | `20260828-1_checklist.md` |
| Pass 1 lock + grouping | `COS/tasks/2026-08-27-002/mvp-cut.md` |
| Stay-pin score + §12 gather | `20260825_checklist.md` |
| Prior persist + gather paper | `20260821_checklist.md` |
| Will / sofa / linger / seek / fourth wall | Grok session `plan.md` (also pasted under `20260825_true_doubles.md`) |
| Walk / home-snap | `20260822_RCA_far-landings.md` · FE letter draft `20260822_FE_inquiry_far-landings.md` |
| Likeness research | `20260825_true_doubles.md` · `TODO_realism_matriAIx.md` |
| Daily trailer | `video/TODO_video.md` · `video/daily/SOT-new-daily.md` |