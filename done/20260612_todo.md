# 2026-06-12 — Closure plan: LIVE mode + hardening Phases 0–3

**Goal:** every box ticked in `20260609_LIVE_mode.md` (MVP scope) and Phases 0–1–2–3 of `TODO_production_hardening.md` marked done.
**Already in hand (no action):** Phase 0 shipped & soaked · Phase 1 shipped & reconciled · DP7 + DP8 QA-closed (4/4 PASS, `20260609_LIVE_mode.md` § QA) · `20260611-2` realism analysis clean · backend halves of Phases 2–3 merged & deployed (`7694f968` + `1762fca7`).
**What actually remains (status 2026-06-16): NOTHING — Phase 2 CLOSED.** Step 5.3 (soak + retire gateway viewer path + flag) ✅, Step 7.2 final tick-off ✅, **PL-12 verified on `20260613-1` un-enroll** (`final_manifest_status:"stopped"`). Phase 3 ✅ (`20260612-3`); 6a ✅ (2026-06-13). **All MVP phases 0–3 done.**

---

## ✅ Step 1 — Run the live validation sim ✅ *operator complete 2026-06-12*

Follow **`20260612_validation_runbook.md` § IVAN** exactly: pre-flight → fork `20260612-1` (response must show `"owner_timezone":"America/New_York"`) → enroll live (no `/start`) → machinery checks → promotion-dinner chat → bomb (write down the pre-bomb step number first) → wrap up.

**Run progress (`20260612-1`):**
- [x] Pre-flight
- [x] Phase A — fork + enroll (`owner_timezone` confirmed; scheduler wake OK)
- [x] Phase B — machinery checks (manifest, edge 404, viewer clock)
- [x] Phase C — promotion chat (`Ivan Pistsov`; gwchat memory ✓, early wake ✓; visible replan not observed)
- [x] Phase D — bomb at pre-bomb step **539** (early wake ✓; visible in-world reaction not observed — bonus demo)
- [x] Phase E — un-enrolled (`final_manifest_status: completed`), Survival restored, engine stopped

**Handoff to Step 2:** sim `20260612-1`, pre-bomb step **539**.

- Starting later than 09:00 EST is fine — catch-up just generates a few extra chunks (~45 min) before settling to real-time pacing.
- **Wrap-up includes restoring `SURVIVAL_MODE_ENABLED=true`** on the VPS (currently `false`, correct for this run).
- This same sim is the FE team's acceptance target (Step 3) — leave its bundles in place.

**Done when:** sim un-enrolled, survival flag restored, and the agent has the sim code + pre-bomb step number.

## ✅ Step 2 — Phase-3 behavioral sign-off *(BE agent · same day · ~1 h)*

Realism analysis (`analyze_sim_realism.py`, Supabase source, capped at the pre-bomb step) + naturalness gates vs `base_family_sim` → formal pass/fail.

**Results (2026-06-12, sim `20260612-1`, steps 0–539; baseline `20260611-2` — `base_family_sim` itself has never run):**
- [x] **Realism: PASS.** 4 minor chat-cooldown nicks over 540 steps; zero teleports/discontinuities/stalls. (81 `CHAT_MISSING_PAYLOAD` flags are analyzer false positives — Luba's cafe *activity descriptions* mention "chatting with customers" who aren't simulated agents; structured-chat payload integrity measured 100%.)
- [x] **Naturalness Gate v1 (`sot_lifecycle.md` §4): PASS** — overdue 0.0 ≤ 0.0 · stall-replan 0.0 ≤ 0.0 · stationary-progressing 0.89 ≥ 0.70 · chat integrity 100% · multi-venue spread present (home/cafe/library/dorm). Subactivity switches 35.9 vs 24.2 (1.48×): the literal ≥2.0× multiplier targets realism-*change* releases for service personas; for identical-code chunked-vs-continuous comparison the no-degradation reading applies — richer, not degraded.
- [x] **Same-day influence: PASS** — P3-1 fix verified on acceptance sim `20260612-3` (2026-06-12). Both injected chats reached behavior via `external_replan` (schedule splice + act-now). Formal realism + Naturalness Gate green vs `20260611-2` at steps 0–139. Phase 3 ticked in `TODO_production_hardening.md`.

**Done when:** ~~PASS recorded; Phase 3 ticked `[x]` in `TODO_production_hardening.md`.~~ ✅ *2026-06-12*

## ✅ Step 3 — FE acceptance run ✅ *complete 2026-06-12; deferred gap re-verified 2026-06-13 on `20260613-1` ✅*

Run the acceptance checklist in **`20260612_FE_phase2_handover.md` §7** against `20260612-1?source=cdn` (smoke script + manual pass: catch-up → 1× at edge → scrub → JTL, zero gateway/realtime traffic). Per the BE response (**§10** of the same doc): cache headers are already correct (test with GET, not HEAD), and add one **engine-published** bundle fixture (e.g. `20260610-90` Run-A steps) — the gateway-rebuilt writer is already covered by the `20260611-3` fixtures, not the other way round.

**Done when:** §7 checklist 5/5 + engine-fixture parse test green.

**Result (2026-06-12, FE): §7 5/5 with one documented gap + engine-fixture tests green** — full results table appended to `20260612_FE_phase2_handover.md` §7. Smoke, scrub/network audit, GET cache-header re-check, and completed-replay-with-gateway-killed (item 4, post-un-enroll) all PASS on `20260612-1`; engine-published fixture (`20260610-90` step 40) added, `cdnViewer` suite 19/19. **Gap (accepted for MVP per Ivan):** true 1× at the reveal edge + JTL not re-run before un-enroll (sim was still catch-up-paced) — re-verify in a short window on the next live-enrolled sim. `ivan/cdn-player` ready for merge → Step 5.1.

**Gap closed (2026-06-13, FE, sim `20260613-1`):** the deferred **1× at the reveal edge + Jump-to-Live + clamp ±1** re-check passed on the live soak sim (Ivan covered it — Nicolas off). At the 1× edge (pinned ~23:15 UTC): a 5-min headless watch showed the viewer burn its ~19-step LIVE cushion then **pin and track at ~1 step/min** (reveal edge advanced exactly 1.0/min; held over a later 38-min window through a between-chunk sleep). Manifest `latest_step` **never exceeded owner-local now and the controller never exceeded the edge — all 20 samples** (clamp held ±1; reveal-gating held while the engine ran ~10–36 steps ahead). `cdn-scrub-audit` (prod): scrub −100 served from CDN, return-to-edge/JTL landed at the live edge from LRU cache; `cdn-smoke`: bundles+manifest+sprite-manifest CDN-served, **0 realtime sockets, 0 gateway step/status polling**, 0 uncaught errors. Only standing flag = the benign day-1 highlights gateway fallback (clears at day-1 close). → strike the gap in `20260612_FE_phase2_handover.md` §7 items 1–2.

## ✅ Step 4 — PL-12: terminal manifest on un-enroll/abort ✅ *closed 2026-06-12 (deployed + verified on VPS; struck in gap table)*

Un-enrolled/aborted sims leave `live.json` frozen at `status:"running"` forever — CDN viewers would wait at the live edge indefinitely (`20260610_parallel_sim_launch.md` §5 PL-12; found by FE, handover §8.6). Fix: publish a final manifest with the sim row's terminal status on `POST /live {"enabled": false}`. (Scheduler-side abort detection dropped by design: `completed` is the normal between-chunk sleep state for live sims, so un-enroll is the only reliable end-of-feed signal — abort procedures already un-enroll first.) Must land **before** the CDN player becomes the default (Step 5).

- [x] **Implemented 2026-06-12** on branch `ivan/pl12-terminal-manifest` (test-first; 6 new tests, touched suites 53/53 green). Merged to `main`/`railway`; deployed VPS 2026-06-12.
- [x] **Verified on VPS 2026-06-12:** un-enroll `20260612-1` → `final_manifest_status: completed`. Strike PL-12 in gap table when BE agent updates trackers.

**Done when:** un-enrolling a test sim updates its manifest `status`; PL-12 struck in the gap table.

## Step 5 — CDN player rollout *(FE + Ivan · soak closed 2026-06-15, gateway viewer path + flag retired)*

- [x] 1. Merge `ivan/cdn-player` to `double-front/main`, deploy — done 2026-06-12.
- [x] 2. Flip `NEXT_PUBLIC_VIEWER_SOURCE=cdn` on Vercel — **done 2026-06-12; prod viewers are CDN-served**.
- [x] 3. Soak a few days on real viewing; then retire the gateway viewer path + the flag (handover §7 closing note — don't leave both alive longer than the transition). **Soak closed + FE flag retired 2026-06-15 — see "Soak closure" below.**

**Done when:** prod viewers are CDN-served; per-viewer gateway polling + realtime retired. **This is the Phase-2 closure milestone.** ✅ **DONE 2026-06-16** — gateway viewer path + flag retired (FE Step 5.3, deployed prod), soak closed on `20260613-1`, PL-12 verified on un-enroll (`final_manifest_status:"stopped"`).

**Soak status (2026-06-13):** dedicated live soak sim **`20260613-1`** up (live-enrolled Survival, owner tz America/New_York, start 06:30). FE-side soak checks **green**: `cdn-smoke` shows bundles+manifest+sprite-manifest CDN-served, **0 realtime sockets, 0 gateway step/status polling**, LIVE loop advancing; watermarks coherent (reveal/published/manifest ≈ owner-now; the engine generates a ~chunk-ahead buffer, so the **1× live edge keeps advancing through the between-chunk sleeps** — confirmed ~1 step/min over a 38-min window that included a sleep with `is_generating:false`/gateway `status:completed` while `live.json` stayed `running`). Reveal-edge re-check passed (Step 3). Remaining for **5.3**: a few clean soak days → FE retires the gateway viewer path + flag (preconditions: legacy sims still watchable via CDN-or-gateway-fallback; keep `/details` + `/card-summary` gateway-served). CDN highlights confirmed live + FE-served at day-1 close (2026-06-14): `highlights/1.json` published, viewer serves closed-day highlights from CDN (only the in-progress day probes the gateway — inherent, benign).

**Soak closure (2026-06-15) — decision: CLOSE on `20260613-1`; FE flag retired.**

*Decision rationale.* The soak's acceptance surface is the **CDN transport plane**, and it stayed green for the whole run. A backend **timezone bug** (`TypeError: can't subtract offset-naive and offset-aware datetimes` in the proximity-observation / conversation-trigger path) corrupted agent *content* from ~03:00 UTC Jun 15 (day-1→day-2 rollover): conversations suppressed, Survival game stalled at day-1 night, one player wrongly eliminated. That is **backend cognition, not the viewer path** — and steps kept generating + publishing throughout, so the transport plane was in fact stress-tested *through* a backend throwing every step (corroborates the plane's decoupling). Content/realism was never the soak's gate — it was signed off separately (Phase 3 on `20260612-1` steps 0–539; P3-1 on `20260612-3`). Full handoff: `20260615_FE_soak_handoff.md`.

*Evidence collected (all green):* CDN-served bundles + manifest + sprite-manifest, **0 realtime sockets, 0 gateway step/status polling** (cdn-smoke); reveal-edge 1×/JTL/clamp ±1 (Step 3); scrub −100 from CDN + return-to-edge from LRU (cdn-scrub-audit, prod); **day-1** close → `highlights/1.json` published → FE-served from CDN, zero gateway for closed days; multiple between-chunk sleeps survived with the 1× edge still advancing.

*Accepted-for-MVP caveats (mitigated):* (1) ~1.5 clean days, not the literal "a few" — mitigated by transport holding under backend-exception stress + every discrete check green; (2) multi-day highlights CDN-serve validated for **one** day close (day-1) — mitigated by the independent `build_highlights 20260611-2 --day 1` byte-identical verification (two confirmations of the publish→CDN path).

*FE Step 5.3 done (2026-06-15, `ivan/link-tracking-fe`).* Retired the gateway viewer path + the `NEXT_PUBLIC_VIEWER_SOURCE` flag: **CDN is now the only configured viewer source** (`lib/viewerSource.ts`). The two narrow gateway paths survive by design — automatic **per-sim fallback** on manifest 404 (legacy/pre-Phase-2 sims stay watchable) and a `?viewer=gateway` operator escape hatch; backend-driven modes (`?headless`/`?recording`) stay on the non-CDN path; `/details` + `/card-summary` + the in-progress-day `/highlights` probe stay gateway-served. Full suite **397 passed / 8 skipped**, `next build` green. Flag row removed from `CLAUDE.md`; `.env.local` flag retired with a breadcrumb.

*Phase 2 closure — DONE 2026-06-16:*
- **Operator (Ivan):** ✅ un-enrolled `20260613-1` 2026-06-16 → **PL-12 verified** (`final_manifest_status:"stopped"`; `live.json` flipped `running`→`stopped`, `latest_step` 3755, fresh `updated_at`). Standard-Playback browser check is the final operator eyeball (the completed-replay-from-CDN mechanism already PASSED in Step 3 on `20260612-1`). Optional cleanup: delete the now-inert `NEXT_PUBLIC_VIEWER_SOURCE` env var on Vercel (harmless to leave).
- **BE:** ✅ Step 7.2 doc tick-off done — `TODO_production_hardening.md` Phase 2 → `[x]`; `20260609_LIVE_mode.md` closure table all ✅.
- **Separate BE track (NOT a Phase-2 blocker):** timezone bug **fixed + deployed 2026-06-16** (`railway`@`7fa22b06`, `_as_naive` in `conversation_manager`). Still to do before the next real-user LIVE sim: re-validate *content* on a fresh post-fix fork (clean Survival + confirm proximity/chat fires with no `TypeError`). Tracked in `20260616_checklist.md`.

## Step 6 — Remaining Phase-2 scope decisions & slices ✅ *complete — 6a merged & deployed to VPS; FE wiring live 2026-06-13*

- [x] **6a — Click-content as CDN artifacts** — **shipped 2026-06-12** (branch `ivan/cdn-click-content`, test-first, 19 new tests green): `{sim}/sprite-manifest.json` (published on live enroll + scheduler backfill, gateway-only) and `{sim}/highlights/{day}.json` (published by the end-of-day builder), both byte-identical to their REST bodies, cache 5 min. Persona cards stay deferred (post-MVP). Contract: `sot_api.md` §9; FE note: `done/20260612_FE_phase2_handover.md` §11. **Deploy confirmed live 2026-06-12** — `20260612-3/sprite-manifest.json` returns 200 on the CDN (sims predating the publisher, e.g. `20260612-1`/`20260610-90`, correctly 400 → FE falls back to gateway, a safe no-op). **FE wiring shipped to prod 2026-06-13** (CDN-first fetch + per-call gateway 404 fallback, +11 FE tests green). **Highlights path verified end-to-end 2026-06-13** — ran `build_highlights 20260611-2 --day 1` against a real completed day: `{sim}/highlights/1.json` went 400→200, `cache 300`, body **byte-identical to the gateway REST response** (8 highlights). The automatic trigger (engine day-close hook → `build_highlights`) is the pre-existing, already-firing path; 6a only added the CDN upload inside it, so the next overnight live sim is final belt-and-suspenders, not an open risk. *(The deferred 1×-at-edge/JTL/clamp re-check — Step 3 gap — was **closed on `20260613-1` 2026-06-13**; Ivan covered it, Nicolas off. **CDN highlights confirmed live + FE-served 2026-06-14**: `20260613-1` closed day 1 → `highlights/1.json` on the CDN (8 highlights; `highlights` array byte-identical to the gateway REST body — only `generated_at`/JSON-formatting differ); cdn-smoke shows the viewer serves day-1 from CDN, **zero** gateway calls for closed days.)*
- [x] **6b — Carried objects** — **Accepted for MVP (Ivan · 2026-06-12):** CDN viewers show carried objects frozen at page load; cosmetic only, rare in family sim. Step-accurate props deferred → `TODO_post_mvp.md` **PM-CDN-1**.

**Done when:** 6a shipped (or explicitly moved to post-MVP with Ivan's sign-off) ~~and 6b decided~~ *(6b done)*.

## Step 7 — Contract docs + final tick-off *(BE agent · ~1 h, doc-only)*

1. ~~Write the publishing contract into `sot/sot_api.md`~~ — **done 2026-06-12**: new **§9 CDN Publishing Plane** (bundle/manifest URLs + shapes, versions, watermark guarantee, status value set + PL-12 terminal rule, two writers, HEAD-vs-GET testing note); also brought §1's field table current (`is_generating`/`last_generated_at` shipped, `live_mode`/`published_step`/`reveal_step` added). SOT bumped to v1.3.
2. Final pass on `TODO_production_hardening.md` (Phase 2 → `[x]`; Phase 3 ✅) and `20260609_LIVE_mode.md` (validation + P3-1 acceptance + closure table) — ✅ **DONE 2026-06-16**: PL-12 verified on `20260613-1` un-enroll (`final_manifest_status:"stopped"`); both trackers ticked.

**Done when:** both trackers show Phases 0–3 done and LIVE-mode MVP fully closed. ✅ **DONE 2026-06-16.**

---

## Parked — tracked, not blocking closure

- **PL-10** (`POST /stop` fails on API-started sims) — fix alongside PL-8; until then: `pkill -f <sim>` + `update_simulation_status`. Worth doing soon: the sole operator can't stop sims via API.
- **PL-5 / PL-7 / PL-8 / PL-9** — Phase-0 ops debt (`20260610_parallel_sim_launch.md` §5); slated for Phase 6 if not earlier.
- **Analyzer long-profile false teleports** — sample-rate-2 makes legal 6-tile/step movement read as 12-tile jumps; raise the threshold so full-run analyses stop crying wolf.
- **Post-MVP LIVE items** — `owner_display_name`, Episode cron (BE-4), server-side reveal enforcement (BE-5), onboarding-finalize tz fallback → all in `TODO_post_mvp.md`.

**Sequence at a glance (updated 2026-06-16):** Steps 1–4, 6 ✅ · Step 2 ✅ (incl. P3-1 same-day influence) · **Phase 3 ✅** · Step 3 deferred reveal-edge/JTL/clamp gap **closed 2026-06-13** (`20260613-1`) · **Step 5.3 ✅ — soak closed + gateway viewer path + flag retired 2026-06-15** (transport green throughout; backend timezone bug a separate BE track) · **Phase 2 CLOSED 2026-06-16** — un-enrolled `20260613-1`, PL-12 verified (`final_manifest_status:"stopped"`), Step 7.2 trackers ticked. **All MVP phases 0–3 done.** Open follow-up (separate): fresh-fork content re-validation on the fixed engine before the next real-user LIVE sim.

---

## ✅ P3-1 — injected user chats never reach behavior ✅ *fixed & accepted 2026-06-12*

**Problem (found in `20260612-1`):** a user message to their double landed in memory and woke the sim, but in-world behavior never changed — both the promotion-dinner ask and the bomb warning were ignored.

**Root cause (RCA 2026-06-12):** two breaks in series — externally-injected memories were never *perceived* (perception reads only the spatial map), and the mid-day reaction path was hard-disabled under `OBSERVATION_PRIMARY` and shaped only for persona-vs-persona events. Full RCA: **`done/20260612_P3-1_RCA.md`**.

**Fix (`ivan/p3-1-external-chat-reaction`, merged `a6ba597c`, deployed VPS 2026-06-12):** external inbox at the core of the cognition loop — memories with `meta.source` (e.g. `gateway_chat`) enter `external_inbox` on Supabase load, drain as perceived events in `perceive._drain_external_inbox` (one per step; deferred only mid-conversation), and trigger `external_replan`: one Tier-B structured LLM call chooses no-change / act now / splice into schedule. No new env flags; zero `external_replan` LLM cost on chat-free steps.

**Unit verification:** 18 new tests (failing-test-first) · prompt contract probed live ×2 · movement-realism smoke green.

### Acceptance — sim `20260612-3` (live-enrolled fork of `base_family_sim`, VPS 2026-06-12)

| Test | Step sent | Result |
|---|---|---|
| Mundane chat (guests / tidy by 11:30) | **67** | **PASS** — `external_replan` spliced `'tidy the living room and prepare snacks and drinks for guests'` at 10:50 sim-time; both gateway_chat memories in Supabase |
| Urgent chat (bomb / act now) | **119** | **PASS** — act-now replan at wake; shelter action visible from **step 121** (`Oak Hill College` classroom). *Caveat:* sent while engine was stopped between chunks; reaction landed within 2 steps after wake |
| Realism (`analyze_sim_realism.py`, steps 0–139) | — | **PASS** — 0 issues vs `20260611-2` at same window |
| Naturalness Gate v1 vs `20260611-2` | — | **PASS** — overdue 0.0 · stall 0.0 · stationary-progressing 0.80 ≥ 0.70 · chat integrity 100% · multi-venue spread (dorm/cafe/college). Subactivity 31.0 vs 24.2 (no-degradation reading) |
| LLM cost discipline | — | **PASS** — exactly **2** `run_gpt_prompt_external_replan` calls in engine log (one per chat); none on chat-free steps |

**Accepted caveats (MVP, documented):** mundane schedule splice was delayed ~50 steps (not visible in first 1–2 chunks operator checked); urgent test not ideal protocol (sim `completed` at send). Neither blocks Phase-3 sign-off.

**Phase 3:** ticked `[x]` in `TODO_production_hardening.md` 2026-06-12.
