# 20260902-1 — Checklist: linger +10 after a full sit

**Purpose:** Was linger +10 (bodies stay ~10 min after a full sit — visual packing, not a deeper talk). **Founder skip 2026-09-02:** do not patch, do not overnight again for linger. Occupancy, mill, leftover, persist are **holds**.

**Prior:** `20260901-1` KEEP **village-green** @619. Mint **10**, mill **0**, `desync_scratch_cleared` **0**, day-1 11:00 **14/15**. Linger helper **0**. 20:00 / day-2 occupancy + evening mill **inherit `20260831-2`**. Card: `20260901-1_checklist.md` Decision. **Do not resume** `20260831-1`, `20260831-2`, or `20260901-1`.

**This tip:** `railway` / `ivan/linger-plus-10` @ **`ec8e2076`** (KEEP `bb22747d` + linger at sit complete). A start off `bb22747d` has no linger. A start off `b8d1fc15` is 2b. A start off `2f5f7269` is the Pass 2 mill.

**Score sim:** `20260902-1`  
**Match 1-B / 2b / 2B exactly:** `soul15_seed_20260224` · `copy_memories=true` · `copy_coords=false` · sprint · **`skip_premiere: true`** · `max_steps: 2400` · **`diagnostic_mode: false`** · `HEADLESS_TAB_REUSE=false` · `TASK_DECOMP_CONTEXTUAL_ENABLED=true`

**Diagnostic stays off.** Linger lines, leftover mint, chat ids, occupancy, and persist are already on disk.

**Score at tiles, not labels.** Cafe box **x 72–83, y 19–30**. Day 1 **/15**; day 2 **/14**. Occupancy bar **12** then **11**.

**Primary bet:** linger +10 — **skipped.** Village harvest through **864**: `SOFA LINGER` = **0** after **55** `beat_budget` sit-stops and **36** leftover mints. That is a miss, not a pass. Founder: skip. Compare to 2b / 2B also **`SOFA LINGER` = 0**.

---

## Issues we wanted to address

- Full sits ended and people walked off at once. The village did not look like they had just talked.
- Pass 2 / 2b / 2B shipped a linger helper, but **`SOFA LINGER` logged 0**. The stay only fired when leftover overlap was already 0. KEEP sits often finished in 1–2 steps with leftover overlap still above 0, so the helper never ran.
- Persist also used linger to keep a **live** session alive. That is the wrong job. Persist should not add +10.
- 2B sit-stop (`beat_budget` at 6 unique lines) skipped persist entirely, so linger never ran on the common KEEP complete.
- Once-key was the CM `session=` id. A new `session=` in the same room could +10 again.
- There was no `SOFA LINGER` line to score.

## Implementation summary

- Linger now runs at **sit complete**, after leftover mint, before scratch reset. Bodies stay +10. Address does not change.
- Allowed complete reasons: `beat_budget` / `commitment_mint` / `no_new_pressure`.
- Must **not** linger: hello, `left_arena`, desync, pause, or no ConversationManager session.
- Once per **pair + 3-part room**, stored in the existing `linger_used_for_conversation_id` field (sit key, not session id).
- Persist overlap ≤0 now returns false. It does **not** +10 to keep the talk open.
- Log: `SOFA LINGER`. Gather / sleep / work still block via the existing will.
- No new env flag. No new scratch field. Seek stays off. `CHAT_PRESERVES_ACTION` stays default true. No dest rewrite. No SOT until this score is green.

**Named fix on this tip (score in village):**
1. After a full sit complete, log `SOFA LINGER` with both names and the 3-part arena. `act_duration` +10 once. `act_address` unchanged.
2. Same pair, same 3-part arena, a second complete → **no** second +10.
3. Hello / `left_arena` / desync / pause → linger **0**.
4. Leftover still mints on full sits. Hello mill stays **0**.
5. Occupancy at day-1 11:00 is a **hold** (bar 12/15). If it dips, **stop and split**. Do not restore the lock.

**Not this pass:** seek, overlay “you played / you were absent”, WHO-gate, Option 2 (hellos through ConversationManager), sit-and-read list widening, leftover cafe walks, Shepard sleep booking, drain/continue, FE home-snap, RLS/disk.

**Out (unchanged):** H3 dest-rewrite, fail-closed, longer pins, fire-when-12, restore the lock, add `study` to the cafe list, turn seek on, stamp `double-docs/sot/` before a green score.

**Record:** fork `2026-09-02T05:06:30.972427+00:00` · UUID `a1dcfc60-3791-4cb9-8dc2-526f78fddada` · PID `1132376` · tip `ec8e2076` · sim `20260902-1` · maze `150ec00d-dad9-4436-a42e-1e278c2d6aa3`  
**Skip-premiere log:** `Skip premiere: clock 2026-09-02 06:30:00 → 2026-09-03 05:55:00 (engine day 2 05:55); start_date unchanged, step=0`

---

## What you would miss if you only did stop → pull → start

1. **Confirm the box is on `ec8e2076` after pull**, before fork. `bb22747d` has KEEP and no linger.
2. **Seek stays off.** Walking toward a named person moves bodies. If occupancy dips, you cannot tell linger from seek.
3. **Do not add `study` to the cafe list.** Mixing list widening in makes an occupancy miss unreadable.
4. **Disable `apt-daily-upgrade` auto-restart of `api-gateway`.** Killed `20260822-1`.
5. **Do not resume `20260831-1`, `20260831-2`, or `20260901-1`.** All stopped. Stopping a leftover PID is not a data loss.
6. **Linger is skipped.** `SOFA LINGER` = **0** through 864 is recorded, not patched. Occupancy is a hold.
7. **If occupancy dips below bar:** stop and split. Do not patch seek. Do not restore the lock.
8. **Do not treat persist as linger.** Overlap ≤0 must end the sit. Linger was the stay after the sit is already complete.

---

### When to look — checkpoint ladder

Step 0 = day-2 05:55. One step = one minute. Pace ~50–80 steps/h. Track the **step**, not the wall clock.

Quote `SOFA LINGER`, `session=`, `act_address`, leftover `class=`, and names **verbatim**. Do not shorten to “they hung around.”

| # | Step | Clock | What you read | If it's wrong |
|---|---|---|---|---|
| 1 | **~5** | 06:00 d1 | Engine jumped; Survival on; no premiere / “full cohort of fifteen”. Tip is `ec8e2076` | **PASS @ start.** Log: `Skip premiere: clock 2026-09-02 06:30:00 → 2026-09-03 05:55:00 (engine day 2 05:55); start_date unchanged, step=0`. Live through **864**: `is_survival=true`, `current_day_label=Survival Season Day 1`, PID `1132376` |
| 2 | **~60** | 07:00 d1 | No crash. Persist over-6 = **0**. Seek logs still quiet | **PASS persist / crash through 864.** `Rejecting movement_report` = **0**. `Traceback` = **0**. `SOCIAL_SEEK:ARM` **4**, all → `Alex Butcher` until_step=105 — old once-a-day hunt, not a new everyday ship |
| 3 | **First cafe sits** (often **~120–240**) | morning | Linger **skipped.** Mill **0**. Leftover still minting | **Linger miss, skipped.** `SOFA LINGER` **0**. First leftover `LEFTOVER MINTED class=meet pair=Diana Ogden ↔ Max Shoemaker session=Diana Ogden_Max Shoemaker_120 step=121`. First sit-stop `SIT COMPLETE reason=beat_budget session=Diana Ogden_Max Shoemaker_120 lines=6 step=121 (no second dump)`. Hobbs-both mill **0** |
| 4 | **240–305** | 10:00–11:00 d1 | Occupancy **hold** (bar 12/15) + mill still 0 + leftover still minting | **PASS occupancy 15/15 @305** (bar 12). All 15 on cafe tiles. Mill still **0**. Linger still 0 (skipped) |
| 5 | **785–845** | 19:00–20:00 d1 | Vote hold (bar 12/15) | **PASS occupancy 15/15 @845**. Harvest freeze **@864** (20:20 d1) |
| 6 | **~1445** | 05:55 d2 | Headcount **14**. Premiere string still gone | **Not scored this freeze.** Inherit 2b unless the run is left live |
| 7 | **1745** | 11:00 d2 | Occupancy **hold** (bar **11/14**) | **Not scored this freeze.** Inherit 2b |
| 8 | **2285** | 20:00 d2 | Occupancy **hold** (bar 11/14) | **Not scored this freeze.** Inherit 2b |

Harvest freeze **@864** (20:20 d1). Founder skip linger. Do not patch. Day-2 occupancy **inherit 2b** unless founder leaves the PID running.

---

### 0. Preflight

- [x] `20260831-1` **not running**; do not resume
- [x] `20260831-2` **not running**; do not resume
- [x] `20260901-1` **not running**; do not resume (status `stopped`, step 665/666, PID null before deploy)
- [x] Local `ivan/linger-plus-10` @ **`ec8e2076`** pushed; `railway` fast-forwarded to that tip (never rebase / force-push `railway`)
- [x] VPS `git pull` in `/var/www/generative_agents` → tip **`ec8e2076`**. `SOFA LINGER` present in `social_will.py`
- [x] `sudo systemctl restart api-gateway` (only after the old PID is gone)
- [x] Unattended-upgrades will not restart that unit mid-run (`apt-daily-upgrade.timer` **inactive** / **disabled**)
- [x] Env unchanged vs 1-B: `HEADLESS_TAB_REUSE=false` · `TASK_DECOMP_CONTEXTUAL_ENABLED=true` · `MAX_TILES_PER_STEP=6` · `SURVIVAL_MODE_ENABLED=true`
- [x] Seek **off** (no everyday / multi-seek ship on this tip)
- [x] Fork + start `20260902-1`: sprint, `skip_premiere: true`, `max_steps: 2400`, **`diagnostic_mode: false`**
- [x] Engine clock jumped to day 2 ~05:55; step **0** (log: `Skip premiere: clock 2026-09-02 06:30:00 → 2026-09-03 05:55:00 (engine day 2 05:55); start_date unchanged, step=0`)

**Record here:** fork `2026-09-02T05:06:30.972427+00:00` · UUID `a1dcfc60-3791-4cb9-8dc2-526f78fddada` · PID `1132376` · tip `ec8e2076` · sim `20260902-1` · maze `150ec00d-dad9-4436-a42e-1e278c2d6aa3`

API status at start still said `current_day_label=null` / not Survival at step **0**. The runner log is the jump evidence. Re-check status at ladder row 1.

---

### Clock map (same as 1-B / `20260831-2`)

| Event | Clock | Step |
|---|---|---|
| Day-1 challenge | 11:00 | **305** |
| Day-1 vote | 20:00 | **845** |
| Day-2 05:55 | — | **1440** |
| Day-2 challenge | 11:00 | **1745** |
| Day-2 vote | 20:00 | **2285** |
| Day-3 replan overwrites day-2 | midnight | 2525 |

---

### 1. Linger +10 — **skipped**

Visual packing only: talk already over, bodies stay ~10 min. **Not** a deeper conversation.

Harvest through **864:** `SOFA LINGER` = **0**. `SIT COMPLETE` pair-rows **55** (`beat_budget`). `LEFTOVER MINTED` **36**. First sit-stop: `SIT COMPLETE reason=beat_budget session=Diana Ogden_Max Shoemaker_120 lines=6 step=121 (no second dump)`.

| # | Signal | Pass? |
|---|---|---|
| 1 | After **4–6 Hobbs full sits**: `SOFA LINGER` ≥ **1** | [x] **Miss. Skipped.** Count **0**. Founder: do not patch |
| 2 | `act_duration` +10 once; `act_address` unchanged | [ ] N/A — linger never logged |
| 3 | Same pair, same room, no second +10 | [ ] N/A |
| 4 | Hello / leave / desync / pause → linger **0** | [x] Vacuous — linger **0** on all reasons |

**Do not abort the village for this.** Occupancy already held without linger.

---

### 2. Hello mill (hold)

Bar: same pair, still at `the Ville:Hobbs Cafe:cafe`: they do **not** greet again inside **30** steps. Leave then a new hello is fine.

| Signal | Pass 2 (`20260831-1` @260) | 2B (`20260901-1` @619) | This run (`20260902-1`) |
|---|---|---|---|
| Greeting re-open within **30** steps, both still at `the Ville:Hobbs Cafe:cafe` | **54** | **0** | **0** through 864. **PASS** |
| Same-room re-open inside 30 (any room) | (Hobbs was the mill) | **0** | Not dumped this freeze (Hobbs-both is the mill bar) |

Hobbs-both greetings parsed **214**; re-open inside 30 = **0**. First cafe hello: `👋 GREETING TRIGGERED: Alex Butcher ↔ Max Shoemaker step=137 cooldown=5 reason=first_daily_fleeting arena_a=the Ville:Hobbs Cafe:cafe arena_b=the Ville:Hobbs Cafe:cafe`. Quiet lines **18981**.

**Pass mill** = Hobbs-both re-open inside 30 stays **0**. **PASS through 864.**

---

### 3. Leftover mint (hold)

KEEP already green. This tip must not kill it.

| # | Signal | Pass? |
|---|---|---|
| 1 | After 4–6 Hobbs full sits: `LEFTOVER MINTED` ≥ **1** (`meet` / `tell` / `avoid`; Survival may also `protect` / `expose` / `mutual_in`) | [x] **PASS through 864.** Mint **36**. Classes: `meet` **28**, `mutual_in` **4**, `avoid` **3**, `tell` **1**. First: `LEFTOVER MINTED class=meet pair=Diana Ogden ↔ Max Shoemaker session=Diana Ogden_Max Shoemaker_120 step=121` |
| 2 | Greeting-only sits → **0** leftovers | [x] **Holding.** First five mints are CM `session=` ids, not `greeting_`. Full greeting-leftover dump not repeated |
| 3 | Unique lines after 12 → **0** | [ ] **Not dumped this freeze.** First sit-stop `lines=6`. Cap not re-counted |

`LEFTOVER HONORED` **36** / `LEFTOVER BROKEN` **0** — still co-presence honor (mint+soon, same room). Not the later thought-choice check (launch 3b). `DESYNC REPAIR keep` **2** (`session=Diana Ogden_Vince Vale_733`). `desync_scratch_cleared` **0**.

**KEEP did not regress** on mint / mill / desync. Line-cap 12 not re-harvested.

---

### 4. Occupancy hold (not the bet)

Same bar as 1-B / 2b / 2B. If it dips, **stop and split** — do not patch on this tip. Do not restore the lock.

| Fire | Bar | 2B (`20260901-1`) | This run |
|---|---|---|---|
| Day-1 11:00 (step **305**) | **12/15** | **14/15** | **15/15 PASS** |
| Day-1 20:00 (step **845**) | **12/15** | inherit 2b **15/15** | **15/15 PASS** |
| Day-2 11:00 (step **1745**) | **11/14** | inherit 2b **13/14** | Not scored @864. Inherit 2b |
| Day-2 20:00 (step **2285**) | **11/14** | inherit 2b **12/14** | Not scored @864. Inherit 2b |

Day-1 11:00: all 15 on cafe tiles (e.g. Alex Butcher `(76, 25)` `Settling into a seat at the cafe`; Irene Dove `(75, 21)` `Listening to the challenge rules announcement (heading to Hobbs Cafe) @ the Ville:Hobbs Cafe:cafe`). Day-1 20:00: all 15 on cafe tiles (e.g. Irene Dove `(72, 25)` `waiting for the evening vote to commence`). Linger **0** did not empty the room.

---

### 5. Persist / whitelist / seek (hold)

| # | Signal | Pass? |
|---|---|---|
| 1 | Persist over-6 = **0** | [x] **PASS through 864.** `Rejecting movement_report` **0** |
| 2 | Seek not shipping everyday / multi | [x] **Holding.** `SOCIAL_SEEK:ARM` **4**, all `→ Alex Butcher until_step=105` — old morning hunt |
| 3 | Cafe whitelist still **no** `study` | [ ] Not re-read this freeze. Dest pile not the 1-A college set at 11:00 / 20:00 |
| 4 | Chat never writes `vote.target` | [x] **PASS 0** |

---

### Abort

**If `SOFA LINGER` is 0 after 4–6 Hobbs full sits**

**Recorded. Founder skip.** Do not patch linger. Do not hide the miss — occupancy held without it.

**If mill returns**

**Stop.** Do not call linger a pass on a hello mill.

**If leftover mint is 0 after 4–6 Hobbs full sits, or remix past 12 lines**

**Stop.** KEEP regressed.

**If occupancy dips below bar**

Split. Do not guess linger vs seek. Do not restore the lock.

**If the box is wrong (wrong tip, seek on, or `study` already on the cafe)**

Not a linger score. Tear down and restart.

---

### 9. Start payload (already used)

```bash
curl -s -X POST http://127.0.0.1:8001/api/simulations/fork \
  -H "Content-Type: application/json" \
  -d '{"sim_code":"20260902-1","baseline":"soul15_seed_20260224","description":"Linger +10 after full sit complete","copy_memories":true,"copy_coords":false}'

curl -s -X POST http://127.0.0.1:8001/api/simulations/20260902-1/start \
  -H "Content-Type: application/json" \
  -d '{"action":"start","parameters":{"max_steps":2400,"generation_mode":"sprint","skip_premiere":true,"diagnostic_mode":false}}'
```

Log path on the box: `/var/www/generative_agents/api_gateway/logs/sims/20260902-1.log`

---

## Decision

**Linger skipped.** Harvest `20260902-1` @**864**. Tip `ec8e2076`. `SOFA LINGER` **0** after **55** full sit-stops. That stay is visual packing after the talk is already over — not a deeper conversation, not the 11:00 crowd. Founder: do not patch, do not overnight again for linger.

| Item | Result | In this card |
|---|---|---|
| Preflight / tip `ec8e2076` / skip-premiere | **PASS** | §0 all `[x]` |
| §1 linger +10 | **Miss. Skipped** — count **0** | §1 |
| §2 hello mill Hobbs-both re-open in 30 | **PASS 0** through 864 | §2 |
| §3 leftover mint on full sits | **PASS** 36 mints. Line-cap 12 not re-dumped | §3 |
| §4 occupancy day-1 11:00 / 20:00 | **PASS 15/15** and **15/15**. Day-2 inherit 2b | §4 |
| §5 persist / seek / `vote.target` | **PASS** persist 0, `vote.target` 0. Seek ARMs are the old hunt | §5 |

**Holds that landed without linger:** mill, leftover mint, KEEP desync **0**, day-1 cafe full at challenge and vote.

Do not resume `20260831-1`, `20260831-2`, or `20260901-1`. Do not stamp linger into SOT. Next leftover story is launch **3b** (honor [A] / break [B] as an internal thought), not this stay.
