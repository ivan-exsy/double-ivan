# 20260903-2 — Headless tab-reuse snap (Item 7b)

**Sim:** `20260903-2`  
**Status:** **PASS** (spawn/home flash on a reused tab)  
**Stopped:** completed @ **31** (`total_steps` 32; last generated minute is step 31)  
**Do not resume.**

**Date:** 2026-09-03  
**Roster:** 4 Doubles (`base_family_sim`: Ivan, Luba, Gosha, Katya)  
**Harness:** skip-Premiere, Survival, sprint, `max_steps` 30 from the restart  
**BE tip on box at smoke:** `railway` @ `5b34c2bf` (drain 7a landed later the same day @ `efdaaf63`)  
**FE under test:** production `https://double-front.vercel.app` @ `14dc6ce` (includes snap `a373423`)  
**VPS `/var/www/double-front`:** after this card, box FE was moved to `vercel` @ `14dc6ce` (headless rebuild 2026-09-03 18:48 UTC). The smoke itself used Vercel CDN, not localhost.

---

## Decision

The spawn/home flash on a **reused Playwright tab** is gone on the production FE.

Playwright kept one tab from the restart through the end. Nobody reported origin `(0, 0)`. All four people filed a walk report every step. Reused steps logged `boot_ms=0`.

Box FE is now that commit. Scoring path is `FRONTEND_URL=http://localhost:3000` with `HEADLESS_TAB_REUSE=true`.

Drain (7a) and the database lockdown (7c) were **not** this sim. Both are live on the box as of 2026-09-03 evening (`railway` @ `efdaaf63`; systemd `TimeoutStopSec=960`).

---

## What this run was

FE shipped sprite snap + leftover-path flush to the production `vercel` branch (`14dc6ce`). Unit tests do not prove a live tab. This was the live check:

| Bar | Result |
|---|---|
| Same Playwright tab after the restart | **PASS** — tab reuse prepared the existing page for steps **3–31** (29 reused, no `goto`) |
| No one-frame jump to spawn/home `(0, 0)` unless `start_pos` is origin | **PASS** — **0** coordinate rows at origin; **0** origin strings in the soak log |
| All 4 walk their injected routes | **PASS** — 32/32 headless completes (steps 0–31); movement reports **4 ok / 0 rejected** whenever a batch was logged; **36** steps with a 1–6 tile move; **0** over-6 |
| Reused steps `tab_reused: true` / `boot_ms` ~ 0 | **PASS** — **20/20** logged reuse metrics have `boot_ms=0` (two cold boots only: aborted local process, then Vercel process) |

The FE letter asked for **≥ 30** reused-tab steps. This process started at engine step 2, so reuse is **29** steps (3–31). The original flash showed by step 5. **29** reused steps with **0** origin is enough. Do not rerun for the extra step.

---

## Harness (as run)

1. First start inherited old gateway env (`FRONTEND_URL=http://localhost:3000`, `HEADLESS_TAB_REUSE=false`). Stopped ~step 2. **Do not score those two minutes as the snap.**
2. Gateway restarted so the child could read `.env.local`. Second start: `FRONTEND_URL=https://double-front.vercel.app`, `HEADLESS_TAB_REUSE=true`, `skip_premiere=true`, `generation_mode=sprint`, 4-person fork of `base_family_sim`.
3. Skip-Premiere did jump the clock: `2026-09-03 06:30` → `2026-09-04 05:55`. Final: Survival Season Day 1, `curr_time` `2026-09-04T06:27:00Z`, `is_survival=true`.

Log: `/var/www/generative_agents/api_gateway/logs/sims/20260903-2.log`

---

## Numbers

| Signal | Value |
|---|---|
| Headless completes | 32 (steps 0–31) |
| `HEADLESS STRICT FAILURE` | 0 |
| Render failed | 0 |
| Tab reuse “prepared existing page” | steps 3–31 |
| Logged `tab_reused=False` | 2 (cold boots; `boot_ms` 495 then 450) |
| Logged `tab_reused=True` | 20 (all `boot_ms=0`) |
| Coord rows | 128 (4 people × steps 0–31) |
| Origin `(0,0)` | 0 |
| Persist over-6 | 0 |
| Stayed (in-place) | 88 |
| Moved 1–6 tiles | 36 |

`TELEPORT FIX` lines are the **6-tile speed cap** on a far plan (e.g. toward cafe `(77, 23)`), not a snap to home.

Reused-tab `animation_ms` / `total_ms` often print as 0–1. That is headless fast-complete, not a frozen village: people still moved 1–6 tiles on 36 person-steps.

---

## What this does **not** prove

- A second 30-step smoke **against localhost** after the box rebuild. The rebuild contains `flushResidualMotion`; the live Playwright pass was Vercel CDN. Extra localhost smoke is optional comfort, not a launch gate.
- 15-person scoring. 4-person was the asked roster; founder already treats 4 as enough for this class of bug.
- Viewer playback on `www.doubland.ai` (landing). Headless used `double-front.vercel.app`.
- Drain / clean restart (7a) — **not this sim.** Live later the same day on `railway` @ `efdaaf63` (`TimeoutStopSec=960`).
- Database RLS (7c) — already applied on `double-openrouter`; not exercised by walking.

---

## Follow-up

1. **Done.** Smoke env restored to localhost; later the box FE was rebuilt at `14dc6ce`.
2. **Done 2026-09-03 18:51 UTC.** `/var/www/double-front` on local branch `vercel` @ `14dc6ce`; `npm run build:headless`; leftover June `next-server` killed (port was stuck); `KillMode=mixed`; `HEADLESS_TAB_REUSE=true`; `FRONTEND_URL=http://localhost:3000`.
3. **Done 2026-09-03 18:59 UTC.** Drain 7a live on the box (`TimeoutStopSec=960`). Do not restore 30s.
4. Do not resume `20260903-2`. Next long score (if any) is the first 15-person run with tab reuse **on** — confidence, not a village-MVP ranked item. Seek stays Hold / not MVP.
