# 20260724-2 — Checklist: score re-prove after headless ready fix

**Purpose:** Full Survival score run after P0 headless fix. Re-prove hygiene + path persist with **real** headless acceptance; finish open gates from `20260723-1` / `20260720-1`.

1. **No screenshots** on the hot path (default)  
2. **Chrome recycle every 500** successful headless renders  
3. **Headless validation real** — sustained `15/15` ACCEPTED (not blind intent-only)  
4. **Reverie RSS creep** time series (`scripts/monitor_reverie_rss.sh`)  
5. **Path persist (slice A)** ≥95% usable `actual_path` on travel steps  
6. **Path playback (slice B)** — live + CDN tween (FE; after §11)  
7. **Self-serve epic** dormant (D7) — remaining signed-in / Look / Join items  
8. **Carry-over natural movement + Survival** (§12) — open human / analyzer gates  

**Context:** `done/20260723-1_checklist.md` · `done/20260723-1_RCA.md` · `20260723_reverie_rss_creep.md` · `20260723_eng-request_path-animation.md` · `20260720-1_RCA.md`  
**Prior score (failed headless):** `20260723-1` — soak `accepted=0/15`, path coverage **0%**, wall clips from intent clamps.  
**P0 smoke (PASS):** `20260724-1` — 100 steps, **15/15** every step, path coverage **100%**, human visual OK.  
**Posture:** No landing CTA; open a QA world only for optional §9.

**Score sim:** `20260724-2`  
**Prior score sims:** `20260723-1` (hygiene paper; headless FAIL) · `20260720-1` (analyzers ~7532; human A/B residue)  
**BE deploy / commit:** VPS still on older tip while this sim runs. **`origin/railway` tip `b230c4be`** (2026-07-28) = place-language P0+P1 (`87644031`) + post-chat assessor harden / consent auto-learn / idle sweep — **pull + `double-api` restart only after this sim fully stops.**  
**FE (VPS headless):** rebuilt against BE Supabase `kkjhsozszgoorwehhsdg`; `FRONTEND_URL=http://localhost:3000`  
**FE viewers (slice B):** human OK on prod/local 2026-07-28  
**Signed-in account:** `ipistsov@gmail.com` (owned Double `owned-c2677e73` / `ipistsov's Double`)  
**RSS CSV path:** `/tmp/reverie_rss_20260724-2.csv` → copy to: `storage/20260724-2/analysis/reverie_rss_20260724-2.csv`  
**Start:** 2026-07-24T20:14:04Z · **7600** steps · sprint · `diagnostic_mode=false` · runner PID **1661266**  
**Stop @ 2310:** Vincent Slater elim → headless false-positive `14/15` abort (fixed). **Resume:** 2026-07-26 ~23:44Z · `max_steps=5290` · PID **1801440** · post-elim expected **14/14**  
**Second abort @ 3620:** intent-persist partial write (Diana/Irene/Owen) → fixed + resumed 2026-07-27; runner PID **1907910**  
**Human review window:** 2026-07-28 ~step **6050–6082** · Survival Day 4 · ~**12** alive · headless **12/12**  
**Env:** `HEADLESS_MOVEMENT_ENABLED=true` · `HEADLESS_STRICT_ABORT=true` · `INTENT_PERSIST_HARD_FAIL=true`  
**Auto-watch:** re-armed post intent-persist resume (see `ops/20260724-2_watch_state.json`); expected_accepted tracks alive cast  
**RSS monitor:** still running (nohup `SIM=20260724-2`)

> **Do not close the run without §10b** (copy CSV + one-paragraph read).  
> **Do not declare path/movement ship without §3 + §11 green on this sim.**

---

### 0. Before the run — env armed?

- [x] Deployed code includes screenshot gate + browser recycle + **headless sim_id override** — VPS `railway` @ `553d6e81`
- [x] Screenshot default **off**: `HEADLESS_SCREENSHOT_EVERY_N=0` (or unset)
- [x] `HEADLESS_BROWSER_RECYCLE_EVERY_N=500` (or unset — default `500`)
- [x] Phaser loop on: `HEADLESS_MOVEMENT_ENABLED=true`
- [x] `HEADLESS_STRICT_ABORT=true` (abort on bad acceptance — keep for this score)
- [x] `diagnostic_mode=false` · sprint · target **7600**
- [x] FE headless host on correct Supabase (not stale `jawqll…`)
- [x] BE self-serve stack present; `CHAT_REQUIRE_AUTH` false/unset; public sims `self_serve_access=closed`
- [x] RSS monitor scripts on VPS: `scripts/monitor_reverie_rss.sh`, `scripts/snapshot_sim_chrome_mem.sh`
- [x] Note: `SIM=20260724-2` · start step `0` · UUID `736e241a-8917-4a4b-828a-01dd6c93d929`
- [x] P0 smoke `20260724-1` already proved ready + path coverage before this score

```bash
grep -E 'HEADLESS_SCREENSHOT_EVERY_N|HEADLESS_BROWSER_RECYCLE|HEADLESS_MOVEMENT|HEADLESS_STRICT|FRONTEND_URL' \
  /var/www/generative_agents/.env.local
```

---

### 1. Screenshots stopped

**Pass:** `rendered/` does not grow with per-step PNGs.

- [x] Midflight (~100+ steps): no new `step-*.png` (unless `EVERY_N>0` on purpose) — **cp100 @ step ~111:** `png_count=0`, `rendered/` absent
- [x] At stop/resume (~2310+): still no `rendered/` dir / png growth — **PASS** 2026-07-26 (contrast: `20260720-1` ~269 MiB). Reconfirm at ~6080: **`rendered/` still absent** 2026-07-28. Final end-of-run reconfirm when sprint completes.

```bash
SIM=20260724-2
ls -lt /var/www/generative_agents/environment/frontend_server/storage/$SIM/rendered 2>/dev/null | head
du -sh /var/www/generative_agents/environment/frontend_server/storage/$SIM/rendered 2>/dev/null
```

**Fail:** new PNG every step with default env → wrong code/flag on VPS.

---

### 2. Chrome recycle every 500 successful renders

**Pass:** recycle after ~500 / ~1000 **successful headless completions**; Chrome count not unbounded; steps keep advancing.

**Note:** Counter is **completions in the current process**, not sim step alone. Clean start from 0 ≈ steps 500/1000. Log `step=` = sim step at recycle time. If literal log missing, **RSS proxy** (`chrome_rss_mb` drop ≥40 MB near 500/1000/2000) is acceptable — as on `20260723-1`.

- [x] ~500 completions: recycle log **or** RSS chrome drop — **PASS via RSS:** chrome_rss **714.0→626.1 MB** @ steps **498→508** (Δ**-87.9**); count stayed **7**
- [x] ~1000 completions: second recycle — **PASS via RSS (lagged sample):** chrome_rss **675.3→629.0→612.1 MB** @ steps **1038→1049→1054** (peak drop ≥**46 MB** from pre-window); count stayed **7**; then climbed again @ 1093
- [x] ~2000 completions (bonus): third recycle — **PASS (restart signature):** chrome_rss was already low (~**366 MB** @ 1994), then **jumped to ~615 MB** @ **2005** after cp2000 (fresh browser after recycle); count stayed **7**; steps still advancing @ **2016**
- [x] `backend_process_active=true` and `current_step` still climbing after recycle — step **520** still generating; still climbing @ **1049** after cp1000
- [x] Chrome/Chromium count does not grow without bound — stable **7** through cp500 / cp1000
- [x] Post-recycle: movement reports / coords still update — headless still completing; steps advancing

```bash
curl -sk https://127.0.0.1:8001/api/simulations/$SIM/status \
  | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('current_step'),d.get('backend_process_active'),d.get('last_generated_at'))"
ps aux | grep -E '[c]hrome|[c]hromium' | wc -l
```

**Fail:** no recycle after ~500 completions · Chrome pile-up · freeze / no movement after recycle.

---

### 3. Regression — validation still real (P0 for this paper)

*Was FAIL on `20260723-1` (headless never ready). Smoke `20260724-1` PASS — re-prove on long run.*

- [x] Soak / probe: sustained **15/15** `[CP-8] ACCEPTED` through Day‑1 elim; abort at **2310** was elim false-positive (`Vincent Slater` still in expected set) — **fixed** (exclude `season.eliminated`). Post-resume: sustained **14/14** @ **2311–2313+**; later **12/12** after further elims (human confirm 2026-07-28 ~step 6050+)
- [x] Log shows collision sim_id override for this UUID at start — `736e241a-…` at boot
- [x] Steps advance post-resume; path coverage **100%** on existing travel steps (§11) — soft midflight OK
- [x] Not a silent “headless disabled” path — Playwright/Chrome under runner (~7 procs)
- [x] Soft: multi-hour / multi-day without OOM kill of `double-api` — **PASS so far** (same runner PID **1907910** through Survival Day 4 / ~6080+; reconfirm at final stop)

```bash
# Local evaluate (preferred)
python ops/ops_watch_20260724-2.py evaluate
# Or on VPS:
grep -ac 'HEADLESS STRICT FAILURE' /var/log/soak/20260724-2.log
grep -aoE 'accepted=[0-9]+/[0-9]+' /var/log/soak/20260724-2.log | sort | uniq -c | sort -rn | head
```

**Fail:** `accepted=0/15` · `headless_ready_false` · `collision_manager_bound=False` · strict abort storm.

---

### 4. Viewer coexistence

- [x] Gateway up — `GET /health` healthy; status API OK — **PASS** 2026-07-26 (`{"status":"healthy"}`; sim `running` post-resume)
- [x] Open **`20260724-2`** (or known prod sim) **without login** — playback/sprites normal — **PASS** human 2026-07-28 (~Day 4 / step ~6050)
- [x] Cast / Survival chat still works without forcing login (gate off) — **PASS** human 2026-07-28

**FE/Landing follow-ups (chat bubbles — not hygiene; carry from `20260720-1`):**  
**minimize button no-op** — **RESOLVED** (FE 2026-07-28).  
**dynamic tip-to-head** — **RESOLVED** (FE 2026-07-28; tip aims at speaker mouth / longer reach).

---

### 5. Auth + ownership — **PASS human 2026-07-28**

- [x] No JWT → `GET /api/me/double/avatar` (or `/double`) → **401** — proven on prior score / deploy
- [x] Signed in → owned Double resolves — **PASS** human 2026-07-28 (`ipistsov@gmail.com` via [double-front `/auth`](https://double-front.vercel.app/auth); Talk/Join flows load personal Double — not Survival cast Ivan)
- [x] Soft: account has a working owned Double link — implied by Talk + Join look step

```bash
curl -sk https://127.0.0.1:8001/api/me/double/avatar -o /dev/null -w "%{http_code}\n"
# With Bearer $TOKEN:
curl -sk https://127.0.0.1:8001/api/me/double -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

---

### 6. Assess → Talk — **PASS* human 2026-07-28** (session-end learning open → fixed on `railway`, not VPS yet)

*Verified on [double-front.vercel.app](https://double-front.vercel.app) with `ipistsov@gmail.com`.*

**Cast vs owned:** Survival cast Doubles (e.g. Ivan Pitts) are admin-seeded — **no product claim path**. §5–§7 mean **owned Double** via quiz/interview/`dbl_agent`. Sign-in: `/auth` (not sim page / not invite-only landing). Magic-link redirect allowlist was fixed to `https://double-front.vercel.app/auth/callback`.

- [x] Quiz OK (or profile already has Big-5) — `/onboarding/quiz` showed completed personality snapshot
- [x] Interview / talk-ready — `/onboarding/interview` answered fully 2026-07-28; interview already **publishes** into `soul_seeds` on submit (`prediction_ready`)
- [x] Talk: 1–2 messages — `/onboarding/talk` prior context + “what do you know about me?” answered from interview; founder: felt accurate to current situation
- [x] **Diagnosis (End session / learning) — 2026-07-28:**
  - Chat rows stay in `double.user_chat_messages` even when assessor fails (not lost).
  - Rate limit: **2 threads/hour** (`chat_count_recent_threads`) — blocked a retest until recent threads cleared.
  - Short session → `insufficient_turns` (need ≥3 user turns / ≥120 chars) — expected skip.
  - Adequate session (3 turns / 378 chars) → BE `assessor_error`: **“Assessor returned empty response”** (tier-C empty content). Assessment `6793ad04-…` stored skipped; **no pending_review card**.
  - FE bug: Talk checked only `skipped: true`, so `status: "skipped"` still showed optimistic “suggestions will appear…” — fixed on FE branch `ivan/post-chat-session-end-skip-ux` (not on `vercel` yet).
- [x] **BE fix landed on `origin/railway` @ `b230c4be` (not VPS-deployed yet):** assessor retry + tier-A fallback; skip payload includes `skipped: true`; **consent ON → auto Path A apply on End session** (no Confirm wait); idle sweep (~16 min) as safety net. Confirm path still shared for consent OFF / force.
- [ ] **Re-validate after VPS pull:** End session with consent ON → `auto_applied` / life chapter updates; skip reasons honest; idle/leave/reopen learning (FE `ivan/talk-session-auto-close`)
- [ ] Soft: post-chat review confirm/dismiss when consent OFF — still available; not exercised this pass
- [ ] Backlog: **PM-OWN-1** “What my Double knows about me” (`double-docs/TODO_post_mvp.md`) — read/edit life-chapter anytime

---

### 7. Look (D8) — **PASS* human 2026-07-28**

- [x] Avatar / look catalog available — appeared when opening `/onboarding/join-world` (pick look)
- [x] **Change look** reachable in product — pick-look step shown on Join flow
- [ ] Soft: persist after refresh + not stuck in `missing_personas` — not separately re-checked (optional)

---

### 8. Join World — dormant — **PASS human 2026-07-28**

- [x] Signed-in Join empty — FE: **“No worlds available to join right now.”** (expected while Survival / public sims stay `closed`)
- [x] FE Join World empty state is OK (not an error) — same message; not a crash

```bash
curl -sk https://127.0.0.1:8001/api/me/double/bind/sims \
  -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

---

### 9. Optional punchline (demo only)

*Open one QA world deliberately, then close it. Skip for dormant D7.*

- [ ] Set one QA world `private` + allowlist (or `open`) briefly
- [ ] Look → Join → job/home → finalize
- [ ] Roster + job/home in scratch; atlas present; map coords seeded at latest step
- [ ] Set world back to **`closed`**

---

### 10. Reverie RSS creep — start monitor + check results

**Pass:** CSV from near step 0 → end (≥1500 steps preferred). Headless stays ON; observe-only.  
Details: `done/20260723_reverie_rss_creep.md` (or sibling doc if still at repo root).

#### 10a. START — within ~5 min of sim start

```bash
cd /var/www/generative_agents
SIM=20260724-2 INTERVAL_SEC=300 bash scripts/monitor_reverie_rss.sh
# nohup / tmux; leave for full sprint
tail -f /tmp/reverie_rss_20260724-2.csv
```

- [x] **Started** with sim — first sample **2026-07-24T20:14:52Z** (step blank, reverie **188.3**, chrome **0.0**, host_avail **2931**)
- [x] Confirmed CSV appending every ~5 min through midflight — **~613 rows**, steps **~20→2311** (continues post-resume)
- [ ] Soft mid-run: `SIM=20260724-2 bash scripts/snapshot_sim_chrome_mem.sh` if Chrome looks weird

#### 10b. AFTER RUN — check results (before declaring hygiene/RSS done)

- [x] Monitor still had rows through stop-at-2310 / resume — last midflight sample step **2311** (monitor still on)
- [x] Copied `/tmp/reverie_rss_20260724-2.csv` into `storage/20260724-2/analysis/reverie_rss_20260724-2.csv`
- [x] Filled **RSS CSV path** at top of this checklist
- [x] One-paragraph read written below (midflight; refresh at final stop):
  - `reverie_rss_mb` vs step → linear / plateau / bands?
  - `chrome_rss_mb` flat or climbing? recycle drops near 500/1000/2000?
  - `assoc_disk_mb` flat while RSS climbs (or tracking)?
  - `png_count` / `rendered_disk_mb` flat with screenshots off?

**RSS read (fill in):**  
Through ~2311 (~609 stepped rows): `reverie_rss_mb` ranged **~180–614** with climb + bands (not a pure linear creep). `chrome_rss_mb` climbed toward **~714** then showed recycle drops near **500 / 1000 / 2000** completions (§2). `assoc_disk_mb` stayed ~**16** while process RSS moved — disk assoc not tracking RSS. `png_count` / `rendered/` empty with screenshots off. Resume boot briefly spiked reverie RSS (~314) as Chrome returned (~605). Reconfirm paragraph at final stop.

**Fail:** forgot to start monitor · started late mid-run only · no CSV at end · no read after stop.

---

### 11. Path persist (slice A) — **PASS on this score (chunked re-prove)**

**Pass:** travel steps have usable `actual_path` (or array `path`) in Supabase. Does **not** prove FE tween (slice B).  
**Smoke:** `20260724-1` → **100%** (228/228) — expect similar on this score.

```bash
python scripts/analyze_travel_path_coverage.py 20260724-2 --min-pct 95
```

> **Note (2026-07-28):** stock one-shot RPC silently truncates at PostgREST ~1000 rows (~194 travel). Trust **chunked** coverage (`scripts/_probe_path_chunked_20260724-2.py` / focused probe).

- [x] Deploy includes path-persist migration + gateway `actual_path` readback
- [x] Headless ON for this score sim (FE emits `actual_path` when ready)
- [x] Coverage script PASS (≥95% of travel steps) — midflight **100%** (194/194) @ ~2310; **chunked re-prove 2026-07-28 @ ~6083:** **100%** (`16141/16141` travel)
- [x] Spot-check one commute step in DB: `movement.actual_path` is a waypoint list (not null) — **Diana Ogden** step **6083** path_len **7** (`has_actual_path=true`); early Alex Butcher step 1 also OK
- [x] Soft: gateway step API returns `actual_path` for that step — same movement blob SOT
- [ ] Soft: contrast only — `20260723-1` remains 0% (no retroactive fix expected)

**Fail:** coverage below bar · `actual_path` still absent on moving steps after headless reports.

---

### 11b. Path playback (slice B) — **PASS human 2026-07-28**

**FE status (as of 2026-07-23):** Slice B on `double-front` `local` — clamp keeps walked prefix; CDN maps multi-waypoint `actual_path`. Scrub **drag** may snap; continuous walk = **play / LIVE**.  
**Depends on:** §11 PASS on **`20260724-2`** + FE build on the score stack viewers.

- [x] FE `local` (or promoted) build with emit + replay harden is what viewers use — human confirm 2026-07-28
- [x] **Live gateway / websocket viewer:** doubles move gradually, no snap-per-step observed — **PASS** human 2026-07-28
- [x] **CDN / replay viewer:** same stored steps also animate (no strip of `actual_path`) — covered by same human pass (no snapping)
- [x] Missing-path steps fall back honestly (snap OK) — no fake invented path — soft OK
- [ ] Soft: scrub **drag** may teleport (documented); play/LIVE must walk — not re-checked

**Fail:** §11 green but both viewers still snap · CDN drops `actual_path` · live OK / CDN broken (or reverse).

---

### 12. Carry-over — open gates from `20260720-1` / `20260723-1`

**Source:** `20260720-1_RCA.md` §A–H · leftover rows in `done/20260723-1_checklist.md`.  
**Prefer validate on `20260724-2`.** Analyzer baselines from `20260720-1` (~7532) still useful:

| Counter | `20260720-1` | vs `20260717-1` |
|---------|--------------|-----------------|
| TELEPORT @ sr1 | **0** | ~1818 |
| Gap1 / Gap2 non-worker | **28 / 17** | 676 / 599 |
| Address divergence | **46** | 3154 |
| Piano false / real | **297 / 73** | want ≈0 false |
| Wait-wrap / `<waiting>` | **0** | — |
| CHAT_MISSING_PAYLOAD (R8) | **0** | — |
| Coord jump max / step | **6 tiles** | clamp OK |

#### 12A. Walk feel / no blink (R3) — **P0 human**

- [x] Prior human (`20260720-1` / smoke `20260724-1`): continuous travel, no town hop / no ping-pong
- [x] **This score sim:** 2–3 building changes — continuous multi-minute travel, not map hops — **PASS** human 2026-07-28 (~Day 4)
- [x] No arrive-one-tick / leave-next ping-pong — **PASS** human 2026-07-28
- [x] **No wall clips** (was FAIL on `20260723-1` while headless blind) — **PASS** human 2026-07-28 on this score
- [x] Soft after §11 + **11b**: path tween visible — **PASS** human 2026-07-28 (gradual motion, no snapping)

#### 12B. Hobbs staff zones (R1) — **PASS analyzer; human visual explained**

- [x] Prior: busy cafe crowd in customer seating / public (not kitchen invasion) — PASS on `20260720-1`
- [x] Spot-check **Max Shoemaker** / **Irene** behind counter — human saw **Max 18:04 Jul 27** + **Irene 18:16 Jul 27** behind counter; **both have `work_area` = Hobbs Cafe** on this cast → staff presence is allowed (not a non-worker invasion)
- [x] Soft: full house = shared seats OK, not musical-chairs teleports — PASS prior
- [x] Optional Gap1/2 on **this** score sim — **PASS 2026-07-28** with worker bypass: **Gap1=0 / Gap2=5** (Owen pub-bar×2, Nick supply-counter×3) ≪ 676/599; Hobbs counter Gap2 for non-workers = **0**

#### 12C. Honest place language (R2) — **FAIL on this score; fixed on `railway`, awaiting VPS + re-score**

- [ ] Walking: `heading to` + `@` = presence (not already far destination) — **FAIL** human 2026-07-28 **Vince Vale** (Jul 28 morning):
  - 07:44 status “Walking to Hobbs Cafe” — actual walking outside past cafe
  - 07:52 still “Walking to Hobbs” — actual arrived Johnson Park
  - 08:10 “Ordering breakfast at the counter” — actual still walking toward cafe
  - 08:19 “Ordering…” — actual in cafe seating
  - 08:27–08:36 “Walking to Hobbs Cafe” — actual leaving cafe / in Johnson Park
  - 08:46 “Reviewing lecture notes over coffee” — actual walking from Johnson Park  
  **This sim stays FAIL.** Close only after deploy + human/analyzer pass on `20260728-1` (or equivalent).
- [x] Stationary: `@` and `address_label` agree — PASS prior (and in Vince window `@` tracks body)
- [x] No clear place fights (e.g. work-at-Willows `@ Hobbs`) on prior spot-check
- [x] Re-check on **`20260724-2`** midflight / human pass — **done; FAIL (Vince Vale windows above)**
- [x] **RCA** (`20260728_rca_12c_place_language.md`): DB steps **5827–5895** — `intent`=“walking to Hobbs” while planned/heading=**Johnson Park** (52 steps); “ordering…” before Hobbs (10 steps). Emit glues conflicting act + planned dest; UI shows raw `intent`.
- [x] **Deeper RCA (why Park while words say Hobbs) — 2026-07-28:** Not a bad Hobbs resolver. At assign step **5827**, decomp sealed an ACTION CONTRACT for `walking to Hobbs Cafe` whose `resolved_address` was already **Johnson Park**. Chain: (1) contextual decomp tags the travel row `activity_type=exercise` (LLM; “walking…”); (2) row inherits a non-exercise parent address (`INHERIT POST-VALIDATE`); (3) whitelist post-validate Tier-2 remaps exercise → nearest exercise arena = Johnson Park; (4) assign short-circuits on that sealed contract — feet follow Park, label still says Hobbs. Same family at steps 30 / 1780 / 2017 / 5874. Fresh `resolve_location("walking to Hobbs…")` alone returns Hobbs. Contributing: `_label_is_travel` omits `"walking to "` (only `"walk to "`), so travel rows inherit parent too often.
- [x] **P0+P1 on `origin/railway` @ `87644031` (merged; tip now `b230c4be`):** emit travel-safe intent + named-dest keep / `walking to` travel / contract reject. Unit tests on branch. **Not VPS-deployed** while `20260724-2` runs. Re-score checklist: `20260728-1_checklist.md`.

**Fix summary (P0 + P1) — now on `railway`**

| Layer | What it fixes | Player-visible result |
|-------|----------------|------------------------|
| **P0 — Status honesty** | Card said “Walking to Hobbs” while feet went to Johnson Park (or “Ordering…” while still en route) | Status matches where the plan is actually sending them |
| **P1 — Plan destination** | “Walking to Hobbs” was mis-tagged as exercise and remapped to the park | Named commute (Hobbs / Oak Hill / …) keeps that place; no more Park bounce |

Full RCA: `20260728_rca_12c_place_language.md`. **Next:** stop this sim → VPS pull `railway` → start `20260728-1` → close §12C if clean.

#### 12D. Piano only when playing (R6) — **PASS this score**

- [x] Non-play acts **not** parked on `:piano` — **0** false (was **297** on `20260720-1`)
- [x] Real play still lands on piano — **673** real play hits
- [x] Re-measure V2 on **this** sim — bar ≈0 false — **PASS 2026-07-28** (focused probe @ ~6083)

#### 12E. Prop / leaf stability (R7) — **P2**

- [x] Classroom teach stays on podium/board — PASS soft prior
- [x] Cafe coffee stays on a seat while task stable — PASS soft prior
- [ ] Soft re-spot on this sim (optional)

#### 12F. Analyzer truth (R4 / R5 / R8) — **mostly PASS**

- [x] R8: no `(from description)` chat-shell flood — **0** `chat_missing_or_from_description` @ ~6083
- [x] R5: Apartment-N noise — only wrong-home / foreign apartment matters — **118** foreign hits after living_area filter (soft residual; not the raw 6.8k false-positive without homes)
- [ ] R4: if playback still feels deep-chat ping-pong, open **separate** social-policy task — **do not** ship gather pair-mute; cafe chatty is OK

#### 12G. Survival regression — **this run is Survival — mostly PASS**

- [~] Gather **spatial_gate** still fires Day 2+ chal/vote — **votes Day 1/2/3 concluded** in soak; elim sentences for **Alex Butcher (Day 2)** + **Alex Shepard (Day 3)** + Vincent Day‑1 elim known from abort. Soak has **0** `reason=spatial_gate` / `survival_phase_trigger` strings (cannot prove gate vs deadline from soak alone)
- [x] No sleep-stuck / sleeping-1440 / empty-tally elim regression — **0** `sleeping_1440` / `empty_tally` tokens; Day 3 tally named live voters (not empty)
- [x] `BACKEND_INTENT_ONLY_PATH` still default — confirm on VPS `.env.local`
- [x] Wait-wrap / nested `<waiting>` still ~0 — **0** / **0**
- [x] Workers still allowed on staff leaves in work area — Max/Irene/Olivia Hobbs work_area; Gap1=0 after bypass

#### 12H. Light soul carry-over (optional)

Only if product wants Light ship on this long Survival (`max_steps` ≥ ~7500–9000).

- [ ] Light soul reasons skim (Day 4+ IDs that ran)
- [ ] Blind triad Ivan · Diana · Mike after `challenge_blind_choice` package
- [ ] Attraction Day 4+ soft
- [ ] Full fidelity / Light ship call only after above

**Skip 12H** if Light stays deferred.

---

### Ops / stop hygiene

- [x] Auto-watch loop left on until terminal **or** explicitly stopped — **re-armed** 2026-07-26 post-resume (`expected_accepted=14`); **disarmed** 2026-07-27 after step-3620 abort
- [ ] Prestop harvest (RSS CSV + soak snippets + chrome snapshot) before stop — adapt `scripts/harvest_prestop_20260723-1.sh` for `SIM=20260724-2` or run by hand
- [x] Stop with `{"action":"stop","parameters":{"force":true}}` if graceful hang — process already dead; status forced to **stopped** in Supabase
- [x] Confirm status **`stopped`/`completed`**, runner gone, Chrome drained — stopped @ **3620**; Chrome 0
- [x] Abort reason documented: step **2310** Day‑1 elim of **Vincent Slater** → headless expected still 15 → `HEADLESS_STRICT_ABORT` (fixed + resumed)
- [x] Second abort: step **3620** — real headless miss `accepted=11/14` missing **Diana Ogden, Irene Dove, Owen Logan** → `HEADLESS_STRICT_ABORT` (not elim-related)

---

### Verdict (fill as gates close)

| Gate | Result | Notes |
|------|--------|-------|
| **0 env / deploy** | PASS | `553d6e81` · strict abort · FE Supabase aligned |
| **1 screenshots off** | PASS | midflight + @~2310: no `rendered/` |
| **2 recycle @ 500** | PASS midflight | 1st **714→626** @ 498→508; 2nd **675→612** @ 1038→1054; 3rd restart **366→615** @ 1994→2005; count=7 |
| **3 validation real** | PASS* | 15/15→14/14→**12/12**; multi-day runner PID 1907910 alive through Day 4 |
| **4 viewer** | PASS | no-login playback + Survival chat OK 2026-07-28; bubble minimize + tip→head **RESOLVED** (FE) |
| **5 auth / ownership** | PASS | `ipistsov@gmail.com` magic-link → owned Double on Vercel 2026-07-28 |
| **6 assess → Talk** | PASS* | quiz/interview/Talk OK; End-session learning FAIL on prod (empty assessor + FE skip copy); fixes on `railway`/`ivan/*` FE — re-validate after deploy |
| **7 look (D8)** | PASS* | look picker on Join flow; persist soft open |
| **8 join dormant** | PASS | “No worlds available…” empty state (not error) |
| **9 punchline** | SKIP | dormant D7 |
| **10a RSS started** | PASS | with start @ 20:14Z |
| **10b RSS CSV + read** | PASS midflight | copied + paragraph; refresh at final stop |
| **11 path persist** | PASS | chunked **100%** `16141/16141` @ ~6083; late Diana path len 7 |
| **11b path playback** | PASS | human 2026-07-28: gradual motion, no snapping |
| **12A walk / walls** | PASS | human 2026-07-28 this score |
| **12B Hobbs counter** | PASS* | Gap1/2 **0/5**; Max/Irene are Hobbs **staff** (counter OK); human sighting explained |
| **12C place copy** | FAIL* | Vince FAIL on this score; P0+P1 on `railway` @ `87644031`+; needs VPS deploy + `20260728-1` re-score |
| **12D piano** | PASS | false **0** / real **673** |
| **12E sticky leaf** | PASS* prior | |
| **12F analyzer** | PASS* | R8=0; R5 foreign apt **118**; R4 human-only |
| **12G Survival** | PASS* | Day1–3 votes + Day2/3 elims; no empty-tally/sleep-1440; spatial_gate token absent in soak |
| **12H Light** | OPEN | Day 4+ available to skim (~6050+) |
| **Ship hygiene?** | PARTIAL | 1+2+3+10 midflight OK; finish run / refresh at stop |
| **Path persist A?** | PASS | confirm again at final stop if desired |
| **Natural movement ship?** | OPEN | 12A/B/D + §11/11b PASS; **12C FAIL** blocks ship until `20260728-1` |
| **Steps to 7600** | RUNNING ~6113+ | Day 4; ~12 alive; finish run / prestop / RSS still open |
| **`railway` tip (remote)** | `b230c4be` | place-language + post-chat auto-learn/idle — **not on VPS until stop** |

**Human visual (`20260724-1` smoke, 2026-07-24):** PASS — looks great (spot-check before score start).

**Human visual (`20260724-2` score, 2026-07-28 ~Day 4):** PASS walk/walls/no-login viewer; **FAIL** place-language lag (Vince Vale); Max/Irene behind counter = **staff** (not Gap2 fail); chat-bubble minimize + tip→head **RESOLVED** (FE).

**Analyzer pass (2026-07-28):** §11 chunked path 100%; Gap1/2 0/5; piano false 0; R8 0; wait-wrap 0; Survival votes Day1–3 + elims Day2/3.

**Self-serve / Talk learning (2026-07-28):** Owned Double path works (quiz→interview→Talk). Session-end assessor empty + FE optimistic skip copy diagnosed; BE on `railway` `b230c4be`; FE skip UX + leave/idle/reopen on `ivan/post-chat-session-end-skip-ux` / `ivan/talk-session-auto-close` (not `vercel` yet). Re-validate after BE VPS + FE promote.