# 15 Sep — talk unmute score (`20260915-3`)

Completed **500 / 14:15**. Do **not** resume `20260915-1` / `20260915-2` / this sim. Do not Join Breakfasts. Do not raise talk-chance. Do not walk-to-person.

Trail / verdict: [`20260915_TODOs.md`](20260915_TODOs.md) **After Fable**.

## Shipped 2026-09-15

### BE
<plan - C:\Users\ipist\.cursor\plans\talk_unmute_next_pass_c8fb406d.plan.md>

- **Plant owns the morning plan.** Cafe sit is schedule row 0 (≥45 min) for the planted pair, same Hobbs tiles as coords. Tiles + caption without the plan will stale-replan them home in ~6 minutes.
- **P1a — story thoughts must not forbid talk.** Leftover / stance / challenge overlay stay memorable (poignancy 8). They must not set pressure ≥ 0.5 / `_hard_stop`. Accidental-deep-chat may promote a sit; it must not mute.
- **P1b — drop remaining-slice as a hard stop** (preferred). Do not treat an expired or held walk as “about to leave.” A body already sitting in the cafe is free.
- **Log which `hard_stop` fired** (`pressure` / `slice` / `sleep` / `prewindow` / `chatting`). No behaviour change; otherwise P1 looks like a fail.
- **P2 hygiene — seek no-op** if both bodies already share the same 3-part cafe. Cross-town seek can stay. Do not headline this as the V2 fix.
- **P2b — proximity uses the body, not the dest string.** A `<persona>Name` dest must not skip the talk scan when both people are already in Hobbs. Otherwise P2/P3 never run.
- **P3 — same room skips Manhattan `too_far`.** Street keeps the short rule. Do not wait for a 2-tile dawn hold.
- **P1c — `PRESSURE_KEYWORDS` in the current action** (`problem`, `issue`, …) must not set pressure ≥ 0.5. Same gate as P1a; leftover landmine.

### FE

Prod: `double-front` `e9f2107` on `vercel` ([double-front.vercel.app](https://double-front.vercel.app)). Hard-refresh Watch. Do not treat a reload of `20260915-2` as the score. T-W4 catch-up (`exitLiveMode`) left alone.

- **T-W4 — LIVE at 0 fills without reload.** Status polling stays on while the sim is `running`. Catch-up does not `exitLiveMode`. **Field PASS** on `20260915-3`.
- **T-W5 — Play at the live tip resumes.** Loop stays alive across pause. Wait copy: “Waiting for next moment…”. **Field:** LIVE **1×** at the tip on this URL (`~06:42` / `t=49`). Wait-then-continue when the next minute lands — confirm if the engine pauses again.
- **Cliff speed — LIVE 1× near the edge and while waiting.** `814c6d4` had dropped auto-1×; pill and drain both stayed 6× (not label-only). Restored: 6× until ~10 minutes from the edge, 1× near the edge / while waiting. User pick stays in Redux; 6× returns when the buffer is ~20. **Field PASS** on `20260915-3` after `e9f2107`.
- **Short-buffer scrub stays in replay.** LIVE edge is the last recorded minute, not dawn. Scrub-back stays on that minute; Jump to Live is explicit. Idle catch-up can still jump to the tip. **Field PASS** on this tab (~50 min tape, scrub ~20% left LIVE, bar ~22%, `t=9`→`11` at 6× replay, no snap to tip). Dawn/`cushion=0` case is tests-only — too many minutes now to re-hit live.
- **T-W6 — `?double=` keeps following.** **Field:** pin held on Ivan through that scrub.

### Landing Page

Prod: `double-landing-page` `987cc8d` on `vercel` ([www.doubland.ai](https://www.doubland.ai)). Branch `ivan/saturday-portal-pain`. Hard-refresh. Do not Join Breakfasts.

- **T-P5 — Meet wait honesty.** Staged copy at 0s / 4s / 10s / 25s. Meet shows it while compile is in flight. Same honesty as chat Send, not skip-ahead.
- **T-P4 / T-P8 — review stays on Meet.** Temperament is retake-only. Life notes are direct edit. Photo/sprite update from Meet and account. Village-25 still leads. ISS PATCH / quiz history / innate regen still BE.
- **T-P6 leftover — Join stills + village map.** Existing village interiors on home/role buttons. Home step shows the map image. No GIS. No new LLM (**S2** postponed).
- **T-P9 — Watch leave warn + way back.** Confirm before Back to account. Account has **Back to sim**. www Watch chrome, not Join / **T-W11**.

### Score on `20260915-3` (completed 500 @ 14:15)

Watch: [double-front `/simulations/20260915-3`](https://double-front.vercel.app/simulations/20260915-3)

| Item | Result |
|---|---|
| **T-E6** | **PASS.** Clock `05:55:00`. Hidden `start_date` `2026-09-14`. |
| **Plant / boot** | **PASS.** `SPAWN KEEP PLANTED` Gosha `(76,23)` Katya `(74,23)`. Same tiles through the sit (steps 0–6). |
| **T-E1 both still on Hobbs @ ≥10** | **No.** Gosha stayed `(76,23)`. Katya left after the sit: step 7 `(77,26)` → step 10 `(90,31)` (off the Hobbs box). Not a boot snap-home. |
| **V2** (Gosha↔Katya) | **PASS.** Hello then **full sit** step **1** (`CM.START` `tier=full`, `talked`, leftover **honored**). Not hello-only. |
| **Through lunch sits** | **PASS.** Four `talked` encounters at Hobbs: Gosha↔Katya @1, Ivan↔Luba @270, Ivan↔Katya @273, Katya↔Luba @310. Hellos **6**. Two pairs stayed greeted-only (Gosha↔Luba, Gosha↔Ivan). |
| **After leftover ≠ `hard_stop:pressure`** | **PASS.** `hard_stop:pressure` **0**. Leftover `meet` minted step 2, honored step 3. Later mute on that pair is `encounter_quiet` / `hard_stop:prewindow` / `hard_stop:chatting`. |
| **Late cafe arrival ≠ `hard_stop:slice`** | **PASS.** `hard_stop:slice` **0**. |
| **Already in cafe: no seek `REDIRECT`** | **PASS.** Six `[SOCIAL_SEEK:REDIRECT]` — all from dorm rooms toward Gosha. **0** from `Hobbs Cafe`. |
| **Same-room `too_far`** | **PASS.** `too_far` **0** (was the V2/V3 lunch miss on `20260915-2`). |
| **11:00 occupancy** | **4/4** on Hobbs tiles (Gosha `(72,23)` Ivan `(78,21)` Katya `(81,25)` Luba `(80,21)`). |
| **T-W4 / cliff 1× / scrub / T-W6** | **Field PASS** after `e9f2107`. Dawn/`cushion=0` tests-only. |
| **V8** | **N/A this tape** (ran to `completed`, no stop/go). Leftover still `honored` in `pair_encounters`. Prior V8 pass on `20260915-2` stands. |
| **T-E5** | **Cannot score owner** on this roster. |
| **Gather prewindow** | Still mutes (`hard_stop:prewindow` **167** on `CM.SHOULD`). Then item, not this cut. |
| **T-E8** | Katya walk-off after the sit is caption-vs-body watch, not this unmute cut. |

`CM.SHOULD` (1028): `encounter_quiet` 784 · `hard_stop:prewindow` 167 · `hard_stop:next_act` 42 · `not_copresent` 10 · `warming` 8 · `encounter_hello` 6 · `encounter_continue` 5 · `encounter_hazard` 3 · `hard_stop:chatting` 2 · `encounter_intent` 1.

## Then — Next Pass

Leftover from [`20260915_TODOs.md`](20260915_TODOs.md). Not in the cut above. Do not raise talk-chance. Do not restore linger. Do not re-open C1.

### Failed / incomplete on `20260915-2` (not this engine cut)

- **T-E5** — prompt shipped; **cannot score owner** on this roster (Watch Chat was stranger). Needs www Login in the iframe + a Double on the sim.

### Engine (open)

- **Step 0 wall-clock** — verify next pass. `20260915-3` stayed on minute 0 for **16+ min** (engine still there at ~25). First daily plan ~75s, then three empty `daily_plan` calls ~8 min each + `planning_stall` pager, then ~70 hourly-schedule calls before the minute publishes. Watch stays empty the whole time. Measure and cut first-minute boot — not only the 90s miss log (**T-E6**). Do not restart this run to debug it.
- **T-E8** — caption vs body (locked A+B). Walking / `heading to <persona>` while in a closet or already behind the bar.
- **First-sight hello** — dorm pass-by (named rooms 3 tiles apart) still silent; no ticket until you say go.
- **T-E7** — sleep on the bed. Blocked; same family as Watch snaps (**T-W10**).
- **Gather prewindow** — 10:30–11:00 still `_hard_stop` (~6% this run). Not P1.
- **T7 SOT** — **stamped Current** 2026-09-16 (`sot_chats.md` §3c, `sot_survival.md` seek/prewindow). Temp lock; further talk design needs founder go.
- Thought `created_at` wall-clock vs sim-time — only if we ever score the 180-min mute **after a resume**.

### Watch / www

- **T-W11** — every sim `www.doubland.ai/{id}`; public also `/{SIM_NAME}`. Landing + FE + BE. Not Join.
- **T-W3** — Saturday email-names: manual rename, low urgency.
- **T-W8** — first-reply latency (copy shipped; engine later).

### Portal (www)

- **S3** — post-chat “update your Double” card. Not until B1 write-back rules.

### Hold

- Everyday walk-to-person — **PM-VIL-1** (empties Hobbs).
- **S2** — suggested homes/roles (no extra model hop).
- **B1 / B2** — owner vs stranger chat, secrets, control. Research before more prompt surgery beyond T-E5.
