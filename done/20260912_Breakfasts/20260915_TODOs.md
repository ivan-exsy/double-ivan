# 15 Sep — open board

Living list after Wave 2 score `20260915-1`. **Work here.** Do not add shipped tickets back.

| File | Job |
|---|---|
| This file | Open work + Wave 2 RCA + **pattern scratchpad** (for second-opinion review) |
| [`20260914_Post_breakfasts_todos.md`](20260914_Post_breakfasts_todos.md) | Archive: closed tickets, Saturday/Friday evidence, Wave 2 post-mortem (score table only) |
| [`20260913_E1E2_hello_design.md`](20260913_E1E2_hello_design.md) | Talk spec + score bars (T0–T7) |
| [`20260912_breakfasts_run.md`](20260912_breakfasts_run.md) | Breakfasts clicks / Start / allowlist |

**Do not resume `20260915-1` to finish talks.** Plant missed; 11:00 gather refuses new talks from 10:30.

Reviewers: start at **Wave 2 RCA**, then **Wave 2.1 RCA**, then **Pattern scratchpad**. Do not treat the post-mortem table as the diagnosis.

---

## External verifier assignment

**Job:** Confirm or kill the diagnosis in **Pattern scratchpad** (search that heading). This is a second-opinion review, not a build.

**Question:** People sit in the same cafe and only nod. We think we keep adding a memory or a veto for the last miss, and that same thing then blocks the next real sit. The recommended next cuts are **P1** (story thoughts must not forbid talk), **P2** (do not hunt someone already in the room), then **P3** (same-room distance veto) — not another talk-chance knob, not walking people to each other.

**Please return:** for checklist items 1–8 in the scratchpad: **confirm / reject / needs data**, one verbatim quote or log line each, then **keep P1→P2→P3 / reorder / different cut**. If you reject a claim, say which next cut dies with it.

**Out of scope:** do not write code; do not Start or Join Breakfasts; do not treat the scratchpad as live contract; ignore V8 stop/go and Watch/FE tickets unless they change the talk diagnosis.

**Read order (this file first):** Pattern scratchpad → Wave 2.1 RCA → Wave 2 RCA. Optional: `20260913_E1E2_hello_design.md` §2, `sot_chats.md` §3b, `double-ivan/done/20260901_chat_realism.md`.

---

## Next pass (Wave 2.1) — one Start, one score

Founder lock 15 Sep: this-minute “free?” only; do **not** sniff the next walk clip; do **not** mix caption rewrite into the engine cut. Remaining talk-score todos fold into **the same new 4-person run** (not into the C1 diff).

**Ship in code (then go):**

1. **T-E1 / C1** — “free?” reads this minute’s action. Day-plan row 0 (`sleeping`) must not veto a cafe sit. Real sleep stays `is_sleep_like_action` on the current act. Do not raise talk-chance. Unit: day plan starts with `sleeping`, both sitting Hobbs, dist 1 → after hello, `warming` / hazard, not `hard_stop`.
2. **Plant script** — require sim-code argv (no default `20260914-1`). After write, assert scratch + coords tiles are Hobbs `[76,23]` / `[74,23]` before Start. Engine must **keep** those tiles at boot (do not snap to dorm / living-area center; do not haul cafe tiles to home egress). Write step-0 coords for the other two as well so boot is a full set.

**Score on a new sim (do not resume `20260915-1`):**

| Folded in | How it rides this Start |
|---|---|
| **V2 / V3** (T-E1) | Plant Gosha+Katya **on those tiles**, affinity 0, both free in Hobbs from wake. Through **lunch**. Collect `CM.SHOULD` reason counts |
| **V8** | **Stop** ~300, resume, confirm the sit is remembered. Do not `completed` |
| **T-E6** | Clock **05:55** dawn; keep engine log for the 90s first planning miss |
| **T-W4** | **FAIL** 15 Sep on `20260915-2`: tab open at 0 minutes (LIVE 6×, empty map, no clock) stayed empty through engine ~33; **reload** then showed bodies at LIVE 6×. Same miss as 13 Sep Block 4 — do not treat reload as a pass |
| **T-W5 / T-W6** | T-W5 **mixed** (see Watch table). T-W6 still: `?double=` camera on this running Start after a reload |
| **T-E5** | One owner Watch Chat on that sim: no invented secret pair |
| **C2 watch** | If the planted pair (2 tiles) talks, V2 can pass without a too-far change. If only far cafe pairs stay quiet, C2 is the **following** cut |
| **T-E8 watch** | Note caption vs body on the same walk. **Do not patch** in this pass (options A+B still locked) |

**Out of this pass:** T-E8 code, C2 code, first-sight hello, T-E7 sleep-on-bed, “refuse a sit because the next clip is a walk,” portal T-P4/P5/P8. Everyday walk-to-person stays **PM-VIL-1**.

**Ops 15 Sep ~16:20 ET:** V8 **stop@376 + resume**. `ENCOUNTER RESTORED n=5`. Short sprint **20** still generating. Do **not** Join Breakfasts. Do **not** restart `api-gateway`.

**Where the leftover bugs live**

| Observation | Repo | Layer |
|---|---|---|
| Empty map until reload; HUD stuck at 06:46 while later minutes existed (`t=` ahead of clock); LIVE 6× + Play does nothing at the newest minute | `double-front` | **FE** (Watch) |
| Scrub-back then Play *does* tick **behind** the tip | `double-front` | **FE** — T-W5 only fails **at** the live tip |
| “Free to talk?” + cafe plant that survives boot | `generative_agents` | **BE** — already on the box; this run is the proof |
| Caption vs body, walk-to-name, too-far cafe seats | `generative_agents` | **BE** — watch-only this pass |
| www / portal chrome | `double-landing-page` | **Landing** — not these Watch misses |

This score URL is [double-front on Vercel](https://double-front.vercel.app/simulations/20260915-2), not the Breakfasts landing iframe.

**Public API hostname (Hetzner nginx):** `https://api.doubland.ai` (no `:8001`) is the locked public name. Flipping Vercel `API_GATEWAY_URL` / Watch API base does **not** require this sim to finish and does **not** restart the engine. Do **not** restart `api-gateway` to “pick it up.” Smoke `GET https://api.doubland.ai/health` first (healthy 15 Sep). Then landing + `double-front` env. Open Watch tabs need a refresh. If nginx were wrong, Watch would go dark — the score would still run.

**Lunch score 15 Sep ~12:30 / step ~360 of 400** (DB + SSH). Still generating. Through lunch **can be scored**. Earlier 08:10 snapshot superseded.

| Item | Lunch |
|---|---|
| **Plant / boot** | **Pass.** Step 0 Gosha `(76,23)` Katya `(74,23)`. |
| **C1 sleep veto** | **Pass (dawn).** Planted pair: **warming ×6**, not leftover-`sleeping`. Lunch is still `hard_stop` while they eat at Hobbs (a different busy-gate). |
| **V2** | **FAIL.** Hello step **1**, Katya left ~7. Second cafe hello **224**, no sit. Lunch: same cafe, **7 tiles** apart `(79,23)` vs `(72,23)`. |
| **V3** | **FAIL.** Hello step **13**, **199** shared minutes, parted **332**, no `talk_session`. |
| **Full talks** | **1** — Katya↔Luba step **11** only. Hellos **7**. |
| **CM.SHOULD** | `hard_stop=1010` `warming=6` `encounter_hello=7` `encounter_quiet=21` `not_copresent=21` `encounter_hazard=1`. `too_far` is a separate line (dist > 4). |
| **11:00 occupancy** | **4/4** on Hobbs (challenge). |
| **T-E6** | Dawn **06:30** not 05:55. First-plan miss already logged. |
| **T-E8** | **Seen.** Watch-only. |
| **C2 watch** | **Seen.** Lunch `too_far (dist=5 > threshold=4)`. Planted 2-tile pair never sat — C2 is the **following** cut for far cafe seats. |
| **T-E5** | **Cannot score owner** on this roster. One stranger Chat (Ivan Pistsov). |
| **T-W4 / T-W5** | Already scored (FAIL / mixed). |
| **T-W6** | `?double=` used; not a timed camera-follow. |
| **V8** | **Pass.** Stopped @376 (drain), resumed. Log `ENCOUNTER RESTORED n=5` (= open rows before stop). Katya↔Luba sit still `talked` + tea leftover. No new hello on that pair at boot. Short sprint 20 still generating. |
| **3rd pass URLs** | Not this run. |

---

## Wave 2.1 RCA — why cafe nods still did not become sits (`20260915-2`)

**V8 ignored** (founder stop/go). Logs + encounter table + scratch. No code in this pass.

**One-line:** C1 did its job (dawn **warming**, not leftover-`sleeping`). The planted pair was then **walked off the table** by Survival seek. The one real sit (Katya↔Luba tea) wrote **poignancy-8 leftover thoughts**, which the “too stressed to talk” gate treats as a **3-hour mute**. Challenge overlay thoughts at 11:00 mute **everyone** through lunch. Far cafe seats (`too_far`, need ≤4 tiles) are a second, independent miss.

### V2 — Gosha↔Katya (FAIL)

C1 **Pass:** after the 06:31 nod, six minutes are `warming` at p≈0.08 (dice never hit). Katya’s plan dest was already `<persona>Gosha Pistsov`.

Then seek fired while she was **already in the cafe**:

- `` `[SOCIAL_SEEK:REDIRECT] Katya Pistsova act_address=<persona>Gosha Pistsov (was 'the Ville:Hobbs Cafe:cafe:cafe customer seating')` ``
- Armed `until_step=30`

She left the planted tiles (~step 7). Encounter parted step 9. That is walk-to-person (T-E8 / **PM-VIL-1**), not “still busy because sleeping.” Second cafe nod ~10:14, still no sit.

### V3 — Gosha↔Luba (FAIL)

Nod step 13 at Hobbs. **199** later shared minutes, no sit. Two gates:

1. **Mute after the tea sit.** Leftover thoughts `` `Leftover meet with Katya Pistsova: See you, Katya. I'll have that tea ready in a jiffy.` `` **poignancy 8**. Pressure ≥0.5 is a hard stop for **180 sim minutes**. Luba (and Katya) are muted ~06:41–09:41 from that one sit.
2. **Too far in the same room.** `too_far (dist=5 > threshold=4)` **212** times for this pair. Same cafe, ~5 tiles, never reaches the dice.

Luba also got the same seek rewrite off cafe seating toward Gosha (then cleared when a hello sit opened).

### Lunch (all four at Hobbs, still `hard_stop`)

Challenge overlay, all four, **poignancy 8**: `` `I was at Hobbs Cafe for the Hold for the Shield challenge.` `` (~11:00). Same 180-minute mute → through **14:00**. Lunch 12:30 is inside that window. Current actions are eat/sip/notes — **not** `walking to` / `sleeping`. C1 is not this.

C2 still true on top: Gosha `(79,23)` vs Katya `(72,23)` = **7** tiles.

### What this is not

- Not a failed C1 (dawn warming is the proof).
- Not “they never met” (7 hellos; 11:00 occupancy **4/4**).
- Not V8.

### Falsification (cheap, before another village)

1. After a leftover thought with poignancy 8, `should_converse` with a free cafe partner at dist 1 → today `hard_stop` for 180 minutes. If it is `warming`/`hazard`, the mute diagnosis is wrong.
2. Seek must not rewrite dest when both bodies are already in the same cafe 3-part. If a unit with both on Hobbs seating still REDIRECT to `<persona>…`, V2’s leave is explained.

**Do not patch in this score.** Seek/T-E8 still watch-only. Pressure-from-leftover and overlay mute are **new** Desired cuts (not C1).

---

## Pattern scratchpad — talk realism keeps losing the same way

**Audience:** external verifier. Confirm or falsify; do not treat as shipped SOT.  
**Author:** engineering pass 15 Sep 2026 after Wave 2.1 logs on `20260915-2`.  
**V8:** out of scope (founder stop/go).  
**Ask:** is there a repeating class of bug, and does that class tell us what to cut *now*?

### Sources (read for this section)

| Source | Role |
|---|---|
| [`20260913_E1E2_hello_design.md`](20260913_E1E2_hello_design.md) §2–§3.9 | Named the band-aid history and the intended “few hard stops” model |
| [`20260914_Post_breakfasts_todos.md`](20260914_Post_breakfasts_todos.md) | Wave 2 score table; T-E1 / T-E8 evidence |
| This file, Wave 2 RCA + Wave 2.1 RCA | C1 sleep tautology; plant miss; then seek + pressure mute + `too_far` |
| `double-ivan/done/20260901_chat_realism.md` | Wanted loop: talk → kept thought → follow-up sit. Seek **off** that tip because walking to a person empties Hobbs |
| `double-docs/sot/sot_chats.md` §3b | Leftover KEEP / stance Current; **Seek-to-person not Current** |
| `double-docs/sot/sot_survival.md` Conversation Priors | Survival seek is movement-only; paused in gather prewindow; does **not** force a chat |
| `double-docs/sot/sot_action-location.md` | Same category error on gather: a **preference veto relocated a correct body** |
| Hello design score cards trail | `20260831-1` mill; `20260831-2` quiet; linger 0 on `20260902-1`; KEEP/stance Current on 4-person |

### 0. What we have been trying to buy

Not “more lines.” The Sep 1 packet is still the product:

> a real conversation → a thought they keep → a follow-up → a deeper conversation → an action you can see

Wave 2 restated it as: same room + free → hello once → often a real sit. **No new walking.** Chance is allowed. A leftover / Survival reason may speak first, but must not drag the body.

Occupancy at 11:00 can pass while that loop is dead. That is not a contradiction. It is the bug class.

### 1. The repeating pattern (one sentence)

**Every talk wave adds a memory or a veto so the last miss cannot recur. The next miss is that same veto (or that memory) firing on a pair who are already together and free.**

Hello design §2 already named it (13 Sep), and gather RCA named the twin:

- **Plan / label mistaken for body** — quiet keyed on `act_address`; overlap = remaining *slice*, not minutes sharing a room; C1 read day-plan row 0 `sleeping`; T-E8 caption `walking to Gosha` while the body is in a closet or already behind the bar; `too_far` after both bodies are in `Hobbs Cafe:cafe`.
- **Process / side-effect mistaken for relationship** — cooldown and “first daily” in RAM; then leftover/stance/challenge thoughts written at **poignancy 8** so retrieve can find them, which `get_internal_pressure_score` reads as “too stressed to talk” for **180 sim minutes**.
- **A preference relocates a resolved body** — gather whitelist veto (deleted after 1-B). Survival seek still writes `act_address = <persona>Name` even when both people are already on cafe seating (`20260915-2` Katya/Luba `[SOCIAL_SEEK:REDIRECT] … was 'the Ville:Hobbs Cafe:cafe:cafe customer seating'`). SOT: seek-to-person empties Hobbs; stay off village MVP. Survival kept a once-a-day walk; it is now hunting *inside* the room.

The hello-design punchline was: *the gather fix was to delete the relocating veto, not to add another.* Talk waves did the opposite: they kept `_hard_stop` and kept writing the memories that trip it.

### 2. Band-aid timeline (same loop, new knob)

From hello design §2 + scores, compressed:

| When | What we added | Symptom it killed | What it then killed |
|---|---|---|---|
| Jan | Hardcoded hello (cost) | LLM on every nod | Hello is not a conversation (root of T-E1) |
| Feb–Apr | First-daily, overlap matrix, cooldowns | Silent mornings / spam / cost | Slice remaining ≠ sitting together; RAM forgets at restart |
| Aug | Will gate (affinity ≥ 0.2) | Too many deep sits | Strangers can only nod (`*_no_will`). Chicken-and-egg |
| Aug 31 | Quiet until leave | Hello mill | Quiet keyed on **plan** address → Friday cafe re-greets |
| Sep 1 | KEEP leftover + stance thoughts | Talks vanished after desync | Thoughts are the drama object — and (now) the mute |
| Sep 2 | Linger +10 | Sitters leave after a talk | **0 fires.** Founder skipped. Occupancy did not need it |
| Wave 2 | Encounter table + hazard | Mill (V1 PASS) | `schedule[0] sleeping` → all-day `hard_stop` (C1). Plant caption ≠ tiles |
| Wave 2.1 | C1 = this-minute act only + plant sticks | C1 **Pass** (`warming` ×6) | Seek walks them off the table. Leftover/overlay poignancy 8 mutes the room. `too_far` at 5 tiles |

We keep winning the *previous* ticket and losing “two people at a cafe table start a sit.”

### 3. Closed loop on `20260915-2` (this run)

C1 is **not** the lunch miss. Three remaining writers:

1. **Seek in the same room (V2).** Katya already at Hobbs seating → dest rewritten to Gosha → she leaves the planted tiles during `warming`. Design Rule 2 / SOT §3b said encounter code must not walk anyone; Survival seek was the exception for *cross-town, once a day*. It fired as an in-room hunt. Same family as “seek empties Hobbs.”
2. **Story thoughts mute talk (V3 + lunch).** Leftover mint and stance write poignancy **8** (`leftover.py`). Challenge overlay writes poignancy **8** (`survival/controller.py` `_write_lived_thought`). `ACCIDENTAL_DEEP_CHAT_MIN_POIGNANCY = 8` and window **180** minutes (`persona.py`). `_hard_stop` if pressure ≥ 0.5. So: tea leftover at ~06:41 mutes Luba/Katya until ~09:41; 11:00 “I was at Hobbs Cafe for the Hold for the Shield challenge” mutes **all four** through lunch (~14:00).  
   **Name inversion:** “accidental deep chat” was built to *promote* a brief encounter to a full sit (`sot_chats.md` §3). The same score is now a **forbid**.  
   **Product inversion:** Sep 1 wanted talk → thought → *next* talk. Shipped: thought → no next talk.
3. **Same room, then too far (C2).** After copresent, Manhattan > ~4 still returns `too_far` and does not `touch`. Gosha↔Luba **212** such lines at dist 5. Lunch Gosha `(79,23)` vs Katya `(72,23)` = 7. Design gate 1 was already “same 3-part body arena.” The second ruler is a leftover proximity veto.

Hellos still work because they return **before** `_hard_stop` / `too_far` on the first open. That is why V1 stays green while V2/V3 stay dead — same shape as “the only unguarded path is the template hello” (hello design §2).

### 4. What would *not* help (we have already paid for these)

- Raise `H_BASE` / talk chance (Wave 2 lock; dice never ran under `hard_stop`).
- Everyday walk-to-person / leftover-driven dest (PM-VIL-1; empties Hobbs).
- Restore linger.
- Sniff the next walk clip as “busy.”
- Another “quiet / cooldown / first-daily” dict.
- Treating T-E8 caption as a hunt (it is emit, not seek proof — except when the log also has `[SOCIAL_SEEK:REDIRECT]`).
- Occupancy pins / gather lock (deleted; 11:00 **4/4** already).

### 5. What would help *now* (ordered, one cut each)

Do **not** bundle. Each has its own early log line. Aligns with “delete the relocating/forbidding job; do not add a gate.”

| # | Cut | Why this is the pattern | Cheap falsification | Early log |
|---|---|---|---|---|
| **P1** | **Unplug pressure-as-forbid.** Story thoughts (leftover, stance, challenge overlay) must not set pressure ≥ 0.5. Keep poignancy 8 for *retrieve* if needed. Accidental-deep-chat may promote a sit; it must not `_hard_stop`. | Self-poison: the KEEP object we shipped for drama is the 3-hour mute | Unit: leftover thought poignancy 8, dist 1, both free, cafe → `warming`/`hazard`, **not** `hard_stop` | After a tea leftover, Gosha↔Luba `reason=` is not `hard_stop` |
| **P2** | **Seek no-op if already same 3-part body arena** (or CLEAR). Survival may still walk *across town* once a day. | In-room hunt is the Hobbs-emptying bug in miniature | Unit: both on Hobbs seating, seek armed → **no** `act_address=<persona>` rewrite | `[SOCIAL_SEEK:REDIRECT]` count 0 while both `arena_body` already cafe |
| **P3** | **Same-room skips Manhattan `too_far`.** Street still uses ≤3 (or current street rule). | Second distance veto after body copresent; cafe is wider than 4 | Unit: same `Hobbs Cafe:cafe`, dist 8, after hello → hazard path, `touch` increments | Gosha↔Katya lunch `copresent_steps` grows; `too_far` 0 for same 3-part |

**Recommended order (superseded — see After Fable):** was P1 then P2, P3 later. **Now:** plant owns schedule → P1a+P1b+log → P3; P2 is hygiene. Do not raise `H_BASE`.

**Out of this cut:** T-E8 caption rewrite (emit vs scratch — separate writer). First-sight hello across named dorm rooms. Owner Watch Chat. www pretty URLs.

### 6. Verifier checklist

Please mark each claim **confirm / reject / needs data**, with a verbatim quote or a unit outcome:

1. Hello design §2 already called the class “veto bolted on the output side” + plan-for-body + RAM-for-relationship.
2. Gather fix that stuck was **delete relocating veto**, not a new pin (`sot_action-location.md` / 1-B).
3. Sep 1 packet wanted thought → follow-up sit; leftover/overlay thoughts are hardcoded poignancy **8**.
4. `get_internal_pressure_score` + `_hard_stop` (≥ 0.5) + 180 min window turns those thoughts into a mute. `20260915-2`: leftover ~06:41; overlay ~11:00; lunch still `hard_stop` while eating.
5. `[SOCIAL_SEEK:REDIRECT]` on `20260915-2` rewrote cafe seating to `<persona>Gosha` (in-room hunt).
6. `too_far` is a separate line after copresent; threshold 4 on this maze; planted pair’s dawn fail was seek-leave, not 7-tile lunch.
7. C1 sleep tautology is **fixed** on this run (`warming` ×6). A “fix C1 again” pass is the wrong next cut.
8. P1/P2/P3 are deletions/narrowing of existing jobs, not a new chance knob.

If (4) fails (pressure not actually in the `hard_stop` path at lunch), stop — re-read `_hard_stop` / overlap remaining-slice before P1. If (5) is only caption with no REDIRECT, P2 is the wrong V2 cut.

---

## 3rd pass — www Watch URLs (after this score)

**Not this run.** Do not stop `20260915-2`. Do not Join it. Address bar stays on **www.doubland.ai**. Canvas stays an iframe of [double-front `/simulations/SIM_NAME`](https://double-front.vercel.app/simulations/SIM_NAME). Users do **not** log in on Vercel.

**Product**

| URL | Who gets it | Opens |
|---|---|---|
| `https://www.doubland.ai/{id}` | **Every** sim (`double.simulations.id`) | Watch that world |
| `https://www.doubland.ai/{SIM_NAME}` | **Public** sims only | Same Watch; `SIM_NAME` = canvas name (`simulations.name`, e.g. `20260915-2`) |

Query string already used on canvas (`t`, `zoom`, `focus`, `double`) must survive on both www URLs. Breakfasts named URL stays [`/pittsburgh-business-breakfasts`](https://www.doubland.ai/pittsburgh-business-breakfasts). UUID Watch is **watch**, not Join — allowlist / `self_serve_access` still gate who can Join.

**Split**

| Team | Repo | Do |
|---|---|---|
| **Landing** | `double-landing-page` | Catch-all Watch page: `{id}` always; `{SIM_NAME}` only if public. Iframe `embed=1` → `/simulations/{SIM_NAME}`. Do not steal `/account`, `/onboard`, `/auth`, Login. Copy/share from www, not `*.vercel.app`. |
| **FE** | `double-front` | Keep `/simulations/{SIM_NAME}`. When embedded, share/copy the **www** URL (`/{id}` default, `/{SIM_NAME}` if public). Headless / Vercel path unchanged. |
| **BE** | `generative_agents` | Resolve `{id}` → name for the iframe. Mark which sims may use the named URL (public Watch). 404 if the row is missing. Do not treat UUID Watch as Join. |

**Out of this pass:** passing www Login into Watch Chat (owner vs stranger / **T-E5**) — needs the session in the iframe; ship after these URLs, do not mix into the talk score. **T-W4 / T-W5** stay FE playback bugs.

**Done when:** `www.doubland.ai/{id}` plays any existing sim; a public sim also plays at `www.doubland.ai/{SIM_NAME}` with the same clock/camera query; Login still only on www.

---

## Engine

### T-E1. Nod → conversation (open — FAIL 15 Sep)

**Product:** two people who share a room and have time should often start a real sit (≥3 exchanges), not only `Good morning`.

**Evidence:** Wave 2 post-mortem in the 14 Sep archive. Six one-hello sits, **0** full talks. Planted Gosha+Katya never sat at Hobbs until 10:14. Gosha↔Luba were close ~70 min after 07:09 and still only the nod.

**Use from prior work:** Wave 2 RCA in this file (C1 hard-stop tautology, C2 same-room then too-far). Spec §3–§5 in the hello design. V1 already passed — keep that. Do not add walking-to-person (PM-VIL-1). Do not tune `H_BASE` to hide C1.

**Next score:** Wave 2.1 pass above — C1 + plant-assert, then the folded score/Watch checks on one new Start.

### First-sight hello (open — no ticket yet)

**Product:** first pass in the dorm this morning should at least nod.

**Evidence:** 06:30–07:00 only Katya↔Luba (06:57). Gosha↔Ivan 4 tiles at 06:30; Ivan↔Katya 3 tiles at 06:32; no hello. Ivan asleep from 06:40 (later silence is fair).

**Do not** fold into T0–T7 until you say so. Current rule is same room (or 3 tiles on a street with no room). Passing between named dorm rooms can look like a meet and still not count.

### T-E8. Caption vs body (open — confirmed 15 Sep)

**Product:** the line under the name is what they are doing **now**. Walking only while they walk. A person name only if that person is this step’s dest.

**Evidence:** 06:40 Luba closet + “walking to Gosha”; 06:45 Gosha on the street + “finishing breakfast at Hobbs”; 07:18 Luba behind the bar, status still walking to Gosha. Locked in the archive: options A+B, not C/D. Wave 2.1: **watch only** — do not patch in the C1 cut.

**T-E7** (sleep on the bed) stays blocked until you say go. Same family as Watch snaps (**T-W10**).

### T-E6. 90s first planning try (shipped, not scored)

Folded into Wave 2.1: 05:55 dawn + engine log on the next talk Start. `20260915-1` started 06:30.

### T-E5. Watch chat “my double” lore (prompt shipped, not scored)

Folded into Wave 2.1: one owner Chat on the next running Start. `20260915-1` had none.

### T7 SOT

Wait for a green 4-person talk score.

---

## Watch

| ID | Status | Next check |
|---|---|---|
| **T-W7** | Done 15 Sep | Hellos that exist show on the person card |
| **T-W9** | Done 15 Sep | HUD **15 Sep** on `20260915-1` |
| **T-W4** | **FAIL** 15 Sep `20260915-2` | Open tab at 0 minutes stayed empty through ~33; reload then LIVE 6× with bodies. Later: HUD **06:46** (step 16, Katya on the path) while URL `t=36` and engine already had later minutes. Next: idle/LIVE-at-0 must seek when `total_steps` grows (no reload). **FE** `double-front` |
| **T-W5** | **Mixed** 15 Sep `20260915-2` | Behind the tip: scrub to ~06:45 + Play → clock at 6× (1 min / 10s). At the tip (**07:13** / step 43): LIVE 6× still on, Play looks unpressed, clicks do nothing — waiting on the next generated minute (engine paused ~6 wall min 43→44). Next: Play at live tip must either wait visibly or resume when the next minute lands. **FE** `double-front` |
| **T-W6** | Code in; field on Wave 2.1 Start | `?double=` camera stays on that person while scrubbing |
| **T-W8** | UX wait-copy shipped | Engine latency still later |
| **T-W3** | New Joins OK | Saturday’s 3 email-names: manual rename only, low urgency |
| **T-P9** | Open | Accidental leave from the sim page — warn / always a way back |
| **T-W11** | **3rd pass** (not this run) | www Watch: every sim `/{id}`; public sims also `/{SIM_NAME}`. **Landing** + **FE** + **BE** — see 3rd pass above |

---

## Portal (www)

Shipped 14 Sep: T-P1 knobs + honesty copy, T-P2 photo glow, T-P3 Look freeze, T-P6 thin (3 open homes, hide taken roles), T-P7 disclaimer gone.

| ID | Open work | Lock |
|---|---|---|
| **T-P4 / T-P8** | Review / correct after done: quiz retake + life-chapter edit, no bounce to /account, photo/sprite update | **S1:** temperament = retake only; ISS text = direct edit; village-25 still wins |
| **T-P5** | Meet wait feels dead | Do not confuse with T-P3 |
| **T-P6 leftover** | Role pictures, map picker | No new LLM (**S2** postponed) |
| **S3** | Post-chat “update your Double” card | Not until B1 write-back rules |

---

## Hold

- **S2** suggested homes/roles — postpone, no extra model hop.
- **B1 / B2** owner vs stranger chat, secrets, control — research before more prompt surgery beyond T-E5.
- **T-W8** first-reply latency — not this talk pass.
- Everyday walk-to-person — **PM-VIL-1**, not T0–T7.

---

## Closed (do not re-open)

Waves **0 / 1 / 1.5**. Portal **D4**. Watch **T-W1 / T-W2 / T-W7**. Wave 2 **V1** (one hello per sit, encounter table). Cap 6 on `20260915-1`. 11:00 occupancy 4/4. Score table: 14 Sep archive. Why V2/V3 failed: RCA below.

---

# Wave 2 RCA — why the talk fix did not talk

**Audience:** third-party senior engineer (second opinion).  
**Author:** engineering pass 15 Sep 2026.  
**Status:** diagnosis from scored sim + current `railway` @ `896ab1eb` source. **No code change in this pass.** Engine stdout (`CM.SHOULD`) was not on disk for this slice (SSH to the box denied). Claims that need that log are marked **unconfirmed-in-log**.

**One-line verdict:** Wave 2 shipped the hello-once memory it designed (V1 **PASS**). It did not ship a path that can open a full talk on a live daily schedule. After the nod, `should_converse` always takes `hard_stop` because “are they free?” reads the **first row of the day plan**, which is still `sleeping`. A second, independent gate then treats two people in the same cafe as `too_far` unless they sit within ~3 tiles. The planted breakfast pair never sat on those tiles. Caption lies (T-E8) were **out of Wave 2 scope** — they are a different writer.

Do not resume `20260915-1`. Do not raise `H_BASE`. Do not treat “walking to Gosha” as a failed hunt.

---

## 0. What “attempt” means

Wave 2 **attempted** (BE `ivan/w2-talks`, live product):

| Attempt | Job | Score on `20260915-1` |
|---|---|---|
| Encounter table `double.pair_encounters` | One sit → one hello; no jsonb-on-sim | **Did its job** (V1) |
| Hello on first copresent; later minutes = per-side hazard / intent | Nod becomes a conversation | **Did not** (V2/V3: 0 full talks) |
| Keep Survival once-a-day seek as movement-only; arrival fires p=1 | Seek then talk without new walking | **Did not** (intent never consumed; `intent_kind` empty on all 6 rows) |
| Delete linger | Stop fake extra sit | N/A (no full sit) |
| Plant script Gosha+Katya at Hobbs as strangers | Fixture for V2 | **Did not land on tiles** |
| T-E6 90s first planning miss; T-E5 Watch-chat lore; T-W4/5/6 LIVE | Other Wave 2 tickets | **Not scored** (wrong clock / no owner Chat / sim `completed`) |

Wave 2 **did not attempt:** T-E8 caption vs body; first-sight hello across named dorm rooms; everyday walk-to-person (PM-VIL-1). Those still look broken on Watch. They are not evidence that the talk patch “partially worked.”

Spec: [`20260913_E1E2_hello_design.md`](20260913_E1E2_hello_design.md). Score table (what passed/failed, not why): [`20260914_Post_breakfasts_todos.md`](20260914_Post_breakfasts_todos.md) §Wave 2 post-mortem.

---

## 1. Score identity (frozen)

| Field | Value |
|---|---|
| Sim | `20260915-1` |
| Supabase | project `double-openrouter` (`kkjhsozszgoorwehhsdg`), `simulations.id` `a3f5a2e1-88af-406c-a1a4-8bf1f788e591` |
| Tip | `railway` @ `896ab1eb` |
| Roster | Gosha Pistsov, Ivan Pistsov, Katya Pistsova, Luba Pistsova |
| Maze | `the_ville` / Hobbs box x `72–83`, y `19–26` (stay harvest y to `30`) |
| Clock | HUD 15 Sep **06:30 → 11:29** (skip-premiere; spec asked 05:55) |
| Steps | `0–299` **completed** (spec asked stop+resume at ~300) |
| Caps | persist over-6 = **0**; 11:00 occupancy **4/4** Hold for the Shield |

`pair_encounters` (verbatim, all 6 rows, `phase=greeted`, empty `talk_session_id`, empty `initiator` / `intent_kind`, `parted_step` null):

| pair_key | opened_step (HUD) | copresent_steps | last_copresent_step |
|---|---|---|---|
| `Katya Pistsova\|Luba Pistsova` | 27 (06:57) | 150 | 297 |
| `Gosha Pistsov\|Luba Pistsova` | 39 (07:09) | **240** | 295 |
| `Gosha Pistsov\|Katya Pistsova` | 224 (10:14) | **1** | 224 |
| `Ivan Pistsov\|Luba Pistsova` | 225 (10:15) | 100 | 296 |
| `Gosha Pistsov\|Ivan Pistsov` | 226 (10:16) | 100 | 299 |
| `Ivan Pistsov\|Katya Pistsova` | 226 (10:16) | 10 | 249 |

Chat memory on this sim: six 2-line hellos only. No `user_chat_threads`.

---

## 2. Causal graph (what actually failed)

```
Wave 2 goal: same-room + free → hello once, then a real talk
        │
        ├─ Hello path: SKIP hard_stop, SKIP overlap. → 6 nods. V1 PASS.
        │
        └─ Talk path (every later minute of an open encounter):
              copresent (same 3-part body arena)
              → too_far if Manhattan > ~3          ← C2, independent
              → touch copresent_steps
              → hard = _hard_stop(A) or _hard_stop(B) or overlap < 2
              → if hard: reason=hard_stop, no dice, no intent   ← C1, load-bearing
              → else hazard / intent
```

**C1 explains V3** (Gosha↔Luba: 240 touches, including a 2-tile sit at step 74, still 0 talks).  
**C2 explains V2 after 10:14** (Gosha↔Katya: hello then `copresent_steps` stuck at 1; they share Hobbs but sit ~8 tiles apart).  
**C3 (plant)** explains why V2 never got the 2-tile fixture the score needed.  
**C1 + C3 together** are why “we built encounters and they still only nod” is not a dice miss.

P(warming never hits for 240 minutes at p≈0.075) ≈ 10⁻¹⁷. Chance-talk luck is **ruled out** for Gosha↔Luba if those 240 rows passed `too_far` (they did: `touch` is after that gate). The remaining production reason on the talk path is `hard_stop`.

---

## 3. C1 — load-bearing: “free to talk?” reads overnight sleep

### 3.1 What Wave 2 kept on purpose

The design correctly stopped using remaining slice as **copresence**. It **kept** remaining slice and `_hard_stop` as **availability** ([hello design](20260913_E1E2_hello_design.md) §3.3: “remaining slice < 2 min”; `_hard_stop` at `social_will.py`).

`should_converse` after hello:

```957:962:reverie/backend_server/persona/cognitive_modules/conversation_manager.py
        overlap_steps = self._compute_overlap(persona_a, persona_b)
        hard = (
            _hard_stop(persona_a, persona_b.name, next_act_must_allow_talk=True)
            or _hard_stop(persona_b, persona_a.name, next_act_must_allow_talk=True)
            or overlap_steps < 2
        )
```

Hello returns **before** this block. That is why nods exist and talks do not.

### 3.2 The tautology

`next_act_allows_talk` (`social_will.py`):

```78:97:reverie/backend_server/persona/cognitive_modules/social_will.py
def next_act_allows_talk(persona) -> bool:
    ...
    schedule = getattr(scratch, "f_daily_schedule", None) or []
    if schedule:
        row = schedule[0]
        ...
            nxt = str(row[0] or "")
    blob = f"{getattr(scratch, 'act_description', '') or ''} {nxt}".lower()
    return not any(tok in blob for tok in _NEXT_NO_TALK)
```

`_NEXT_NO_TALK` is a **substring** list: `"sleep"`, `"asleep"`, `"heading to"`, `"walking to"`, `"shift"`, …

`schedule[0]` is the **first row of the stored day plan**, not the current act.

End-of-run `persona_scratch.f_daily_schedule` on this sim (verbatim prefixes):

- Gosha: `[["sleeping", 300], ["waking up and starting his morning routine at home", 60], …]`
- Ivan: `[["sleeping", 540], …]`
- Katya: `[["sleeping", 360], …]`
- Luba: `[["sleeping", 360], ["waking up and starting morning routine at home", 60], ["Unlock and prep behind the cafe counter …", 10], …]`

So `nxt` is always `"sleeping"`. Token `"sleep"` is in `"sleeping"`. `next_act_allows_talk` is **false for every Double, every minute**, as long as the day plan is still on disk in this shape.

`_hard_stop(..., next_act_must_allow_talk=True)` is therefore **true every later minute**. Hazard never runs. Intent is collected, then discarded:

```991:1002:reverie/backend_server/persona/cognitive_modules/conversation_manager.py
        if hard:
            result = ChatParams(
                False,
                reason="hard_stop",
                ...
            )
```

Spec §3.9: hard stops **delay** intent, they do not cancel. A delay that is true all day **is** a cancel. All 6 encounter rows have `intent_kind=""`.

**Unconfirmed-in-log:** `CM.SHOULD … reason=hard_stop` histogram. The code + the stored schedules make any other reason for 240 silent touches implausible.

### 3.3 Why units were green

`tests/test_conversation_manager.py` `MockScratch` sets `f_daily_schedule = []`. Empty list → `nxt=""` → `"sleep"` does not match `"working"` / `"sitting"`. `test_second_step_same_encounter_is_warming` therefore sees `warming`, not `hard_stop`. Hazard/intent units use `overlap_min_*=30`.

There is **no** unit that feeds a real day plan starting with `sleeping`. That is how Wave 2 merged with green tests and a silent village.

### 3.4 Secondary hard-stop landmines (same function, not needed to explain this slice)

Even after C1 is fixed, these still sit in `_NEXT_NO_TALK` / `_hard_stop`:

| Token / gate | Production trigger |
|---|---|
| `"heading to"` / `"walking to"` | Planner text **or** leftover hourly. Watch captions on this sim are full of them (see C4). Emit does **not** write `scratch.act_description`; only the planner string counts here. |
| `"shift"` | Substring. Any “morning shift” hourly would mute the barista all day. Luba’s stored hourlies on this sim did **not** need it; C1 already muted her. |
| `overlap_steps < 2` | Remaining minutes of **current** `act_duration`. Luba’s opening tasks are 5–15 min slices (`Unlock and prep` 10, `Wipe down…` 10). Near a slice boundary the pair goes hard even if C1 is gone. Design kept this on purpose; cafe work will still look “too busy to talk” unless the bar is on **body still in the room**, not slice remainder. |
| Gather 10:30–11:00 | `_SURVIVAL_GATHER_LEAD_HOURS = 0.5` before 11:00. Explains silence after the 10:14–10:16 hellos. Does **not** explain 07:09–10:14 Gosha↔Luba. |

---

## 4. C2 — same cafe, then `too_far`

Design gate 1: same **body** 3-part arena (or street ≤ 3). Implementation: that, **then**:

```908:920:reverie/backend_server/persona/cognitive_modules/conversation_manager.py
        if not skip_distance and distance > effective_max_tiles:
            ...
            return ChatParams(False, reason="too_far", copresent=True, ...)
```

`effective_max_tiles` comes from `CONVERSATION_CONFIG["threshold"]["base_tiles"]` **3** plus maze `proximity_mod` (default **0** if meta missing). Cafe width is ~12 tiles. Two people can be copresent in `the Ville:Hobbs Cafe:cafe` and still be 8 tiles apart.

`too_far` returns **before** `touch`. That is why Gosha↔Katya is `copresent_steps=1` after the 10:14 hello: one open, then every later scan is `too_far`, counter frozen.

Step 270 (11:00, all four on Hobbs tiles): Gosha `[72,23]`, Ivan `[74,23]`, Luba `[76,23]`, Katya `[78,21]`. Gosha↔Katya Manhattan **8** > 3. Occupancy 4/4 can still be a talk miss.

Plant target tiles `[76,23]` and `[74,23]` are Manhattan **2** — the fixture was specifically to beat C2. It never sat there (C3).

Proximity **pre-filter** is a third copy of the plan-vs-body error the design named:

```9435:9440:reverie/backend_server/reverie.py
                      _prox_addr_a = getattr(self.personas[name_a].scratch, 'act_address', None)
                      _prox_addr_b = getattr(self.personas[name_b].scratch, 'act_address', None)
                      if not _same_arena(_prox_addr_a, _prox_addr_b) and session is None:
                          ...
                          continue
```

`_same_arena` on `act_address`. Survival seek writes `act_address = "<persona>Gosha Pistsov"` (`plan.py` `_maybe_apply_social_seek`). `parse_address` of that string is a 1-part tuple. It does not match `the Ville:Hobbs Cafe:cafe`. **New** talks are skipped at the scan even when both bodies are in Hobbs.

Hello at 07:09 still happened (likely `skip_distance=True` on `movement_report_interrupt`, `reverie.py` ~10573, or a scan while both plans were still cafe). 240 later touches for Gosha↔Luba show some path kept calling `should_converse` after C2 passed — C1 still dropped the talk.

---

## 5. C3 — plant did not land on tiles

Script `scripts/plant_r1_stranger_pair.py`:

- Default sim **`20260914-1`**, not `20260915-1`.
- Writes scratch `curr_tile` `[76,23]` / `[74,23]`, `act_address` `the Ville:Hobbs Cafe:cafe:cafe customer seating`, `act_description` `sitting at a cafe table`, duration 45.
- Updates **latest** `personas_coords` **x,y only** (not `description`).

What step 0 on `20260915-1` actually stored:

| name | tile | `description` (verbatim) |
|---|---|---|
| Gosha Pistsov | `[129,45]` | `sitting at a cafe table (heading to <persona>Ivan Pistsov) @ the Ville:Dorm for Oak Hill College:Dorm Room 1` |
| Katya Pistsova | `[125,50]` | `sitting at a cafe table (heading to <persona>Gosha Pistsov) @ the Ville:Dorm for Oak Hill College` |
| Luba Pistsova | `[123,46]` | `waking up and starting morning routine at home (heading to <persona>Gosha Pistsov) @ the Ville:Dorm for Oak Hill College:common room` |
| Ivan Pistsov | `[130,48]` | `walking to Dorm for Oak Hill College @ the Ville:Dorm for Oak Hill College` |

Those tiles are **dorm spawn**, not Hobbs. Gosha reaches Hobbs by step 20 `[76,22]` **by walking**. Katya is not on Hobbs until the 10:14 hello (`opened_step=224`).

The cafe **caption** at dawn is not proof of plant. It is leftover in-place breakfast copy plus Survival seek (`heading to <persona>…`). Plant’s clean `sitting at a cafe table` with no persona dest is not what Watch showed.

Operator error (script default / Start after a no-op plant) and engine overwrite are not separable without the plant command log. **Observable fact:** V2’s fixture was not on tiles at step 0. Scoring V2 on this slice was invalid **and** C1 would still have silenced a correct plant.

---

## 6. C4 — T-E8 is a different writer (not a failed Wave 2 talk patch)

Emit does **not** overwrite `scratch.act_description`. Comment in `reverie.py` ~5678: scratch keeps the planner directive; FE gets `build_presence_intent_description`.

`_infer_travelling` is true when presence 3-part ≠ planned 3-part. Planned `<persona>Gosha Pistsov` never equals a dorm or Hobbs 3-part, so Watch rewrites to `walking to` / `heading to` that dest even when the body is in a closet or already behind the bar.

Verbatim, this sim:

- Step 10 Luba `[114,59]` closet: `waking up and starting morning routine at home (heading to <persona>Gosha Pistsov) @ the Ville:Dorm for Oak Hill College:Dorm Room 3:closet`
- Step 10 Gosha `[112,46]` street: `finishing breakfast at Hobbs Cafe @ the Ville`
- Step 48 Luba `[79,18]` already `behind the cafe counter`, caption still `(heading to <persona>Gosha Pistsov)`

`_PREMATURE_INPLACE_RE` does not include “finishing breakfast” / “sitting at a cafe table”, so those strings survive on the sidewalk.

`_collect_intent` only treats `chat with` / `talk to` / `talk with` as plan intent — **not** `heading to` / `walking to`. So the Watch line is not a talk reason. Hello with Gosha **clears** Survival seek (`already_chatting_with_target` in `_maybe_apply_social_seek`). Wave 2’s “arrival fires p=1” never got a free minute after that (C1).

Founder lock: options A+B on T-E8; do not invent walk-to-person. Do not fold C4 into T0–T7.

---

## 7. What was not a failed attempt

| Observation | Classification |
|---|---|
| Dorm pass-bys silent except Katya↔Luba 06:57 | Current rule: same **named room** (3-part) or street ≤3. `Dorm Room 1` vs `Dorm Room 4` is not copresent. New product bar if you want first-sight. |
| Ivan quiet after 06:40 | Asleep. Fair. |
| 07:08 “Luba greeted Ivan” | **Gosha** at 07:09. Ivan `[106,61]` bed. |
| T-E6 / V8 / T-E5 / T-W4–6 | Shipped or specified; this slice could not score them. |
| 11:00 4/4 + cap 6 | **PASS**. Gather occupancy is not the talk miss. |

---

## 8. Why the *process* also failed

1. **Category error the design already named, then re-implemented.** “Plan mistaken for body” is in the hello design as the Friday/Saturday root pattern. Wave 2 still: proximity scan on `act_address`; `too_far` after same-room; emit dest from `<persona>` plans.
2. **Availability helper never pointed at the current act.** `schedule[0]` + substring `"sleep"` is a one-line defect. No reviewer test used a real `f_daily_schedule`.
3. **Green units ≠ production trigger path.** Empty schedule, 30-minute overlap, distance 1, no `<persona>` dest, no proximity skip.
4. **Score protocol not followed.** Spec: 05:55, plant at 06:00 on tiles, ≥900 through lunch, **stop** at ~300 then resume (V8). Actual: 06:30, dorm spawn, **completed** at 300.
5. **Plant script defaulted to yesterday’s sim code.** Fixture bugs are how V2 gets a dirty fail (same class as caption-only plant).
6. **No `CM.SHOULD` reason rollup on the score.** Without that histogram, a second opinion cannot see `hard_stop` vs `warming` vs `too_far` counts. This RCA infers C1 from code + stored schedules + 240 touches / 0 talks.

---

## 9. Falsification (for the second-opinion reviewer)

Cheap, before another 300-step village:

1. **Unit (C1):** `MockScratch.f_daily_schedule = [["sleeping", 360], ["opening Hobbs Cafe", 60]]`, both sitting cafe, dist=1, overlap 30. After hello, `should_converse` today → `hard_stop`. After a correct fix → `warming` or `encounter_hazard`, **not** `hard_stop` from `"sleeping"` on row 0.
2. **Unit (C2):** same body arena `the Ville:Hobbs Cafe:cafe`, dist=8, after hello → today `too_far` and **no** `touch`. Spec same-room → should still be on the hazard path.
3. **SQL already true:** `copresent_steps=240` with `talk_session_id=""` and `intent_kind=""` on Gosha\|Luba. If a reviewer finds a full-talk chat node on this sim, this RCA is wrong.
4. **Log (unconfirmed here):** on a new 20-step cafe sit, count `CM.SHOULD` reasons. C1 predicts `encounter_hello` once then `hard_stop` until C1 is gone.

If (1) is already `warming` on current `main`/`railway`, C1 is stale — stop and re-read. At `896ab1eb` it is not stale.

---

## 10. Next pass (locked 15 Sep — implement when founder says go)

**Code in this cut (only):** C1 this-minute “free?” + plant script argv/assert. Do not raise `H_BASE`. Do not sniff the next walk clip. Do not patch T-E8 or C2 here.

**Fold onto the same new Start (not into the C1 diff):** V2/V3 plant-on-tiles through lunch; V8 stop+resume; T-E6 05:55 log; T-W4/5/6 on that running tab; T-E5 one Watch Chat; watch C2 and T-E8 without fixing them.

C2 code only if the planted 2-tile pair talks and far-cafe pairs still never do. T-E8 code stays a later ticket (A+B).

Arithmetic: fixing only C2 / only plant cannot clear V3. Gosha↔Luba already sat at dist 2 (step 74) and still did not talk. **C1 is the V3 bug.** C2+C3 are why V2 was a dirty fail on top.


# Fable: Wave 2 RCA Analysis

## Verdict in one paragraph

The diagnosis is mostly right, but two things in it are wrong in ways that change the cut order. **(a)** The V2 dawn leave was **not** Survival seek. Seek did fire on Katya from cafe seating at step 0, but it was cleared at step 1 by the hello and her body never moved; she left at step 7 because the engine force-replanned a "stale" schedule and her day plan said *breakfast at home*. **(b)** `hard_stop` has a second writer the scratchpad underweights: the "remaining slice < 2 min" gate, which fires on **expired-but-held actions** (someone who just walked in and whose walk act is past its planned duration). It accounts for ~29% of all `hard_stop` minutes and **100% of the planted pair's 10:14–10:22 miss**. Pressure-as-mute is real and is the biggest single writer (~60%), so **P1 stays first**, but it needs a sibling (slice gate) and a log-line change or its own falsification test will misread. P2 drops to hygiene. P3 will come sooner than the doc expects.

Evidence source: the run's engine log on the box (`20260915-2.log`, 213,740 lines, read-only), the on-disk memory nodes, and current source. Nothing was started, joined, or restarted.

## Checklist 1–8

**1. Hello design §2 named the class — CONFIRM.**
> "Every fix is a **veto bolted onto the output side**, keyed on whichever field was handy (planner address, remaining slice, a process dict)… Two recurring category errors underneath: **plan mistaken for body** (fixes 3, 9) and **process memory mistaken for relationship memory** (fixes 2, 5, 6, 9, 10)." — `20260913_E1E2_hello_design.md` §2

Caveat worth saying out loud: §3.1 principle 3 then **kept** exactly the two gates now muting the room: "a short list of hard stops that already exist (`_hard_stop`: sleep, gather pre-window, chatting with someone else, **pressure ≥ 0.5**, leaving next)" plus "remaining slice ≥ 2 min". The design named the disease and kept the two carriers.

**2. Gather fix that stuck was a deletion — CONFIRM (with a footnote).**
> "**Current / `railway` `2dea146e`:** the whitelist branch is removed from `_validate_address_post_resolution`… do not re-add a relocating whitelist gate" — `sot_action-location.md` line 73–74

Footnote: the gather family also shipped a stay-keep (`GATHER STAY keep`) that is Current. The load-bearing fix was the deletion (1-B PASS after it), but "no new pin at all" overstates it.

**3. Sep 1 packet wanted thought → follow-up; thoughts are hardcoded poignancy 8 — CONFIRM.**
> "a real conversation → a thought they keep (debt, suspicion, appointment, hurt, plan) → a follow-up … → a deeper conversation … → an action you can see" — `20260901_chat_realism.md` lines 20–24; and "Seek | **Off** this tip. Walking to a named person moves occupancy" (line 43).

Code: `leftover.py` `_write_thought` and `_write_stance_thought` both pass the literal `8` in the poignancy slot of `add_thought`; `survival/controller.py` `_write_lived_thought` passes `8` too. Three writers, one constant, no knob.

**4. Pressure + `_hard_stop` ≥ 0.5 + 180 min = a mute; leftover ~06:41, overlay ~11:00, lunch still hard_stop — CONFIRM, by elimination, with a correction.**

Code chain is exact: `ACCIDENTAL_DEEP_CHAT_MIN_POIGNANCY = 8.0`, `ACCIDENTAL_DEEP_CHAT_WINDOW_MIN = 180` (`persona.py:46–47`); any of the last 5 thoughts with poignancy ≥ 8 inside 180 minutes → `score = 0.8`; `PRESSURE_HARD_STOP = 0.5` (`social_will.py:36`); `_hard_stop` returns True on it before the "next act" check.

Timing lines:
- `🔵 CM.START: Katya Pistsova ↔ Luba Pistsova … tier=full … step=11` and `🔴 CM.END … reason=beat_budget` at step 11 → leftover minted **06:41**. Thought text on disk: `Leftover meet with Katya Pistsova: See you, Katya. I'll have that tea ready in a jiffy.` poignancy 8 (Luba and Katya both).
- `I was at Hobbs Cafe for the Hold for the Shield challenge. I did not win.` poignancy 8 on all four at the 11:00 resolve (Luba's says `I won.`).

The correction: the `hard_stop` log line **does not say which sub-gate fired**. So I attributed every one of the 1,167 `hard_stop` minutes by checking the other gates from the `PLAN PRE` lines (sleep tokens, `chatting_with`, no-talk tokens, keywords, gather pre-window, remaining minutes of the current act):

| Writer | hard_stop minutes | share |
|---|---|---|
| Nothing else left → **pressure** | 695 | ~60% |
| **Remaining slice < 2** (act expired or nearly so) | 344 | ~29% |
| Gather pre-window 10:30–11:00 | 68 | ~6% |
| chatting_with someone else | 4 | — |
| no plan line that minute | 56 | — |

Specific confirmations for pressure: Gosha↔Luba at 08:10 (Gosha `rem=5` "ordering and waiting for the second coffee", Luba `rem=5` "Clearing breakfast dishes", `chat=None`) and 08:40 (`rem=10`/`rem=10`) are `hard_stop` with **no other gate available** — Luba's 06:41 tea leftover is the only thing left. Ivan↔Katya at 12:30 lunch (Ivan `rem=5` "eating lunch at the table", Katya `rem=20` "eating a sandwich and salad") — same, only the 11:00 overlay thought remains. So "lunch still hard_stop while eating" holds. Also the planned mute end is consistent: Katya↔Luba shows `encounter_quiet` (= not hard-stopped) at 10:02–10:08, right after Katya's 06:41+180 window closed at 09:41.

**5. Seek REDIRECT rewrote cafe seating to `<persona>Gosha` — CONFIRM the line, REJECT the causation.**
> `[SOCIAL_SEEK:REDIRECT] Katya Pistsova act_address=<persona>Gosha Pistsov (was 'the Ville:Hobbs Cafe:cafe:cafe customer seating')` — step 0 (×3), also Luba at step 0, and **all four** at step 376 on resume.

But the next lines kill the "walked off the table" story:
> step 1: `[SOCIAL_SEEK:CLEAR] Katya Pistsova target='Gosha Pistsov' reason=already_chatting_with_target`
> steps 1–6: `[CP-6] Katya Pistsova … start_pos=[74, 23] | movement=[74, 23]` (body did not move)
> step 6: `🗓️ SCHEDULE STALE REPLAN: Katya Pistsova step=6 stale_count=5 — forcing replan`
> step 7: `[LIFECYCLE:CHECK] Katya Pistsova expired=True … action='sitting at a cafe table'` → `DECOMP_GATE=RUN … reason=force_replan_boundary scope=hourly_60 action='having breakfast at home while sketching nail art designs'` → `✅ NEW ACTION: Katya Pistsova - Now: settling at the desk with yogurt, fruit, and a mug of tea` with `target_zone={'min_x': 123 … 'min_y': 55}` (her dorm desk).

Katya left because the plant put her body on the tiles but **not in her day plan**; after 5 "stale" minutes the engine forced the plan back in and the plan said home. That is the plan-vs-body class again, but it's a **fixture** miss (and arguably legitimate life behaviour), not a seek miss. **What dies with this:** "P2 is the V2 cut." P2 remains a correct deletion (the resume storm at step 376 rewrote all four from cafe seating, and `CLEAR` never restores the old `act_address`), but in this run seek moved nobody's body.

**6. `too_far` is separate, after copresent; threshold 4; dawn fail was seek-leave not 7-tile lunch — CONFIRM first two, REJECT third.**
Code order in `should_converse`: `not copresent` → `too_far` (before `touch`) → hello/touch → `hard_stop`. Log: `Gosha Pistsov ↔ Luba Pistsova → too_far (dist=5 > threshold=4)` ×212; 270 `too_far` lines total, 268 of them at dist 5–6. Threshold 4 is in the line itself. Dawn fail: see item 5 — stale-schedule replan, not seek, not distance.

Extra: the planted pair's **second** hello (10:13) died to the slice gate, not pressure or seek. Katya's act was `'walking from the dorm over to Hobbs Cafe' start=10:00 dur=10`, she arrived 10:13, the arrival-based timer only expired it at 10:23 → remaining = **−4 … −12** → `overlap_steps = 0 < 2` → `hard_stop` at 10:14–10:22. Gosha↔Katya `hard_stop` attribution: 17 slice, 6 pressure, 2 chatting-other.

**7. C1 fixed; "fix sleep again" is wrong — CONFIRM.**
`next_act_allows_talk` now reads only `scratch.act_description` (`social_will.py:78–87`), no `f_daily_schedule`. Log: `reason=warming` at steps 1–6 for Gosha↔Katya, then `not_copresent` at 7 (she walked). 9 `warming` minutes total, 1 `encounter_hazard` hit (Katya↔Luba, `p=0.056`, on the street) which became the run's only full talk — proof the dice path works when nothing hard-stops it.

**8. P1/P2/P3 are deletions/narrowings, not chance knobs — CONFIRM**, with the addition below.

## What I would ship, in order

**P1 — no non-body hard stops (one cut, two deletions, one log change).**
- Story thoughts must not read as "too stressed to talk": either the leftover/stance/overlay writers stop feeding `get_internal_pressure_score`, or that function stops treating poignancy as stress. Keep poignancy 8 for retrieval.
- **P1b (same cut):** the remaining-slice gate must not count an **expired or held** act as "about to leave." A person whose walk act is past its duration and who is sitting in the cafe is the freest person in the room, not the busiest. 23% of awake persona-minutes this run had remaining < 2; 12% were negative (expired, held by the arrival timer). Alternative that fits the pattern better: delete the slice gate as a hard stop entirely (it's plan-for-body).
- **Log:** make `reason=hard_stop` say which gate (`hard_stop:pressure`, `hard_stop:slice`, `hard_stop:sleep`, `hard_stop:prewindow`, `hard_stop:chatting`). Zero behaviour change. Without it, this review had to attribute 1,167 lines by elimination, and P1's own early-log check ("Gosha↔Luba reason= is not hard_stop") would still show `hard_stop` from the slice gate and be misread as P1 failing.

**P3 — same-room skips the Manhattan veto.** Move it up. Once pressure and slice go, `too_far` is the next biggest remaining blocker on cafe seats (270 lines, 214 on Gosha↔Luba alone, all at 5–6 tiles). The founder lock says C2 follows a 2-tile pass; my read is that a 2-tile pass needs the fixture to hold two people at 2 tiles for an hour, which the day plan won't do on its own — so the lock will delay the pass, not protect it. Keep the lock if you want, but expect to hit this immediately after P1.

**P2 — seek no-op when both bodies share the arena.** Keep it, demote it to hygiene. Cheap, correct, and the resume storm (all four rewritten from cafe seating at step 376, then Katya re-hijacked at 382/387/390) says it will bite on any stop/go. Not a talk cut.

**Fixture, not engine:** the plant must also own the schedule (row 0 = the cafe sit, ≥45 min) or `SCHEDULE STALE REPLAN` will undo it in six minutes again. Without that, V2 at dawn cannot be scored no matter what engine cut ships.

## Two landmines to check before trusting any 180-minute claim after a stop/go (needs data)

- Memory `created_at` in Supabase is **insert wall-clock**, not sim time (`db_reference.md`: "ignores `created_at` in the payload — column defaults to insert time"). On disk the tea leftover reads `2026-09-15 17:16:04`, the overlay `19:29:45`. After a reload, `get_internal_pressure_score` computes `sim_now − wall_created`, which is negative → always "inside 180 minutes." That predicts a permanent mute after resume.
- Yet the log shows `warming` at step 376 (12:46, right after resume) for three pairs who all carried the 11:00 overlay thought, which should have hard-stopped them either way. So post-resume pressure behaved neither like the 180-min model nor the permanent-mute model. Out of scope for this verdict (V8), but do not run P1's falsification on a resumed sim until this is understood.

Minor latent writer, zero hits this run: `PRESSURE_KEYWORDS` (`report`, `problem`, `issue`, `important`, …) in the current action text also sets pressure to 0.5 → hard stop. A barista "handling a customer problem" would be muted by it.

---

## After Fable — updated cuts (15 Sep)

**Keep:** the class (plan-for-body / process-for-relationship / relocating preference). C1 stays closed. Do not raise talk-chance. Do not walk-to-person. Do not treat occupancy as a talk pass.

**Change:** V2 at dawn was **not** seek. Seek fired and cleared; the body left because plant tiles/caption did not own the **day plan**, then `SCHEDULE STALE REPLAN` at step 6 sent Katya home. `hard_stop` is one log bucket for **two** writers: pressure (~60%) and remaining-slice &lt; 2 (~29%; overlap clamps expired acts to 0, same forbid). The 10:14–10:22 planted-pair miss was slice, not pressure.

**Next score (new Start, not a resume):**

| Order | Cut | Why now |
|---|---|---|
| **0** | **Plant owns the morning schedule** (row 0 = cafe sit, ≥45 min, same tiles). Score fixture, not a talk knob. | Without this, dawn V2 cannot be scored. |
| **1** | **P1a + P1b + log** in one engine cut. Unplug story-thought pressure as a forbid. Delete (preferred) or ignore-expired the slice `&lt; 2` hard stop. Log `hard_stop:pressure` / `:slice` / `:sleep` / `:prewindow` / `:chatting`. | Pressure is still the biggest mute. Slice is why a second hello died after she walked back in. Opaque `hard_stop` makes P1 look like a fail. |
| **hygiene** | **P2** seek no-op if already same 3-part body. Ship with P1 if cheap; do not headline it. | Resume storm rewrites cafe seating; this run moved **nobody**. |
| **2** | **P3** same-room skips Manhattan `too_far`. Do **not** wait for a 2-tile dawn pass that the day plan will undo. | After P1, this is the next cafe blocker (270 lines, mostly dist 5–6). |

**Do not score P1 on a resumed sim** until wall-clock `created_at` vs sim-time in the 180-min window is understood. `PRESSURE_KEYWORDS` in action text is a watch, not this cut.

**Early logs on the next Start:** planted pair still on Hobbs tiles at step ≥10 (no stale-replan home); after leftover, same-room `reason=` is `warming`/`hazard` not `hard_stop:pressure`; after a late arrival, not `hard_stop:slice`; `[SOCIAL_SEEK:REDIRECT]` 0 while already cafe.