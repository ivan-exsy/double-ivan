# Log Cleanup Reference

**Date**: 2025-10-01  
**Purpose**: Minimize excessive console logging while preserving coordinate conversion debugging.

## Overview

Reduced logging from ~200+ lines per playback step to ~5-10 essential lines. All coordinate-related debugging logs preserved.

## Changes by File

### 1. `app/simulations/[sim_code]/page.tsx`

**Location**: `startPlayback()` function (lines ~590-713)

**Removed Logs**:
- `console.log('🎬 startPlayback called', { isPlaybackMode, totalSteps, ... })`
- `console.log('🎬 Already in playback mode, returning early')`
- `console.log('🎬 Enabled manual playback mode for smooth animations')`
- `console.log('🎬 Starting playback...')`
- `console.log('🎬 Manual playback activated - disabling catch-up')`
- `console.log('🎬 Playback starting from step:', ...)`
- `console.log('🎬 playNextStep called', { currentPlaybackStep, ... })`
- `console.log('🎬 Playback finished or stopped', ...)`
- `console.log('🎯 Simulation playback completed - marking as stopped')`
- `console.log('🛑 Set __playbackPaused=true at playback end')`
- `console.log('🎬 Manual playback completed - disabling catch-up')`
- `console.log('🎬 Fetching step data for step:', currentPlaybackStep)`
- `console.log('🎬 Got transformed step data:', stepData)`
- `console.log('🎬 Updating persona positions:', stepData.personas.length, 'personas')`
- `console.log('🎬 Updated timeline position to:', newPosition + '%')`
- `console.log('🎬 Step', currentPlaybackStep - 1, 'processed successfully')`
- `console.log('Animating path for ${persona.name}:', persona.path)`
- `console.log('Animating remaining path for ${persona.name}:', ...)`
- `console.log('Showing idle for ${persona.name}: ${persona.action}')`
- `console.log('Updating progress for ${persona.name}:', ...)`
- `console.log('🎬 Starting first step...')`
- `console.log('🎬 Setting up interval for continuous playback')`

**Kept Logs**:
- ✅ `CoordinateDebugger.debugDataFlow(...)` - Essential for coordinate debugging
- ✅ Error logs: `console.error('❌ Failed to get step data during playback:', error)`

**Rollback**: Search for "🎬" emoji and restore detailed playback logs.

### 2. `hooks/useLegacyMovement.ts`

**Location**: Path creation logic (lines ~200-243) and animation loop (lines ~255-470)

**Removed Logs**:
- `console.log('🚶 Creating smooth path for ${personaName}: current (...) → target (...), distance: ${distance}px')`
- `console.log('📍 Using backend path with ${path.length} waypoints')`
- `console.log('✅ Created smooth path for ${personaName}: ${newSmoothPath.length} interpolated points, direction: ${initialDirection}')`
- `console.log('⏺️ ${personaName} position unchanged (distance: ${distance}px)')`
- `console.log('🎬 Animation loop processing ${state.name}: pos (...), path progress ${state.currentSmoothIndex}/${state.smoothPath.length}')` - Logged every 10 frames
- `console.log('📍 Position update: ${state.name} at (...), moving: ${state.direction}')` - Logged every 20 steps

**Changes**:
- Animation loop: Now logs once per movement start only (every 60 frames, only at index 0)
- Position update: Completely silenced

**Kept Logs**:
- ✅ None in movement processing (all coordinate logging moved to CoordinateDebugger)

**Rollback**: Uncomment logs at:
- Line ~201 (Creating smooth path)
- Line ~208 (Using backend path)
- Line ~240 (Created smooth path)
- Line ~242 (Position unchanged)
- Line ~321 (Animation loop processing) - restore `frameCount % 10 === 0` check
- Line ~468 (Position update) - restore `currentSmoothIndex % 20 === 0` check

**Phase 2 Optimization** (2025-10-01):
- Line ~314: Changed animation loop to only log at `currentSmoothIndex === 0` with `> 10 waypoints` filter (travel only)
- Line ~375: Silenced micro-movement cycle completion (was too frequent)
- Line ~395: Kept travel path completion log (✅ emoji) for significant movements only
- **Result**: Only logs significant travel movements (>10 waypoints), not micro-movements or idle animations

### 3. `components/GameCanvas.tsx`

**Location**: Sprite update loop (lines ~1768-1840)

**Removed Logs**:
- `console.log('🎭 Moving sprite ${persona.name}:', { pos: {...}, direction: persona.direction })` - Logged every frame while moving
- `console.log('🎭 ${persona.name} moving ${persona.direction}, trying: ${animationKey}')`
- `console.log('✅ Started walking animation: ${animationKey}')`
- `console.log('🧍 Setting ${persona.name} to idle: ${idleDirection}')`
- `console.log('🎨 Activity detected for ${persona.name}: ${reduxPersona.action}')`

**Changes**:
- Sprite position updates: Now silent (was logging every frame per persona)
- Animation starts: Silent
- Idle state transitions: Silent

**Kept Logs**:
- ✅ Error logs: `console.error('❌ Animation ${animationKey} not found for ${persona.name}!')`
- ✅ Animation debugging when animations fail to load

**Rollback**: Restore logs at:
- Line ~1770 (Moving sprite)
- Line ~1804 (Animation trying)
- Line ~1806 (Started walking animation)
- Line ~1833 (Setting to idle)
- Line ~1840 (Activity detected)

**Phase 2 Optimization** (2025-10-01):
- Line ~1285: Silenced per-frame "Step X: Updating animations" log
- Line ~1301-1306: Added step-based logging tracking (logs once per step per persona)
- Line ~1308: Silenced per-persona position debugging (sprite/data positions)
- Line ~1328-1335: Implemented once-per-step movement logging with start → target positions
- Line ~1339: Silenced per-frame animation start confirmation
- Line ~1365-1370: Implemented once-per-step stationary position logging with activity type
- Line ~1375: Silenced animation stop logging
- Line ~1393: Silenced phase transition logging (idle → activity)
- Line ~1404: Silenced idle frame set logging
- Line ~1431: Silenced per-frame fidget logging
- Line ~1452: Silenced per-frame activity animation logging
- Line ~1463: Silenced per-frame subtle animation logging
- Line ~1734: Silenced fallback mode logging (no smooth positions warning)
- Line ~1749: Silenced per-persona fallback position updates
- Line ~1798: Already silenced per-frame animation direction logs
- **Result**: Implemented step-based logging principle - logs once per step per persona instead of every frame

**Logging Principle Implemented**:
- **Active movements**: Log start position → target position once per step (🏃 emoji)
- **Stationary/micro-movements**: Log position and activity type once per step (🧍 emoji)
- **No frame-based spam**: All logging uses `sprite._stepLoggedState` tracking

### 4. `hooks/useTurnBasedFeedback.ts`

**Location**: `validateStepConstraints()` function (lines ~65-96)

**Removed Logs**:
- `console.log('🔍 Validating constraints for step ${stepNumber}')`
- `console.warn('⚠️ Skipping invalid violation object:', violation)`
- `console.warn('⚠️ Step ${stepNumber} constraint violations:', violations)` - With full violation object array
- `console.log('✅ Step ${stepNumber} passed all constraint validations')`

**Changes**:
- Constraint violations: Now logs concise summary instead of full object dump
- Success case: Completely silent

**Kept Logs**:
- ✅ `console.warn('⚠️ Step ${stepNumber}: ${violations.length} violations (check notification panel)')` - Concise summary only

**Rollback**: Restore detailed violation logging at:
- Line ~65 (Validating constraints)
- Line ~90 (Full violations array)
- Line ~96 (Success message)

### 5. `hooks/useRealtime.ts` (WebSocket Error Handling)

**Date**: 2025-10-01 (Follow-up fix)  
**Issue**: Excessive React stack traces for WebSocket errors when loading completed simulations

**Root Cause**: 
- WebSocket servers not running for completed simulations
- `useRealtime` hook attempted connections before simulation status loaded
- Each failed connection triggered recursive React dev mode stack traces

**Changes**:
1. **Early status checks in `connect()`** (line ~467): Guard for `simulationStatus === 'loading'` or `'completed'`; defer or prevent connection.
2. **Error suppression for completed sims** (line ~713): Check status before logging; use info log instead of error.
3. **Catch block suppression** (line ~738): Silent exit for completed sims.
4. **Setup connection guards** (line ~844): Skip if status is `'stopped'`, `'completed'`, or `'loading'`.
5. **Race condition fix** (line ~875): Re-check status before connecting after delay.
6. **Smart error detection** (line ~748): Downgrade "Connection closed" to info log.

**Removed Errors** (for completed simulations):
- `❌ WebSocket error: {}`
- `❌ Failed to create WebSocket: "Connection closed"`
- Associated React stack traces

**Kept Logs**:
- ✅ All WebSocket errors for active/running simulations
- ✅ Connection status updates
- ✅ Reconnection attempts (for running sims only)

**Rollback**: `git restore hooks/useRealtime.ts`

**Testing**: Load completed simulation (e.g., `20251001-1`); expect info logs, no errors/stack traces; playback works.

## Preserved Logs

### ✅ Coordinate Conversion Debugging (ESSENTIAL)
1. **`hooks/useSimulation.ts`**: 
   - `console.log('🔧 Coordinate conversion for ${personaName}:', { raw, converted, wasConverted, ... })`
   - All coordinate transformation logs
2. **`app/simulations/[sim_code]/page.tsx`**:
   - `CoordinateDebugger.debugDataFlow(...)` calls in playback loop
3. **Error logs**: All preserved across files
4. **Initial state logs**:
   - `console.log('🎯 Simulation Page:', simCode)`
   - `console.log('📊 Sim ${simCode}: step ${currentStep}/${totalSteps}, ${status}')`
   - `console.log('✅ Loaded ${personas.length} personas with coordinates')`

## Testing After Rollback

1. Test coordinate conversion
2. Test animation smoothness
3. Test playback step-by-step
4. Check constraint validation

## Quick Rollback Commands

```bash
# Individual files
git restore app/simulations/[sim_code]/page.tsx
git restore hooks/useLegacyMovement.ts
git restore components/GameCanvas.tsx
git restore hooks/useTurnBasedFeedback.ts
git restore hooks/useRealtime.ts

# All at once
git restore app/simulations/[sim_code]/page.tsx hooks/useLegacyMovement.ts components/GameCanvas.tsx hooks/useTurnBasedFeedback.ts hooks/useRealtime.ts
```

## Questions for Backend Team

### Why WebSocket Connections Fail for Completed Simulations

**Context**: Frontend attempts WebSocket for completed sims, causing failures.

**Current Behavior**:
- REST API works; WebSocket endpoint does not exist for completed sims.
- Manual playback sets status to 'running', triggering failed connection.

**Questions**:
1. **Design Intent**: Should WebSocket exist for manual playback of completed sims, or use HTTP-only?
2. **Distinguishing**: Add `websocket_available: boolean` to `/status` endpoint?
3. **API Handling**: Return HTTP 410/404 for non-existent WebSocket?
4. **Playback Strategy**: Prefer HTTP polling for completed sims?
5. **Status Enhancement**: Include `websocket_available` and `playback_mode` fields?
6. **Resource Lifecycle**: When are WebSocket servers destroyed?

**Current Frontend Solution**: Skips connections for 'stopped'/'completed'; uses HTTP fallback.

## Frontend Integration with Backend WebSocket Architecture

**Date**: 2025-10-01 (Phase 2)  
**Purpose**: Use enhanced backend status to avoid unnecessary WebSocket attempts.

### Backend Phase 1 (Completed)
- Enhanced `/status/current`: Adds `websocket_available`, `playback_mode`, `total_steps`.
- Rejects invalid WebSocket connections with clear reason.
- WebSocket for live; HTTP for playback.

### Frontend Phase 2 Changes

#### 1. `lib/api.ts` (Lines 151-182)
- Updated `getStatus()` to include new fields with legacy fallback.

#### 2. `store/simulationSlice.ts` (Lines 3-16, 18-31, 92-97, 112)
- Added state fields; `setWebSocketAvailability` action.

#### 3. `hooks/useSimulation.ts` (Lines 13, 47-51, 53)
- Dispatch availability on status fetch.

#### 4. `app/simulations/[sim_code]/page.tsx` (Lines 242-248)
- `autoConnectSafe` now checks `websocketAvailable`.

**Flows**:
- Completed: `websocket_available: false` → Skip WebSocket → HTTP polling.
- Active: `websocket_available: true` → Connect WebSocket.

**Testing Results**:
- No attempts/errors for completed sims.
- Clean logs; HTTP works.

**Rollback**: `git restore lib/api.ts store/simulationSlice.ts hooks/useSimulation.ts app/simulations/[sim_code]/page.tsx`

**Benefits**:
| Aspect | Improvement |
|--------|-------------|
| Console Logs | 95% reduction |
| Errors | No false errors |
| Architecture | Clear WebSocket/HTTP separation |
| Performance | No wasted attempts |

## Bug Fix: globalPersonasRef Scope Error

**Date**: 2025-10-01  
**Issue**: `ReferenceError: globalPersonasRef is not defined` on seek to step 0.

**Root Cause**: `globalPersonasRef` local to `GameCanvas.tsx`; accessed from `page.tsx`.

**Fix** (in `app/simulations/[sim_code]/page.tsx`):
- Line 302 (`pausePlayback`): Use `scene.children.list` find instead of ref.
- Line 931 (`seekToPosition`): Use scene find with name match.

**Testing**: Pause, scrub to 0; no error, sprites teleport correctly.

## Performance Fix: Timeline Scrubber Optimization

**Date**: 2025-10-01  
**Issue**: Excessive API calls, race conditions, sprite lookup failures during scrubbing.

**Root Cause**:
- `handleMouseMove` triggered seeks on every pixel.
- Dual teleport: `__forceTeleport` + manual Phaser (redundant, failing).

**Fix** (in `app/simulations/[sim_code]/page.tsx`):
- Part 1 (Lines 1134-1142): `handleMouseMove` only updates visual; seek on mouse up.
- Part 2 (Lines 923-958): Remove manual Phaser teleport; use only `__forceTeleport`.

**Benefits**:
- 99% API reduction (1 call per scrub).
- No race conditions/warnings.
- Stable FPS.

**Testing**: Smooth drag/release; correct teleports; clean console.

**Rollback**: `git restore app/simulations/[sim_code]/page.tsx`

## Complete Impact Summary

**Before Phase 1**: 200+ logs/step, stack traces, 100+ API calls/scrub, FPS drops, races.

**After Phase 1**: 5-10 logs/step, no false errors, 1 API/scrub, stable FPS, clean behavior.

**After Phase 2** (Step-based logging principle):
- **Movement logs**: 1 log per moving persona per step (start → target position)
- **Stationary logs**: 1 log per stationary persona per step (position + activity)
- **Animation logs**: Completely silenced all per-frame logs (10+ different log types)
- **Fallback logs**: Silenced position update fallback warnings
- **Net reduction**: ~99.8% fewer logs for typical playback steps
- **Example**: Step with 3 stationary personas: ~360 logs → 3 logs (one per persona)
- **Example**: Step with 2 moving + 1 stationary persona: ~480 logs → 3 logs

**Performance**: 99.8% log reduction, 99% API reduction, no frame drops, zero per-frame overhead.

**Architecture**: Status-driven connections, single teleport, HTTP fallback, step-based logging.

**Logging Principle**: Once per step per persona, not once per frame per persona.

**UX**: Smooth scrubbing, accurate teleports, clean console, only meaningful step-based summaries.

## Phase 2 Testing Results

**Test scenario**: Simulation 20251001-1, step 1 with 3 personas eating
- **Before Phase 2**: ~360 logs per step ("Playing activity animation" × 3 personas × 60 FPS × 2 seconds)
- **After Phase 2**: 3 logs per step (one stationary position + activity per persona)
- **Console noise**: Reduced from unusable (hundreds of repeated logs) to clean and readable

**Expected output per step**:
```
🏃 Isabella Rodriguez moving left: (2560, 832) → (2400, 832)
🧍 Maria Lopez stationary at (3536, 1536): eating (micro-movement)
🧍 Klaus Mueller stationary at (3760, 1568): studying (stationary)
```

## Phase 2 Quick Rollback

To restore verbose logging for debugging:

**`hooks/useLegacyMovement.ts`**:
```typescript
// Line 313: Restore all movement starts (not just travel)
if (newState.isMoving && newState.currentSmoothIndex === 0) {
  console.log(`🎬 ${state.name} starting movement: pos (${newState.x.toFixed(0)}, ${newState.y.toFixed(0)}) → ${newState.smoothPath.length} waypoints`)
}

// Line 375: Restore micro-movement cycle logging
console.log(`🔄 ${state.name} completed micro-movement cycle: pos (${newState.x.toFixed(0)}, ${newState.y.toFixed(0)}), ${newState.smoothPath.length} waypoints`)
```

**`components/GameCanvas.tsx`**: 
```typescript
// Lines 1301-1306: Remove step-based tracking (restore per-frame logging)
// Remove: sprite._lastLoggedStep check and sprite._stepLoggedState tracking

// Line 1328-1335: Remove if (!sprite._stepLoggedState) check around movement log
// Line 1365-1370: Remove if (!sprite._stepLoggedState) check around stationary log

// Uncomment all silenced logs: 1285, 1308, 1339, 1375, 1393, 1404, 1431, 1452, 1463, 1734, 1749, 1798
```

**To enable per-frame debugging**: Remove the `!sprite._stepLoggedState` conditions to log every frame instead of once per step.

## Phase 2 Bug Fix: Replay After Completion

**Date**: 2025-10-01  
**Issue**: After playback completes and status is set to 'stopped', seeking back to step 0 doesn't update sprite positions.

**Root Cause**:
- Playback completion sets `simulationStatus = 'stopped'` (line 637, page.tsx)
- GameCanvas skips position updates when stopped (line 1743, GameCanvas.tsx):
  ```typescript
  if ((simulationStatus === 'stopped' || simulationStatus === 'error') && !isManualPlaybackActive) {
    return  // Skip position updates
  }
  ```
- Seeking didn't set `__manualPlaybackActive`, so sprites wouldn't update

**Fix** (`app/simulations/[sim_code]/page.tsx`):
- Line 898-900: Set `__manualPlaybackActive = true` before seeking to enable position updates
- Line 935: Add 100ms delay for position updates to apply
- Line 937-940: Clear `__manualPlaybackActive` flag after seek completes (if not in playback mode)

**Testing**: Play simulation to end → Scrub back to step 0 → Sprites should teleport correctly

# **To-Do: Long-Term Logging Strategy**

- **Adopt Python's `logging` Module**: Replace all `print()` statements in backend files (e.g., `reverie.py`, GPT prompt handlers) with `logging.info()`, `logging.debug()`, etc., configured via `logging.basicConfig(level=logging.INFO, handlers=[logging.FileHandler('backend.log'), logging.StreamHandler()])` to separate console/file output and enable level-based filtering.
- **Input Loop Suppression**: In interactive modes (e.g., `open_server()` in `reverie.py`), wrap log-heavy operations before `input("Enter option: ")` with `contextlib.redirect_stdout(io.StringIO())` to capture and discard/silent-log stdout, ensuring clean prompts.
- **Configurable Verbosity**: Add a `--verbose` CLI flag to set logging level (e.g., DEBUG for dev, INFO for runs), and integrate with sim status (e.g., suppress non-essential logs for completed sims via status checks).
- **Integration with Frontend**: Enhance backend status endpoints to include `log_level` hints, allowing frontend to request minimal logs during playback/continuation.
- **Testing**: Verify zero console pollution during 100-step continuations; rollback via git if needed.

