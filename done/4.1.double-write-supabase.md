### **Enable Double-write Backend**

*VALIDATE BEFORE PROCEEDING*
--------------------------------------------
1. Do I have to have Supabase enabled for MVP-Deploy?- *skip MVP deploy if works as-is and the cost is manageble*
2. Validate Supabase DB schema @supabase\db_schema.md
3. Legacy(?) Supabase analysis @docs\upd_supabase.md
--------------------------------------------

1. Implement Reality Grid (mirrors reality with immuvable and moveable objects, used by back & front) - per @docs\movements\3.4.4.SOT_coords.md


  *GOAL:*
  a. migrate the legacy file-based memory architecture to Supabase with minimal changes: replicate JSON storage in Supabase Storage or tables (e.g., using blobs for associative_memory/nodes.json and embeddings.json), 

  b. integrate dual-write logic in reverie.py and persona/memory_structures to sync file and DB operations without altering core simulation flows, and 

  c. validate consistency via cross-check scripts (e.g., dbl_get_recent_memories RPCs). 

  d. Focus on schema isolation with dbl_ prefixes and RLS to avoid impacting production, then test a 24-hour simulation to baseline performance metrics like token usage and query latency. 