# 20260901-1 — Checklist: Phase 2B leftover KEEP

**Purpose:** Score leftover KEEP + Writer 2 on one tip. A **full talk** (not a hello) mints one leftover on ConversationManager + memory. The sit stops at mint or 6 unique lines (hard cap 12). Fake `desync_scratch_cleared` is not a completed sit and not a sequel. Occupancy at **11:00** and **20:00** is a **hold**. Hello mill from Pass 2b must stay **0**.

**Prior:** `20260831-2` Pass 2b **partial**. Hello mill **PASS** (Hobbs-both re-open inside 30 = **0**). Occupancy hold **PASS**. Writer 2 **FAIL** (**53 / 97** `CM.END` were `desync_scratch_cleared`). Linger helper **0**. Card: `20260831-2_checklist.md`. **Do not resume** `20260831-1` or `20260831-2`. Do not copy that FAIL as if it were this sim.

**This tip:** `railway` / `ivan/talk-leftover-keep` @ **`bb22747d`** (Pass 2b `b8d1fc15` + leftover mint + sit stop + same-arena desync restore). A start off `b8d1fc15` is another 2b, not 2B. A start off `2f5f7269` is the Pass 2 mill.

**Score sim:** `20260901-1`  
**Match 1-B / Pass 2 / 2b exactly:** `soul15_seed_20260224` · `copy_memories=true` · `copy_coords=false` · sprint · **`skip_premiere: true`** · `max_steps: 2400` · **`diagnostic_mode: false`** · `HEADLESS_TAB_REUSE=false` · `TASK_DECOMP_CONTEXTUAL_ENABLED=true`

**Diagnostic stays off.** Leftover lines, chat ids, first lines, occupancy, and persist are already on disk.

**Score at tiles, not labels.** Cafe box **x 72–83, y 19–30**. Day 1 **/15**; day 2 **/14**. Occupancy bar **12** then **11**.

**Primary bet:** leftover KEEP + Writer 2. Compare mill to `20260831-1` @260 (**77** re-opens inside 30, **54** at `the Ville:Hobbs Cafe:cafe`) and to 2b through 2285 (**0** Hobbs-both). Compare Writer 2 to 2b **53 / 97** `desync_scratch_cleared`.

**Named fix on this tip (score in village):**
1. Same pair, same 3-part arena, empty `chatting_with` → restore partner. Log `DESYNC REPAIR keep`. Do **not** `CM.END`. Do **not** mint leftover. Do **not** mint a new `session=` while both still in-room.
2. Sit stop: 6 unique `[speaker, text]` rows = no second dump; hard cap **12**. Log `SIT COMPLETE reason=beat_budget` (or `commitment_mint` / `no_new_pressure` / `left_arena`). 80-minute remix is the abort.
3. Full sit, allowed complete reason → `LEFTOVER MINTED class=… pair=… session=…`. Classes `meet` / `tell` / `avoid` always. Survival may also tag `protect` / `expose` / `mutual_in`. Greeting / desync / `slicing_complete` / `pause_timeout` mint **0**.
4. Honor only if co-present after mint (`LEFTOVER HONORED`). Break if due (60 steps) with no co-presence (`LEFTOVER BROKEN`). No walk. Seek off.
5. Chat never writes `vote.target`. Start-of-talk `record_alliance_from_chat` is gone.

**Not this pass:** seek, linger +10, WHO-gate, Option 2 (hellos through ConversationManager), sit-and-read list widening, leftover cafe walks, overlay “you played / you were absent”, drain/continue, FE home-snap, RLS/disk, Shepard sleep booking, appointment table, P3-1 `external_inbox`.

**Out (unchanged):** H3 dest-rewrite, fail-closed, longer pins, fire-when-12, restore the lock, add `study` to the cafe list, turn seek on, lengthen linger, stamp `double-docs/sot/`.

**Record:** fork `2026-09-01T23:19:57.672134+00:00` · UUID `258ff6c9-f232-4841-9478-2b9698fc2d77` · PID `1091572` · tip `bb22747d` · sim `20260901-1` · maze `80af36a4-ecec-4e7c-8b39-650729663351`  
**Skip-premiere log:** `Skip premiere: clock 2026-09-01 06:30:00 → 2026-09-02 05:55:00 (engine day 2 05:55)`

---

## What you would miss if you only did stop → pull → start

1. **Confirm the box is on `bb22747d` after pull**, before fork. `b8d1fc15` has no leftover mint. `2f5f7269` mills hellos.
2. **Seek stays off.** Walking toward a named person moves bodies. If occupancy dips, you cannot tell leftover honor from seek.
3. **Do not add `study` to the cafe list.** Mixing list widening in makes an occupancy miss unreadable.
4. **Disable `apt-daily-upgrade` auto-restart of `api-gateway`.** Killed `20260822-1`.
5. **Do not resume `20260831-1` or `20260831-2`.** Both stopped. Stopping a leftover PID is not a data loss.
6. **First cafe read is leftover mint + mill**, not the 11:00 count. Occupancy is a hold. If mill returns, or 4–6 Hobbs full sits mint **0** leftovers, more steps will not save it — **stop**.
7. **If occupancy dips below bar:** stop and split. Do not patch linger or seek. Do not restore the lock.
8. **Do not treat `DESYNC REPAIR keep` as an END.** Same `session=` while both still at the same 3-part arena is the pass. A new `session=` with no leave is Writer 2 again.

---

### When to look — checkpoint ladder

Step 0 = day-2 05:55. One step = one minute. Pace ~50–80 steps/h. Track the **step**, not the wall clock.

Quote `conversation_id`, first line, `act_address`, leftover `class=`, `session=`, and names **verbatim**. Do not shorten to “they promised something.”

| # | Step | Clock | What you read | If it's wrong |
|---|---|---|---|---|
| 1 | **~5** | 06:00 d1 | Engine jumped; Survival on; no premiere / “full cohort of fifteen”. Tip is `bb22747d` | **PASS @ start.** Tip `bb22747d`. Log: `Skip premiere: clock 2026-09-01 06:30:00 → 2026-09-02 05:55:00 (engine day 2 05:55)`. Live status through **619**: `is_survival=true`, `current_day_label=Survival Season Day 1`, `engine_day=2`, PID `1091572` |
| 2 | **~60** | 07:00 d1 | No crash. Persist over-6 = **0**. Seek logs still quiet. Hello mill not the cafe yet | **PASS persist / crash through 619.** `Rejecting movement_report` = **0**. `Traceback` = **0**. `SOCIAL_SEEK:ARM` **8**, all → `Alex Butcher` (until_step 45–105). `SOCIAL_SEEK:CLEAR` reasons `target_sleeping` **7** / `seek_window_expired` **7** / `already_chatting_with_target` **1** — old once-a-day hunt, not a new everyday ship |
| 3 | **First cafe sits** (often **~120–240**, do not wait for 11:00) | morning | **Mill + KEEP.** Same pair still at `the Ville:Hobbs Cafe:cafe`: **no** second `👋 GREETING TRIGGERED` inside **30** steps. `🛋️ SOFA QUIET` present. After **4–6 Hobbs full sits**: `LEFTOVER MINTED` ≥ **1**. No remix past 12 unique lines. `DESYNC REPAIR keep` instead of a new `session=` with no leave | **Holding through 619. Do not stop.** Hobbs-both re-open inside 30 = **0**. Same-room re-open inside 30 = **0**. Quiet **14135**. First Hobbs mint `LEFTOVER MINTED class=meet pair=Dean Sanford ↔ Diana Ogden session=Dean Sanford_Diana Ogden_316 step=317`. `DESYNC REPAIR keep` **4**. `desync_scratch_cleared` **0**. Unique lines after 12 = **0** (two sits hit **12**, none past). See §1–§3 |
| 4 | **240–305** | 10:00–11:00 d1 | Occupancy **hold** (bar 12/15) + mill still 0 + leftover still minting on full sits | **PASS occupancy 14/15 @305** (bar 12). Curve: 10:00 **5/15** → 10:15 **13/15** → 10:30 **14/15** → 10:45 **14/15** → 11:00 **14/15** → 12:00 **15/15**. Off @305: Dean Sanford `(58, 28)` `Arriving at Hobbs Cafe and finding a seat @ the Ville:The Rose and Crown Pub:pub:bar customer seating` — pub leftover, not a stay-gone pile. Mill still **0**. **Do not stop** |
| 5 | **785–845** | 19:00–20:00 d1 | Vote hold (bar 12/15). Evening sofa still one talk until leave. Honor/break by co-presence only | **Not scored on this tip.** Inherit 2b **15/15 @845** + evening mill **0**. KEEP does not walk. Founder stop @619 |
| 6 | **~1445** | 05:55 d2 | Headcount **14**. Premiere string still gone | **Not scored on this tip.** Inherit 2b |
| 7 | **1745** | 11:00 d2 | Occupancy **hold** (bar **11/14**) | **Not scored on this tip.** Inherit 2b **13/14 @1745** |
| 8 | **2285** | 20:00 d2 | Occupancy **hold** (bar 11/14). Then **stop** | **Not scored on this tip.** Inherit 2b **12/14 @2285**. Stopped @619 |

Harvest freeze **@619** (16:15 d1). Founder stop: KEEP village-green. Do not run to 2285. Evening mill + 20:00 / day-2 occupancy **inherit 2b**, not re-scored on this tip.

---

### 0. Preflight

- [x] `20260831-1` **not running**; do not resume (status `stopped`, step 260)
- [x] `20260831-2` **not running**; do not resume (status `stopped`, step 2301)
- [x] Local `ivan/talk-leftover-keep` @ **`bb22747d`** pushed; `railway` fast-forwarded to that tip (never rebase / force-push `railway`)
- [x] VPS `git pull` in `/var/www/generative_agents` → tip **`bb22747d`**. `leftover.py` present
- [x] `sudo systemctl restart api-gateway` (only after the old PID is gone)
- [x] Unattended-upgrades will not restart that unit mid-run (`apt-daily-upgrade.timer` **inactive** / **disabled**)
- [x] Env unchanged vs 1-B: `HEADLESS_TAB_REUSE=false` · `TASK_DECOMP_CONTEXTUAL_ENABLED=true` · `MAX_TILES_PER_STEP=6` · `SURVIVAL_MODE_ENABLED=true`
- [x] Seek **off** (no `SOCIAL_SEEK=` in `.env.local`; no everyday / multi-seek ship on this tip)
- [x] Hobbs cafe whitelist still **no** `study` — maze `sectors/Hobbs Cafe/arenas/cafe/activity_whitelist` = `['eat', 'social', 'serve', 'relax']`. `[whitelist]` relocate lines in this log **0**
- [x] Fork + start `20260901-1`: sprint, `skip_premiere: true`, `max_steps: 2400`, **`diagnostic_mode: false`**
- [x] Engine clock jumped to day 2 ~05:55; step **0** (log: `Skip premiere: clock 2026-09-01 06:30:00 → 2026-09-02 05:55:00 (engine day 2 05:55)`)

**Record here:** fork `2026-09-01T23:19:57.672134+00:00` · UUID `258ff6c9-f232-4841-9478-2b9698fc2d77` · PID `1091572` · tip `bb22747d` · sim `20260901-1` · maze `80af36a4-ecec-4e7c-8b39-650729663351`

API status at start still said `current_day_label=Premiere` at step **0** until the first published step. The runner log is the jump evidence. Re-check status at ladder row 1.

---

### Clock map (same as 1-B / `20260831-2`)

| Event | Clock | Step |
|---|---|---|
| Day-1 challenge | 11:00 | **305** |
| Day-1 vote | 20:00 | **845** |
| Day-2 05:55 | — | **1440** |
| Day-2 challenge | 11:00 | **1745** |
| Day-2 vote | 20:00 | **2285** |
| Day-3 replan overwrites day-2 | midnight | 2525 |

**Minimum useful run: first cafe sits** for the KEEP abort. Occupancy hold *would* need **2285**; founder stopped @619 and inherited 2b’s four fires + evening mill. Do not run to 4000.

---

### 1. Hello mill (re-verify, abort if it returns)

Bar: same pair, still at `the Ville:Hobbs Cafe:cafe`: they do **not** greet again inside **30** steps. Leave then a new hello is fine.

| Signal | Pass 2 (`20260831-1` @260) | Pass 2b (`20260831-2` @2285) | This run (`20260901-1`) |
|---|---|---|---|
| Distinct chat opens | **180** `greeting_{step}_…` + **19** CM ids | **473** `👋 GREETING TRIGGERED` + **102** full-talk session ids | Through **619**: **270** `👋 GREETING TRIGGERED` + **20** `CM.END` (27 `CM.START` lines / 20 unique sessions) |
| Greeting re-open within **30** steps, both still at `the Ville:Hobbs Cafe:cafe` | **54** | **0** | **0** through 619. **Holding** |
| Same-room re-open inside 30 (any room) | (Hobbs was the mill) | **0** | **0** through 619. **Holding** |

Quote at least one continued sit: first `👋 GREETING TRIGGERED` then `🛋️ SOFA QUIET` / `reason=sofa_greeting_quiet` while they never left.

Harvest through **619**: first cafe hello `👋 GREETING TRIGGERED: Max Shoemaker ↔ Vincent Slater step=72 reason=first_daily_fleeting arena_a=the Ville:Hobbs Cafe:cafe arena_b=the Ville:Hobbs Cafe:cafe` then `🛋️ SOFA QUIET: Max Shoemaker ↔ Vincent Slater already said hello in this room`. Quiet **14135**. Greeting reasons: `short_overlap_no_will` **103**, `medium_overlap_no_will` **69**, `first_daily_fleeting` **60**, `fleeting_same_arena` **37**, `long_overlap_no_will` **1**.

Cafe mill to beat from `20260831-1` (both already at `the Ville:Hobbs Cafe:cafe`): Owen Logan ↔ Vince Vale steps **75**, **78**, **83**. Must not return.

This run: **one** cafe hello `👋 GREETING TRIGGERED: Owen Logan ↔ Vince Vale step=260 reason=first_daily_fleeting arena_a=the Ville:Hobbs Cafe:cafe arena_b=the Ville:Hobbs Cafe:cafe`. Later `step=607` is **not** both cafe (`arena_a=the Ville:The Willows Market and Pharmacy:store` / `arena_b=the Ville:Hobbs Cafe:cafe`). Pass 2 mill **did not return**.

**Pass mill** = Hobbs-both re-open inside 30 stays **0**. Do not require opens → 0 — strangers may still say hello **once**. **PASS morning re-verify through 619.** Evening mill not re-scored; inherit 2b **0** through 2285 (first hellos on walk-in still allowed).

---

### 2. Writer 2 — fake desync is not an end (named fix)

On `20260831-2`, emit emptied `chatting_with`, `process_step` ended the live CM session (`desync_scratch_cleared`), then a new `session=` opened with no leave. **53 / 97** ENDs.

| # | Signal | Pass? |
|---|---|---|
| 1 | Same pair, same 3-part arena, empty `chatting_with` → log `DESYNC REPAIR keep`; same `session=`; `chatting_with` restored | [x] **Holding through 619.** Keep **4**: `⚠️ DESYNC REPAIR keep: CM session Irene Dove_Max Shoemaker_274 Irene Dove ↔ Max Shoemaker still same arena — restored chatting_with` (×3) then same `session=Irene Dove_Max Shoemaker_274` until `reason=beat_budget duration=32`. Also keep on `session=Alex Shepard_Alexis Reed_308` |
| 2 | `CM.END` `reason=desync_scratch_cleared` only after they **left** the arena (or true participant mismatch). Count ENDs by reason | [x] **Holding.** `CM.END` **20**: `beat_budget` **18**, `pause_timeout` **1** (`session=Alex Shepard_Alexis Reed_308`), `left_arena` **1** (`session=Alex Shepard_Andrew Abrams_593`). `desync_scratch_cleared` **0** |
| 3 | That desync reason mints **0** leftovers | [x] **Holding.** `desync_scratch_cleared` never logged. No leftover on that reason |
| 4 | No new `session=` while both still at the same 3-part arena with no leave | [ ] **Watch, not the 2b leak.** After a real `beat_budget` complete, same pair still at `the Ville:Hobbs Cafe:cafe` can open a **new** `session=` (Irene Dove ↔ Max Shoemaker `_247` → `_274` → `_321` → `_336` → `_488`). That is sit-stop then another full talk, **not** `desync_scratch_cleared` then a sequel. Keep held `_274` as one id. Do not abort |

Quote one keep: `⚠️ DESYNC REPAIR keep: CM session Irene Dove_Max Shoemaker_274 Irene Dove ↔ Max Shoemaker still same arena — restored chatting_with` — same `session=` before and after. Leave-arena END: `🔴 CM.END: ['Alex Shepard', 'Andrew Abrams'] session=Alex Shepard_Andrew Abrams_593 reason=left_arena duration=13` — leftover mint on that complete used `left_arena`, not desync.

**Pass Writer 2** = keep path fires in village; sequel-without-leave is gone. A leftover `desync_scratch_cleared` END **after leave** is not 2b’s leak. **Keep path is live. 2b leak is dead through 619.**

---

### 3. Leftover mint (the KEEP bet)

One leftover per **full** sit. Heuristic class. No new LLM. No appointment table.

**May mint:** `commitment_mint` · `beat_budget` · `no_new_pressure` · `left_arena`  
**Must not mint:** `desync_scratch_cleared` · `desync_participant_mismatch` · `pause_timeout` · `slicing_complete` · greeting / `greeting_{step}_…` / `👋 Hardcoded greeting`

| # | Signal | Pass? |
|---|---|---|
| 1 | After **4–6 Hobbs full sits**: `LEFTOVER MINTED` ≥ **1** with `class=` in `meet` / `tell` / `avoid` (Survival may also show `protect` / `expose` / `mutual_in`) | [x] **Holding through 619.** `LEFTOVER MINTED` **10**. Classes: `meet` **7**, `mutual_in` **2**, `avoid` **1**. First Hobbs mint: `LEFTOVER MINTED class=meet pair=Dean Sanford ↔ Diana Ogden session=Dean Sanford_Diana Ogden_316 step=317`. Also cafe `mutual_in` Irene Dove ↔ Max Shoemaker `_336` |
| 2 | Greeting-only sits → **0** leftovers | [x] **Holding.** All 10 mints are CM `session=` ids. `greeting_` / hardcoded greeting leftover hits **0** |
| 3 | Sit stops: `SIT COMPLETE reason=` in the allowed set. No second `generate_batch` after 6 unique lines. Unique lines after 12 → **0** | [x] **Holding.** `SIT COMPLETE` **35** lines (`beat_budget` **34**, `left_arena` **1`). Pair-row line counts: 6, 10, 10, 6, 6, 6, 6, **12**, 6, 8, 10, 8, 8, 10, 10, 6, **12**, 10, 6. After 12 = **0**. `generate_batch_conversation` log **0** |
| 4 | If no class matches: complete `no_new_pressure` and **do not** fake a leftover | [ ] **Partial.** Early Hobbs completes `_247` / `_258` / `_274` / `_306` / `_321` ended `beat_budget` with **no** mint (no fake leftover). None logged `no_new_pressure`. Not an abort |

Log watch: `LEFTOVER MINTED class=… pair=… session=…` · `SIT COMPLETE reason=beat_budget session=…`

**Abort:** 0 mints after 4–6 Hobbs full sits, **and/or** remix past 12 unique lines. **Not fired.**

Quote at least one mint with verbatim `class=`, pair, `session=`, and one chat clause. Quote one hello with mint **0**.

Mint: `LEFTOVER MINTED class=meet pair=Dean Sanford ↔ Diana Ogden session=Dean Sanford_Diana Ogden_316 step=317`. Hello with mint 0: `👋 GREETING TRIGGERED: Max Shoemaker ↔ Vincent Slater step=72 … arena_a=the Ville:Hobbs Cafe:cafe arena_b=the Ville:Hobbs Cafe:cafe` — no leftover on that greeting.

---

### 4. Honor / break (co-presence only)

No walk. No dest rewrite. Due = minted_step + **60**.

Honor is allowed **while they are still in the same room after mint** (`step > minted_step` and same 3-part arena). That is not a fail. Honor **without** co-presence is a fail.

| # | Signal | Pass? |
|---|---|---|
| 1 | `LEFTOVER HONORED` only when both share a 3-part arena after mint | [x] **Holding through 619.** Honor **10** / mint **10**. Each honor is mint+1 to mint+3 while the sit is still open (e.g. `LEFTOVER HONORED class=meet pair=Dean Sanford ↔ Diana Ogden session=Dean Sanford_Diana Ogden_316 step=318`). Same-room after mint is allowed |
| 2 | `LEFTOVER BROKEN` when due elapsed with no co-presence | [ ] **N/A so far.** `LEFTOVER BROKEN` **0**. Every mint was honored immediately. Do not require a broken row |
| 3 | No seek / no new walk toward the partner to force honor | [x] **Holding.** Seek ARMs are the old morning hunt to `Alex Butcher`, all cleared by step **105**. No leftover-driven walk |

**Pass honor** = no honored row without co-presence. Do not require a broken leftover on this run if every open leftover is still inside the due window. **Holding.**

---

### 5. Ballot firewall + Survival read

| # | Signal | Pass? |
|---|---|---|
| 1 | **0** leftover / CM writes to `vote.target` | [x] **Holding through 619.** `vote.target` log hits **0** |
| 2 | Survival may copy `protect` / `expose` / `mutual_in` into `scratch.survival["alliance_commitments"]` **after mint**, both in Survival. Family / non-flavor leftover does not need that dict | [ ] **Minted, scratch not dumped.** `mutual_in` **2** (Irene Dove ↔ Max Shoemaker `_336`; Alex Butcher ↔ Diana Ogden `_557`). Did not read `alliance_commitments` this harvest |
| 3 | No `record_alliance_from_chat` at `start_conversation` | [x] **Holding.** `record_alliance_from_chat` log hits **0** |

**Abort:** any chat → `vote.target` write.

---

### 6. Occupancy hold (not the bet)

Same bar as 1-B / 2b. If it dips, **stop and split** — do not patch on this tip.

| Fire | Bar | 1-B | `20260831-2` | This run |
|---|---|---|---|---|
| Day-1 challenge 11:00 | 12/15 | **15/15** | **15/15** @305 | **14/15 @305** (bar 12). Off: Dean Sanford pub leftover. **Do not stop** |
| Day-1 vote 20:00 | 12/15 | **14/15** | **15/15** @845 | **Not re-scored.** Inherit 2b. Founder stop @619 |
| Day-2 challenge 11:00 | **11/14** | **12/14** | **13/14** @1745 | **Not re-scored.** Inherit 2b |
| Day-2 vote 20:00 | 11/14 | **12/14** | **12/14** @2285 | **Not re-scored.** Inherit 2b |

Curve at 10:00 / 10:15 / 10:30 / 11:00 — do not invent 11:00. Do not read a 10:00 ~6/15 as an occupancy miss.

**Accepted leftovers (not a 2B fail, same as 1-B / 2b):** Shepard at the pub; late bodies whose dest is still Hobbs; Vince breakfast-at-home; pub `relax`/`eat`. Four-room dests that **return** by fire are not the 1-A stay-gone pile.

---

### 7. Hold from 1-B / Pass 2b (regression watch)

- [x] Start-jump persist over-6 = 0 through **619** (`Rejecting movement_report` = **0**, `Traceback` = **0`)
- [ ] Honest caption: watch, not this bet. @305 Dean caption `Arriving at Hobbs Cafe and finding a seat` while tile is pub
- [x] Off-cafe ritual verbs at fire = **0** (Dean is arrive/seat only; no hold/fold/vote on the wrong tile)
- [ ] Fourth wall stays **0** spoken lines. Inner `Currently: On day 1 in Doubland…` leftover is **not this bet**
- [ ] Present-only / overlay — **not scored**
- [x] Lock log lines **0**; `[whitelist]` relocates **0**; Hobbs whitelist `['eat', 'social', 'serve', 'relax']` — no `study`
- [x] Seek did not wreck occupancy (`SOCIAL_SEEK:ARM` **8**, all morning hunt to `Alex Butcher`, last until_step **105**)
- [x] `CHAT_PRESERVES_ACTION` still default true (action/dest unchanged during full chat; **596** preserve lines)

---

### 8. After the score — next steps

**If leftover mints, Writer 2 stays keep, mill stays 0, occupancy holds**

Village KEEP is green. Then, **not** on a mixed occupancy bet:

1. Optional paperwork: stamp talk SOT (`sot_chats.md`) only after this score. Gather SOT Desired → Current still waiting on 1-B paperwork, not this tip.
2. Seek (everyday + multi, satiation) — own run; occupancy is now **that** bet.
3. Linger +10 if you still want it — own harvest; 2b never saw the helper.
4. WHO-gate / Option 2 hellos through CM — later.
5. Overlay spoken “you played / you were absent.”
6. Drain / continue, FE home-snap, RLS/disk.

**If mill returns (77-like Hobbs-both re-opens)**

**Stop.** Do not turn on seek. Do not lengthen linger. Do not restore the lock. Do not resume `20260831-1`, `20260831-2`, or this sim.

**If leftover mint is 0 after 4–6 Hobbs full sits, or remix past 12 lines**

**Stop.** KEEP missed. Do not hide it with occupancy. Do not add an appointment table on this tip.

**If new `session=` with no leave, same cafe**

Writer 2 is still live. **Stop.** Do not call KEEP a pass on poisoned ENDs.

**If occupancy dips below bar**

Split. Do not guess leftover honor vs seek vs linger. Do not restore the lock.

**If chat writes `vote.target`**

**Stop.** Ballot firewall failed.

**If the box is wrong (wrong tip, seek on, or `study` already on the cafe)**

Not a 2B score. Tear down and restart.

---

### 9. Start payload (already used)

```bash
curl -s -X POST http://127.0.0.1:8001/api/simulations/fork \
  -H "Content-Type: application/json" \
  -d '{"sim_code":"20260901-1","baseline":"soul15_seed_20260224","description":"Phase 2B leftover KEEP + Writer 2 desync restore","copy_memories":true,"copy_coords":false}'

curl -s -X POST http://127.0.0.1:8001/api/simulations/20260901-1/start \
  -H "Content-Type: application/json" \
  -d '{"action":"start","parameters":{"max_steps":2400,"generation_mode":"sprint","skip_premiere":true,"diagnostic_mode":false}}'
```

Log path on the box: `/var/www/generative_agents/api_gateway/logs/sims/20260901-1.log`

---

## Decision

**KEEP village-green. Stop `20260901-1` @619.** Do not run to 2285. Do not resume this PID.

Founder call (2026-09-02): KEEP’s village proof is first cafe sits, not two days. 20:00 / day-2 occupancy and evening mill **inherit `20260831-2`**. This tip does not walk bodies. Day-1 11:00 on **this** tip already held **14/15**.

| Item | Result | In this card |
|---|---|---|
| Preflight / tip `bb22747d` / skip-premiere | **PASS** | §0 all `[x]` |
| Ladder 1–8 | **1–4 scored.** 5–8 inherit 2b (not re-scored) | table |
| §1 hello mill Hobbs-both re-open in 30 | **PASS 0** through 619 (morning). Evening inherit 2b **0** | §1 |
| §2 Writer 2 `DESYNC REPAIR keep` / no sequel without leave | **PASS keep.** `desync_scratch_cleared` **0**. Watch: new `session=` after real `beat_budget` still at cafe — not 2b’s leak; parked | §2 |
| §3 leftover mint on full sits | **PASS.** 10 mints (`meet` 7 / `mutual_in` 2 / `avoid` 1). Cap 12 held. Hellos mint 0 | §3 |
| §4 honor/break co-presence | **PASS honor.** 10 honored / 0 broken. No leftover-driven walk | §4 |
| §5 `vote.target` firewall | **PASS 0** | §5 |
| §6 occupancy four fires | **Day-1 11:00 14/15 PASS** on this tip. 20:00 / day-2 **inherit 2b** | §6 |
| §7 persist / whitelist / seek | **PASS** through 619 | §7 |

**Parked (not a 2B fail):** new full-talk `session=` after sit-stop while still at cafe; `no_new_pressure` never logged (early no-class sits used `beat_budget` and minted 0); `LEFTOVER BROKEN` never seen; `alliance_commitments` scratch not dumped.

Do not resume `20260831-1`, `20260831-2`, or this sim. Stop PID **1091572** before the next deploy. Stamp talk SOT after this card. Do not turn seek on. Do not add `study` to the cafe list.

---

## Cheap falsification

Same pair, still at `the Ville:Hobbs Cafe:cafe`, a second `👋 GREETING TRIGGERED` inside **30** steps → **stop**.  
4–6 Hobbs full sits and `LEFTOVER MINTED` = **0**, or unique lines past **12** → **stop**.  
New `session=` with no leave, same 3-part arena → **stop**.  
Chat writes `vote.target` → **stop**.  
Occupancy dips below bar → **stop and split**.



==================================================================

# Phase 2B — leftover KEEP

**Date:** 2026-09-01  
**Status:** village-green on `20260901-1` @**619**. Tip `bb22747d`. Founder stopped; 20:00 / day-2 occupancy + evening mill inherit 2b. Card Decision above.  
**Repo:** `ivan-exsy/generative_agents`  
**Branch:** `ivan/talk-leftover-keep` from `ivan/pass-2-hello-quiet` @ `b8d1fc15`  
**Do not:** resume `20260831-1`, `20260831-2`, or `20260901-1`. Do not widen this closed tip (seek, linger, WHO, Option 2, occupancy/`study`). Talk SOT (`sot_chats.md`) may stamp now. Gather SOT Desired → Current is 1-B paperwork, not this tip.

This file is the locked product rules **and** the implement plan. Do not keep a second Phase 2B paper.

---

## Goal

A full talk (not a hello) mints one leftover on ConversationManager + memory in every simulation mode; the sit stops at mint or 6 beats; honor/break is co-presence only. `desync_scratch_cleared` is not a completed sit and not a sequel.

---

## Locked product rules (founder)

- Pipe is mode-agnostic. ConversationManager + memory. Every sim mode. SurvivalController does not own mint.
- Survival may **read** `protect` | `expose` | `mutual_in` into `alliance_commitments` after mint. Family / non-survival sits still mint `meet` | `tell` | `avoid`.
- Do not hang KEEP on `record_alliance_from_chat`.
- Do not invent a new appointment table.
- Leftover only on full talks, not hellos (`greeting_{step}_…` / hardcoded greeting).
- Sofa = one sit (same pair, same 3-part arena until leave). Hello mill must stay 0.
- Honor/break by co-presence only. Do not walk. Seek stays off.
- `CHAT_PRESERVES_ACTION` stays default true.
- Writer 2: 53/97 `CM.END` on `20260831-2` were `desync_scratch_cleared`. Fake END must not mint leftover and must not mint a new `session=` while both still in-room.
- Sit stop at first of: leftover mint; beat budget N=6 (hard cap 12 unique lines after that → 0); `no_new_pressure`; `left_arena`. 80-minute remix is the abort.
- Chat must never write `vote.target`.
- Occupancy is a hold, not this score.
- Normative SOT follows green score — leave `double-docs/sot/` untouched until then.

**Beat:** one unique `[speaker, text]` row on `session.chat_history`. Budget **N=6** to try mint. Hard cap **12** rows, then stop. No second dump after cap.

**Mint classes (heuristic, no new LLM):** `meet` | `tell` | `avoid` always. Survival may read `protect` | `expose` | `mutual_in` after mint. If no class matches, complete with `no_new_pressure` and **do not** fake a leftover.

**Complete reasons that may mint:** `commitment_mint` | `beat_budget` | `no_new_pressure` | `left_arena`.  
**Must not mint:** `desync_scratch_cleared` | `desync_participant_mismatch` | `pause_timeout` | `slicing_complete` | greeting / no CM session.

---

## Live facts (engine)

- Full talk is **one LLM dump** (`generate_batch_conversation`) then slice ~3 lines/step. 80-minute remix is **continuation** (`start_conversation` appends a new dump) plus **new `session=` after desync**, not one 240-line batch.
- `try_persist_or_linger` empties `chat_queue` on purpose. Emit then wipes `chatting_with` unless `keep_sit_partner` is true. Village still ended **53 / 97** as `desync_scratch_cleared` on `20260831-2`.
- `process_step` **ended** the session when `scratch.chatting_with` was empty. That was Writer 2. 2B restores the partner when the pair is still the same 3-part arena (`DESYNC REPAIR keep`).
- `record_alliance_from_chat` **ran** at full-talk start in `reverie.py` (wrong time, Survival-gated). 2B dropped that hook. Do not mint there.
- `reflect_on_interaction` writes a thin caption thought. Replace that structure at sit complete; do not add biography to the chat prompt.

---

## Context to read first

- `double-docs/sot/index.md`
- `double-docs/sot/sot_chats.md` — Current, lags code on trigger/batch/ids. Live ConversationManager + `reverie.py` bridge win when they disagree.
- `double-docs/sot/sot_memory.md` — thought writes. P3-1 `external_inbox` is user chat only; do not reuse for village leftover.
- `double-docs/sot/sot_survival.md` — Survival may read leftover; it does not own the pipe.
- `double-docs/sot/sot_observability.md` — log lines only.
- `20260901_chat_realism.md` §2.4 / §6 · `20260831-2_checklist.md` Decision · `20260901_launch.md` Phase 2B · `20260901-1_checklist.md`
- Live code: `conversation_manager.py` (start / end / `process_step` / `try_persist_or_linger`) · `leftover.py` · `reverie.py` greeting vs full bridge

---

## Constraints

- Follow existing patterns; no drive-by refactors.
- Feature-branch was `ivan/talk-leftover-keep`; `railway` fast-forwarded to `bb22747d` for the score. Do not rebase / force-push `railway`.
- Cite SOT when changing runtime contracts; do not edit SOT files on this tip.
- Replace weak structure (thin `reflect_on_interaction` caption); do not stack biography / OCEAN into every chat call.

---

## Work (chunks — shipped)

Stop condition during implement: chunk check fails → stop. Hard max: 6 chunks.

### Chunk 0 — branch
- Files: none
- Check: `git rev-parse --short HEAD` is `b8d1fc15` (or descendant of `ivan/pass-2-hello-quiet`). `sofa_greeting_quiet` still in tree.
- **Done.** Branch `ivan/talk-leftover-keep` from `b8d1fc15`.

### Chunk 1 — Writer 2: desync is not an end
Same pair, same 3-part arena, empty `chatting_with` → **restore** from the live session. Do not `end_conversation`. Do not mint a new `session=`.
- Files: `conversation_manager.py` · `tests/test_conversation_manager.py`
- Log `DESYNC REPAIR keep` (not `CM.END`). Left the arena may still end — **no leftover mint** on desync reason.
- Check: `python tests/test_conversation_manager.py TestDesyncDetection -q`
- **Done.**

### Chunk 2 — sit stop (beat budget)
A full sit cannot grow past 12 unique lines and cannot start a second dump after 6. Persist must not fight the cap.
- Files: `conversation_manager.py` · `reverie.py` (`slicing_complete` → `beat_budget` when cap hit) · `tests/test_conversation_manager.py`
- Log `SIT COMPLETE reason=beat_budget session=…`
- Check: `python tests/test_conversation_manager.py TestSitStop -q`
- **Done.**

### Chunk 3 — leftover mint (KEEP)
On allowed complete reasons, one leftover per **full** sit, written to **both** agents’ memory + CM open-list.
- Files: new `leftover.py` · `conversation_manager.py` (`end_conversation` calls mint) · `tests/test_leftover.py`
- Classify from the last ≤12 lines (keyword / time-place). No new chat prompt. No `record_alliance_from_chat`.
- `add_thought`: partner + class + one verbatim clause; keywords `leftover`, class, partner name.
- CM `_open_leftovers[pair_key]`; `due_step = minted_step + 60`.
- Log `LEFTOVER MINTED class=… pair=… session=…`
- Check: `python tests/test_leftover.py -q`
- **Done.**

### Chunk 4 — honor / break by co-presence
Open leftover + same 3-part arena → honored; due elapsed without co-presence → broken. No walk.
- Files: `leftover.py` · `conversation_manager.py` `process_step` · `tests/test_leftover.py`
- Log `LEFTOVER HONORED` / `LEFTOVER BROKEN`
- Check: `python tests/test_leftover.py TestHonorBreak -q`
- **Done.**

### Chunk 5 — Survival read + drop start hook
Survival may copy protect/expose/mutual_in into `alliance_commitments` **after mint**. Chat never writes `vote.target`.
- Files: `reverie.py` (no `record_alliance_from_chat` at `start_conversation`) · `leftover.py` · `tests/test_leftover.py` · `tests/test_survival_stage4_5.py`
- Check: `python tests/test_leftover.py TestSurvivalRead -q`
- **Done.**

### Chunk 6 — verify + PR
- `generative_agents/AGENTS.md` Recent Learnings one line. PR `ivan/talk-leftover-keep` (https://github.com/ivan-exsy/generative_agents/pull/4). SOT untouched.
- Check: leftover 14 OK; sit-stop / desync / sofa 14 OK; survival stage 4/5 23 passed.
- **Done.** Later: `railway` fast-forward + VPS deploy + skip-Premiere `20260901-1` (founder ask).

---

## Acceptance (village score)

- Full talk → ≥1 `LEFTOVER MINTED` with class in `meet` | `tell` | `avoid` (Survival may also tag `protect` | `expose` | `mutual_in`)
- Greeting-only sits → 0 leftovers
- `desync_scratch_cleared` → 0 leftovers and no new `session=` while both still in the same 3-part arena
- Sit stops with allowed reason; unique lines after beat 12 → 0
- No `LEFTOVER HONORED` without co-presence
- 0 chat → `vote.target`
- Seek still off; `CHAT_PRESERVES_ACTION` default true; `sofa_greeting_quiet` still present
- SOT files untouched until green

Score sim: **`20260901-1`**. Do not paste `20260831-2` numbers into that card.

| Signal | Abort |
|---|---|
| `LEFTOVER MINTED` ≥ 1 after 4–6 Hobbs full sits | 0 mints and remix past 12 lines |
| Hello mill Hobbs-both re-open in 30 = 0 | mill returns |
| `desync_scratch_cleared` not used as sequel | new `session=` no leave, same cafe |
| Occupancy 11:00 / 20:00 hold | dip → split, do not patch linger/seek |
| 0 chat → `vote.target` | debt writes ballot |

---

## Out of scope

Seek / path-to-person / linger +10. WHO-gate. WORDS prompt soup / Big Five. Option 2 hellos through ConversationManager. Occupancy / whitelist / lock / `study` on cafe. Overlay copy. Stamp `sot_chats.md` before green score. P3-1 `external_inbox` for village talk. New first-class appointment table.