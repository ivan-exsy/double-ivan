# 20260723-1 — Checklist: headless hygiene + self-serve epic (prod)

**Purpose:** On the next VPS score/sprint run, verify:

1. **No screenshots** on the hot path (default)  
2. **Chrome recycle every 500 successful headless renders**  
3. **Self-serve epic** live but dormant after `ivan/dev` → `railway` (viewer unchanged; worlds closed)  
4. **Reverie RSS creep** time series (`scripts/monitor_reverie_rss.sh`) — see `20260723_reverie_rss_creep.md`  
5. **Path persist (slice A)** — travel steps store usable `actual_path` in Supabase (`20260723_eng-request_path-animation.md`)  
6. **Path playback (slice B)** — live + CDN viewers tween along stored paths (FE; verify after their ship)  
7. **Carry-over from `20260720-1`** — finish human / open gates for natural movement + optional Light (§12)

**Context:** `20260720-1_RCA.md` (checklist + OOM) · `EPIC_self-serve-double.md` (D7) · `20260723_reverie_rss_creep.md` · `20260723_eng-request_path-animation.md`  
**Headless validation stays ON.** Hygiene/epic gates are primary; §12 closes unfinished action/location + Light items from `20260720-1`.  
**Posture:** No landing CTA; open a QA world only for optional §9.

**Score sim:** `20260723-1`  
**Prior score sim (carry-over):** `20260720-1` (analyzers run ~step 7532; human A/B still open)  
**BE deploy / commit:** `railway` @ `f80bf3d4` (VPS confirmed 2026-07-23 midflight)  
**FE deploy (if any):** `________` (slice B on `double-front` `local` only — §11b blocked until promote)  
**Signed-in account:** `________`  
**RSS CSV path:** `/tmp/reverie_rss_20260723-1.csv` → copied to: `________` (after stop)  
**Midflight note (2026-07-23 ~20:00 UTC):** step **30**, generating, backend alive; PNG **0** / no `rendered/`; Chrome **7**; host avail **~2633 MB**; RSS CSV **4** rows (reverie RSS 308→371 MB over ~10 min — observe only); recycle not due. Auto-watch loop armed (**15 min**, PID local shell).  
**Auto-watch:** Cursor session loop every **15 min** (~30 s/step ETA). State: `ops/20260723-1_watch_state.json`. On each tick: probe VPS → update this checklist at checkpoints (100 / 500 / 1000 / 7500) → **auto-stop sim on fatal** (stall ≥15m, backend dead, OOM hints, host avail &lt;200MB, PNG flood ≥50, Chrome pile-up ≥40).

> **Do not walk away after starting the sim without §10a.**  
> **Do not close the run without §10b** (copy CSV + one-paragraph read).

---

### 0. Before the run — env armed?

- [x] Deployed code includes screenshot gate + browser recycle (env alone is not enough if an old binary is still running) — VPS `railway` @ `f80bf3d4`
- [x] Screenshot default **off**: `HEADLESS_SCREENSHOT_EVERY_N=0` (or unset — code default `0`). No `HEADLESS_SCREENSHOTS` flag — **unset on VPS**
- [x] `HEADLESS_BROWSER_RECYCLE_EVERY_N=500` (or unset — default `500`; `0` disables) — **unset on VPS** (default 500)
- [x] Phaser loop on: `HEADLESS_MOVEMENT_ENABLED=true`
- [x] `diagnostic_mode=false` for long sprint — handoff + Survival sprint posture
- [x] BE includes self-serve stack (`/api/me/*`, bind, avatar, map-seed on join) — deploy smoke + midflight `GET /api/me/double/avatar` → 401
- [x] `CHAT_REQUIRE_AUTH` still **false**/unset; `ENABLE_ONBOARDING_HOST` left as-is — unset / `true` respectively
- [x] All public sims `self_serve_access=closed` — deploy smoke (joinable empty)
- [x] Shared Supabase migrations already applied (identity, post-chat, self-serve access/allowlist) — pre-run
- [x] Note: `SIM=20260723-1` · start step `0` (fresh fork) · target ≥ `7600`
- [x] **RSS monitor scripts** present on VPS after pull: `scripts/monitor_reverie_rss.sh`, `scripts/snapshot_sim_chrome_mem.sh`

```bash
grep -E 'HEADLESS_SCREENSHOT_EVERY_N|HEADLESS_BROWSER_RECYCLE|HEADLESS_MOVEMENT|ENABLE_HEADLESS|CHAT_REQUIRE_AUTH' \
  /var/www/generative_agents/.env.local
ls -l /var/www/generative_agents/scripts/monitor_reverie_rss.sh \
      /var/www/generative_agents/scripts/snapshot_sim_chrome_mem.sh
```

---

### 1. Screenshots stopped

**Pass:** `rendered/` does not grow with per-step PNGs.

- [x] Midflight (~100+ steps): no new `step-*.png` (unless `EVERY_N>0` on purpose) — **at step 24:** `rendered/` does **not exist**; storage ~1.1M total. Re-check ≥100 and at end.
- [ ] End of run: no unbounded `rendered/` growth (contrast: `20260720-1` ~269 MiB)

```bash
SIM=YYYYMMDD-N
ls -lt /var/www/generative_agents/environment/frontend_server/storage/$SIM/rendered 2>/dev/null | head
du -sh /var/www/generative_agents/environment/frontend_server/storage/$SIM/rendered 2>/dev/null
```

**Fail:** new PNG every step with default env → wrong code/flag on VPS.

---

### 2. Chrome recycle every 500 successful renders

**Pass:** recycle log after ~500 / ~1000 **successful headless completions** in this process; Chrome count not unbounded; steps keep advancing.

**Note:** Counter is **completions in the current process**, not sim step alone. Clean start from 0 ≈ steps 500/1000. Resets on process restart / mid-run resume / headless failures. Log `step=` = sim step at recycle time.

- [ ] ~500 completions: log shows `HEADLESS BROWSER RECYCLE … every=500`
- [ ] ~1000 completions (if run lasts): second recycle
- [ ] `backend_process_active=true` and `current_step` still climbing after recycle
- [ ] Chrome/Chromium count does not grow without bound (helpers OK; pile-up not)
- [ ] Post-recycle: movement reports / coords still update

```bash
curl -sk https://127.0.0.1:8001/api/simulations/$SIM/status/current \
  | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('current_step'),d.get('backend_process_active'),d.get('last_generated_at'))"

ps aux | grep -E '[c]hrome|[c]hromium' | wc -l

grep -R "HEADLESS BROWSER RECYCLE" \
  /var/www/generative_agents/logs/sims/$SIM.log \
  /var/www/generative_agents/api_gateway/logs/sims/$SIM.log 2>/dev/null | tail
```

**Fail:** no recycle after ~500 completions · Chrome pile-up · freeze / no movement after recycle.

---

### 3. Regression — validation still real

- [ ] Steps advance; coords in Supabase for new steps
- [ ] Not a silent “headless disabled” path
- [ ] Soft: multi-hour run without OOM kill of `double-api` (vs freeze at 6860 on `20260720-1`)

---

### 4. Viewer coexistence

- [ ] Gateway up after deploy
- [ ] Open score sim (or known prod sim) **without login** — playback/sprites normal
- [ ] Cast / Survival chat still works without forcing login (gate off)

---

### 5. Auth + ownership

- [ ] No JWT → `GET /api/me/double/avatar` (or `/double`) → **401**
- [ ] Signed in → `GET /api/me/double` resolves owned Double + host
- [ ] Soft: `GET /api/me/doubles` returns this user’s agent link

```bash
curl -sk https://127.0.0.1:8001/api/me/double/avatar -o /dev/null -w "%{http_code}\n"
# With Bearer $TOKEN:
curl -sk https://127.0.0.1:8001/api/me/double -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

---

### 6. Assess → Talk

*Skip if already proven for this account on prod and you’re only re-checking deploy.*

- [ ] Quiz OK (or profile already has Big-5)
- [ ] Interview / `prediction_ready` true for owned Double
- [ ] Talk: 1–2 messages (`/onboarding/talk` or `POST /api/me/double/chat`)
- [ ] End session → assessment stored, **no silent ISS apply**
- [ ] Soft: post-chat review confirm/dismiss works

---

### 7. Look (D8)

- [ ] `GET /api/me/double/avatar` returns catalog (+ current if set)
- [ ] **Change look** / `POST` `atlas_code` persists (sprite-manifest / active row)
- [ ] Soft: Double not stuck in `missing_personas`

---

### 8. Join World — dormant

- [ ] Signed-in `GET /api/me/double/bind/sims` → **`sims: []`**
- [ ] FE Join World empty state is OK (not an error)

```bash
curl -sk https://127.0.0.1:8001/api/me/double/bind/sims \
  -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

---

### 9. Optional punchline (demo only)

*Open one QA world deliberately, then close it. Skip for dormant D7.*

- [ ] Set one QA world `private` + allowlist (or `open`) briefly
- [ ] Look → Join → job/home → finalize
- [ ] Roster + job/home in scratch; atlas present; map coords seeded at latest step
- [ ] Set world back to **`closed`**

---

### 10. Reverie RSS creep — start monitor + check results

**Pass:** CSV from near step 0 → end (≥1500 steps preferred). Headless stays ON; observe-only.  
Details: `20260723_reverie_rss_creep.md`

#### 10a. START — within ~5 min of sim start (same VPS)

```bash
cd /var/www/generative_agents
tmux new -s rss
SIM=YYYYMMDD-N INTERVAL_SEC=300 bash scripts/monitor_reverie_rss.sh
# Ctrl-B D to detach
tail -f /tmp/reverie_rss_YYYYMMDD-N.csv
```

- [x] **Started** monitor with `SIM=20260723-1` (nohup PID **1547634**, not tmux — same outcome). **Late start:** first sample ~19:49 UTC (~7–8 min after sim ~19:42) — early gap noted for §10b read.
- [x] Confirmed CSV exists and is appending every ~5 min (header + growing rows) — `/tmp/reverie_rss_20260723-1.csv`; samples at 19:49 (step blank, reverie 308.4 / chrome 0.0) and 19:54 (step 24, reverie **344.5** / chrome **608.3**, host_avail **2659**, assoc_disk **15.0**, rendered blank)
- [x] Detached and left running for the **full** sprint — still alive midflight
- [ ] Soft mid-run: `SIM=… bash scripts/snapshot_sim_chrome_mem.sh` if Chrome looks weird

#### 10b. AFTER RUN — check results (before declaring hygiene/RSS done)

- [ ] Monitor still had rows through stop (or note last step in CSV)
- [ ] Copied `/tmp/reverie_rss_<SIM>.csv` off-box **or** into `storage/$SIM/analysis/`
- [ ] Filled **RSS CSV path** at top of this checklist
- [ ] One-paragraph read written below (or linked):
  - `reverie_rss_mb` vs step → linear / plateau / bands?
  - `chrome_rss_mb` flat or climbing?
  - `assoc_disk_mb` flat while RSS climbs (or tracking)?
  - `png_count` / `rendered_disk_mb` flat with screenshots off?

**RSS read (fill in):**  
`________`

**Fail:** forgot to start monitor · started late mid-run only · no CSV at end · no read after stop.

---

### 11. Path persist (slice A) — after deploy + new score run

**Pass:** travel steps have usable `actual_path` (or array `path`) in Supabase. Does **not** prove FE tween (slice B).

```bash
python scripts/analyze_travel_path_coverage.py YYYYMMDD-N --min-pct 95
```

- [ ] Deploy includes path-persist migration + gateway `actual_path` readback
- [ ] Headless ON for the new score sim (FE must emit `actual_path` in movement reports)
- [ ] Coverage script PASS (≥95% of travel steps)
- [ ] Spot-check one commute step in DB: `movement.actual_path` is a waypoint list (not null / missing)
- [ ] Soft: gateway step API returns `actual_path` for that step
- [ ] Regression only on `20260720-1`: TELEPORT still ~0 (no retroactive paths expected)

**Fail:** coverage below bar · `actual_path` still absent on moving steps after headless reports.

---

### 11b. Path playback (slice B) — after FE ship + same new score run

**FE status (2026-07-23):** Slice B shipped on `double-front` `local` — clamp keeps walked prefix in `actual_path`; replay won’t drop usable `path` when `actual_path` is `[]`; CDN maps multi-waypoint `actual_path` for tween. Scrub **drag** may snap (deliberate); continuous walk = **play / LIVE**.  
**Depends on:** §11 PASS (paths in DB) + this FE deploy on the score stack.  
**Pass:** agents **walk** along waypoints within a step on both viewer surfaces — not snap-only.

- [ ] FE `local` (or promoted) build with emit + replay harden is what the score run / viewers use
- [ ] **Live gateway / websocket viewer:** 2–3 dorm↔cafe / campus↔Hobbs walks look continuous (tween within step)
- [ ] **CDN / replay viewer:** same stored steps also animate (no strip of `actual_path` in bundle rebuild)
- [ ] Missing-path steps fall back honestly (snap OK) — no fake invented path
- [ ] Soft: scrub **drag** may teleport (documented); play/LIVE must walk

**Fail:** §11 green but both viewers still snap · CDN drops `actual_path` · live OK / CDN broken (or reverse).

**Note:** FE also reports `remaining_path` (clamp tail). BE still force-clears server continuation under intent-only today — **not required** to clear the 95% / walk-feel bar (FE re-A*s each step). Open a follow-up only if long routes look choppy across steps despite good `actual_path`.

---

### 12. Carry-over from `20260720-1` — not fully validated

**Source:** checklist in `20260720-1_RCA.md` §A–H.  
**On this new score sim** (preferred) **and/or** finish human gates on `20260720-1` where noted.  
**Already known from `20260720-1` analyzers (~7532 steps)** — do not re-treat as unknown:

| Counter | `20260720-1` result | vs baseline `20260717-1` |
|---------|---------------------|---------------------------|
| TELEPORT @ sr1 | **0** | ~1818 @ long/sr2 |
| Gap1 / Gap2 non-worker | **28 / 17** | 676 / 599 |
| Address divergence | **46** | 3154 |
| Piano false / real play | **297 / 73** | want ≈0 false |
| Wait-wrap / `<waiting>` | **0** | — |
| CHAT_MISSING_PAYLOAD (R8) | **0** | — |
| CHAT_COOLDOWN flags (R4) | **1578** (mostly cafe) | separate social policy, not movement fail |
| Coord jump max / step | **6 tiles** | clamp OK; viewer snap = empty `path`/`actual_path` |

**Path animation:** coords do not blink; viewer *looks* jumpy because stored `path` is null and `actual_path` is missing — close via **§11** (persist) then FE slice B (not this hygiene ship).

#### 12A. Walk feel / no blink (R3) — **P0 human**

Analyzer TELEPORT already strong on `20260720-1`; **human still required**.

- [ ] Viewer: 2–3 building changes (dorm↔cafe, campus↔Hobbs) — continuous multi-minute travel, not map hops  
- [ ] No arrive-one-tick / leave-next ping-pong on the same commute  
- [ ] Soft on **new** sim after §11 + **11b**: path tween visible (else snap-per-step until FE slice B)

#### 12B. Hobbs staff zones (R1) — **P0 human**

Gap1/2 counters crashed on `20260720-1`; **Max kitchen immersion still open**.

- [ ] Busy cafe / gather: crowd in **customer seating / public**, not kitchen invasion  
- [ ] Spot-check **Max Shoemaker** (and other non-workers) not living on cooking / behind counter  
- [ ] Soft: full house = shared seats OK, not musical-chairs teleports onto 4 chairs  
- [ ] Optional: re-run Gap1/2 on **this** score sim — expect still ≪ 676 / 599

#### 12C. Honest place language (R2) — **P0 soft**

Divergence near noise on `20260720-1`; residue fights still need eyes.

- [ ] Walking: `heading to` + `@` = presence (not already far destination)  
- [ ] Stationary: `@` and `address_label` agree  
- [ ] No clear place fights (e.g. work-at-Willows `@ Hobbs`) on spot-check steps

#### 12D. Piano only when playing (R6) — **P2 FAIL carry**

- [ ] Non-play acts **not** parked on `:piano` (was **297** false on `20260720-1`)  
- [ ] Real play still lands on piano (was OK)  
- [ ] Re-measure V2 on **new** sim after any affordance fix; bar ≈0 false

#### 12E. Prop / leaf stability (R7) — **P2**

- [ ] Classroom teach stays on podium/board for a stretch  
- [ ] Cafe coffee stays on a seat while task stable  
- [ ] Leaf may move when schedule/action changes

#### 12F. Analyzer truth (R4 / R5 / R8) — **P1**

- [ ] R8: no `(from description)` chat-shell flood on new sim (was **0** on `20260720-1`)  
- [ ] R5: Apartment-N noise — only wrong-home / foreign apartment matters (true-home suppress)  
- [ ] R4: if playback still feels deep-chat ping-pong, open **separate** social-policy task — **do not** ship gather pair-mute; cafe chatty is OK

#### 12G. Survival regression — **if this run is Survival**

- [ ] Gather **spatial_gate** still fires Day 2+ chal/vote  
- [ ] No sleep-stuck / sleeping-1440 / empty-tally elim regression  
- [ ] `BACKEND_INTENT_ONLY_PATH` still default  
- [ ] Wait-wrap / nested `<waiting>` still ~0  
- [ ] Workers still allowed on staff leaves in work area

#### 12H. Light soul carry-over (optional)

Only if this run is long Survival and product wants Light ship (`max_steps` ≥ ~7500–9000).

- [ ] Light soul reasons skim (Day 4+ IDs that ran)  
- [ ] Blind triad Ivan · Diana · Mike after `challenge_blind_choice` package  
- [ ] Attraction Day 4+ soft  
- [ ] Full fidelity / Light ship call only after above

**Skip 12H** if this sprint is hygiene/path-only and Light stays deferred.

---

### Verdict

| Gate | Result | Notes |
|------|--------|-------|
| **0 env / deploy** | PASS | `f80bf3d4` · movement on · screenshot/recycle unset→defaults · scripts on disk |
| **1 screenshots off** | WIP | no `rendered/` at step 24; confirm end-of-run |
| **2 recycle @ 500** | PENDING | need ~500 completions; sim log paths not under usual `logs/sims/` yet |
| **3 validation still on** | WIP | step 24 climbing · `backend_process_active=true` · chrome=7 |
| **4 viewer** | PASS* | deploy smoke; re-spot if needed |
| **5 auth / ownership** | PASS* | no-JWT avatar → 401 midflight |
| **6 assess → Talk** | SKIP* | proven at deploy; dormant check only |
| **7 look (D8)** | SKIP* | deploy smoke |
| **8 join dormant** | PASS* | deploy smoke joinable empty |
| **9 punchline (optional)** | SKIP | dormant D7 |
| **10a RSS monitor started** | PASS w/ gap | PID 1547634; ~8 min late start |
| **10b RSS CSV + read** | PENDING | after stop |
| **11 path persist coverage** | PENDING | after enough travel |
| **11b path playback (FE)** | BLOCKED | FE B on `local` only |
| **12A walk human** | PENDING | TELEPORT was 0 on `20260720-1` |
| **12B Hobbs human** | PENDING | Gap1/2 strong; Max kitchen open |
| **12C place copy** | PENDING | |
| **12D piano** | OPEN | FAIL residue 297 false |
| **12E sticky leaf** | PENDING | |
| **12F analyzer truth** | PENDING | R4≠movement fail |
| **12G Survival regression** | PENDING | this run is Survival |
| **12H Light** | SKIP | deferred unless product asks past ~7500 |
| **Ship hygiene?** | PENDING | Pass = 1+2+3 |
| **D7 / epic dormant?** | PASS* | Pass = 4–8 (9 optional) — deploy smoke |
| **RSS investigation?** | PENDING | Pass = 10a + 10b |
| **Path persist A?** | PENDING | Pass = 11 (new sim) |
| **Path playback B?** | BLOCKED | Pass = 11b (after FE) |
| **Natural movement ship?** | PENDING | Pass = 12A+12B+12C human (+ §11 + 11b for walk *feel*) |

**Hygiene ship:** screenshots off + recycle observed + headless still validating. Does not close `PM-INFRA-1` or Phase 5 browser-free.  
**Epic / D7 ship:** viewer unchanged + `/me` works + look OK + join list empty while worlds closed.  
**RSS ship:** 10a at start + 10b after stop (CSV + short read per `20260723_reverie_rss_creep.md` §5).  
**Path persist A:** §11 on the **new** score sim.  
**Path playback B:** §11b after FE ship — live + CDN tween on the same stored steps.  
**Natural movement:** finish §12A–C on new sim (or complete human on `20260720-1`); 12D–F polish; 12H optional.  
**Out of scope:** Landing CTA · W4 · D3/D4 · fixing RSS creep in this run · opening worlds for everyone · folding OOM cgroup/swap into path persist A.
