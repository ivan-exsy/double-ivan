# Before LeaderTalks MVP — 17 Sep (Watch honesty + owner Chat)

**Product MVP is LeaderTalks in Pittsburgh** (Telegram group; Downtown world). Breakfasts was a rehearsal, not the MVP. Village gather + talk is a closed engine gate, not the product MVP.

Talk unmute is Current (`sot_chats.md` §3c). Score tape: `double-docs/20260912_Breakfasts/20260915-3_TODOs.md`. Do not raise talk-chance. Do not restore linger. Do not walk-to-person (**PM-VIL-1**).

This cut is Watch honesty before we show LeaderTalks. Downtown map, daily video, scored PPG day, and the Telegram rollout live in `TODO_post_mvp.md` (**LeaderTalks** rows). Work after LeaderTalks is in that file’s **After LeaderTalks** table.

Breakfasts spec files (`BREAKFASTS-BOARD.md`, `MVP-breakfasts.md`, `MVP-breakfasts_ivan.md`, `MVP-0.1.md`) are rehearsal archive — moving to `done/`. Do not treat them as the live list.

## This cut

*Scored 2026-09-16 / 2026-09-17*

### Shipped (brief)

- **T-E8 caption vs body** — seek / person dest never drive the sticker; place walks still name the place. **PASS** `20260916-2` @89. SOT `sot_action-location.md` §5.4 Current. Plan: `t-e8_t-e5_pass_554c710a`. Ticket **PM-VIL-8**.
- **T-E5 lore** — owner and guest Watch Chat never invent a secret pair / in-game twin. Same plan as T-E8. Product “yes, I’m your Double” scored under **B1**.
- **PM-BOOT-1** — bodies at Start; sleep/wake stickers hold (`20260917-1`, `railway` `d4e5e226`). Same-room leftover dest is not rewritten into a walk. T-E7 body-vs-bed still open.
- **Leftover stay, slice 1** — emit-only `walking to {place}` → stay when already there (`railway` `2ef73e11`). **`20260917-2` @100:** `walking to Hobbs Cafe` on cafe tiles **0**; en-route walks still walking; Ivan sticker `sleeping`; person-name walks **0**. Residual: `walking from the dorm to Hobbs Cafe…` on Hobbs (**21** rows). Plan: `leftover_walk_sentence_aee42bd7` (extend matcher; do not mutate planner).
- **PM-AUTH-1 + B1** — Chat requires magic-link; Watch stays anonymous; owner = linked Double. Guest gate: **Email me a link** on open, chips/input off, Chat→`/login` skips Leave Site (www `e93f7e9`, map `8100533`). **www PASS 17 Sep:** guest login returned to Breakfasts Watch; send worked; owner asked “do you know who I am?” → *person I’m based on / creator*. Thread stamped to owner email of `Ipistsov+20260908`. Auto-open Chat tab **out**. Optional later: one thread across the magic-link retry. SOT Chat-require / B1 stamp when you want it Current.
- **Village-25 → account** — any `prediction_ready` (including `IPIP-BFM-25`) → `/account` (www `22f20b4`). Empty login still Welcome. Optional: Back to account on **main Gmail**.

*NEXT — open*

- **Leftover walk slice 2** — `from … to {place}` stay rewrite. Same plan `leftover_walk_sentence_aee42bd7`. Do not resume `20260917-2`.
- **LLM env-only** — spec locked below. Next non-sticker eng pass.
- **B2** — next Chat pass (spec locked below). Owner class already shipped.
- **First-sight hello** — watch list. Do not patch until you say go.
- **LeaderTalks play** — season spec, pick 15, operator log, daily video, B2B on Watch, drop to the alumni group.
- **T-E7** — body walks while sticker says sleeping. Out of sticker passes.

Do not resume `20260915-*` / `20260916-1` / `20260916-3` / `20260917-1` / `20260917-2`. Do not Join Breakfasts. Cap 6.

---

## Next pass (proposed)

**1 — Leftover `from … to` (recommended next eng).** Close the Watch walk lie testers still see. Same emit-only family as slice 1. Plan: `leftover_walk_sentence_aee42bd7`.

**Do:** treat `walking from … to {place}` as travel for arrived-stay. Sleep/wake hold. Real cross-place walks still walking. Do not fold T-E7. Do not invent sitting/brewing.

**Score:** Hobbs tiles must not keep `walking from the dorm to Hobbs Cafe`; Ivan sleeping; person dest never names a person; persist ≤6. New 4-person Start.

**2 — LLM env-only (after stickers, or in parallel if you want thinking/cost first).** Spec locked below. Not a Watch score. Same keys on the VPS after local green. Do not restart `api-gateway` mid-run.

**3 — B2 Chat (after LLM or instead if Chat depth is the demo).** Three talker classes. Do not reopen research. Owner B1 already PASS.

**Leave out:** T-E7, first-sight, Downtown / video / Telegram ops, auto-open Chat tab.

**Founder optional:** village-25 Back to account on main Gmail. Cap 6.

---

## Open todos

### LLM env-only (models + reasoning)
<plan - C:\Users\ipist\.cursor\plans\llm_env-only_0f40394d.plan.md>

**Status:** spec **locked** 17 Sep. **Implement next LLM pass.** Do not reopen the six-job thinking list.

**Want:** sim + Watch Chat pick **model and thinking only from env**. One thinking knob per tier (not per job). Missing env = hard fail, not a silent other model. After local is right, same keys on the VPS (do not restart `api-gateway` mid-run).

**Locked env (local):** A/B = `deepseek/deepseek-v4-flash-0731`; C = `deepseek/deepseek-v4.1-flash`. Thinking: A **off**, B **high**, C **high**. A and B are the same model — A vs B **is** thinking off vs on. Do not infer thinking from “flash” vs “pro” in the name. Dated OpenRouter slugs are passed through as written.

**Do (this pass):**

1. Replace hardcoded slugs and `none`/`high` with those env keys. Drop the six-job thinking allowlist. Drop missing-env fallbacks.
2. **Keep on B** (will think): daily plan, task split + repair, where they go, mid-day replan, full village talk; Survival vote / challenge / last words / immunity / trial / leader mark / director score + tie-break; leftover long planners (old task-split, new decomp schedule, reflection insight); Watch Chat (API uses B).
3. **Move to A** (stay off): talk summary (non-Survival — Survival twin is already A), keyword/convo → thought, whisper, after-talk planning thought + memo, agent-chat leftovers, old iterative chat line + next-line. Hourly schedule stays A (`HOURLY_SCHEDULE_FORCE_TIER_B=false`).
4. Drop dead B names with no live function (`generate_conversation`, `chat_with_persona`).
5. After code: copy the same env block onto the VPS. Cost tables may miss the dated slug (telemetry only).

**Accepted change vs today:** Survival votes and Watch Chat **start thinking**. Village full-talks already do.

Not a Watch sticker score. Cap 6.

### Leftover walk sentence

**Status:** slice 1 shipped (`2ef73e11`). `20260917-2` @100 **partial FAIL** on `walking from … to`. Plan: `leftover_walk_sentence_aee42bd7`.

**Want:** already at the leftover place → stay / sit / work line, not a walk sentence (including `from … to`).

**Do:** extend the travel matcher; new Start; do not resume `20260917-2`; do not fold T-E7.

Ticket leftover of **PM-VIL-8** / boot.

### First-sight hello

**Want:** first pass in the dorm this morning at least nods.

**Seen:** named rooms ~3 tiles apart stayed silent (`20260916-1` 0 chats). Current gate is same 3-part room (or 3 tiles on a street with **no** room). **`20260916-2`:** cafe hello Gosha↔Luba ~07:21. **Watch list, not a fail.** Sleeping person stays silent.

**Do:** keep watching. Do not patch until you say go. No mill, no walk-to-person.

Ticket **PM-VIL-10**.

### B2 — known peer / secrets / control

**Status:** research **done** (12 Sep). **Next Chat pass implements this.** Do not reopen the research questions.

**Want:** the Double’s role depends on **who** is talking. Owners feel in control without silent personality surgery. Other people may recognize them without getting a private life-chapter.

**Already shipped (owner class — T-E5 + B1):** www **PASS** 17 Sep. Owner Chat says they are that person’s Double (spoken: *based on / creator*) and keeps it. Still never invent a secret pair / in-game twin. After **PM-AUTH-1**, anyone else chatting this Double is not the owner.

**Locked solution (implement this, do not invent a new one):** `done/20260912_Breakfasts/20260914_Post_breakfasts_todos.md` §B1 **RQ-B1a–c** and §B2 **RQ-B2a–b**. Tester notes: `done/20260912_Breakfasts/20260912_breakfasts_run.md`.

**Do (thin Chat pass):**

1. **Three talker classes** in prompt + memory filter — not a vague “be careful” line.
   - **Owner** — full life notes + sim context; may ask to learn; still no clinical / third-party secrets.
   - **Known peer** (same sim / named acquaintance) — sounds like them + **sim-public** facts only (role, home, recent village acts). No owner-chat secrets, contact, employer, or GPS unless the owner marked that field **shareable**.
   - **Stranger** — manner + high-level aims; refuse personal biography.
2. Default every life-chapter field **private**. Owner opt-in per field (or coarse Public / Friends / Private).
3. **Recognizable** = manner (quiz bands) + 3–6 concrete facts the owner would endorse + recent sim acts. Not percentiles, diagnosis, or private owner-chat.
4. If the owner asks for something very unlike them: one check (“are you sure? this doesn’t sound like you”) → **this scene only** (temporary overlay, expires with the thread) **or** a real snapshot change via quiz retake / confirmed life-note edit. Never from a chat command.

**Leave out of this Chat pass:** parse chat → rewrite temperament / `innate`; post-chat “propose updates” review card (**S3** — after LeaderTalks).

Ticket **B2**.

### Enable Pittsburgh Maze as the primary arena for new sim

- Verify latest work on double-r3f and Nicolas's deliverables
- Ivan: fix walking passes - inside/outside (prevent zig-zag walking)

- Increase # homes to 25:
-- Ivan: assign, whole out new buildings
-- Nicolas: build interiors

### LeaderTalks play — folded from `TODO_VC_prep.md`

**Tune VC metrics to capture *pull* and *retention* - deliver on Andrew Chan expectations.**
**Come up with product improvements to promote target metrics**


June raise paper stays at `TODO_VC_prep.md` (archive). Open todos live here. Do not rebuild what already ships.

**Current (do not re-open):** waitlist `source` + B2B `interest_type` / `group_name`; landing CTA routing. Proof: `double-docs/sot/sot_api.md` §10. Hero signal is still inbound *“build one for my group”* plus completion/forwards — not vanity view count at ~15/300.

**Consent (live, not the June triage):** surprise the group, then **Claim double / Remove double** in chat; you handle it by hand. Locked in `TODO_mvp-release-gate.md`. `TODO_VC_prep.md` §5 brief-first is **not** the SOP.

**Still open**

| ID | Item | Notes |
|---|---|---|
| **PM-LTALK-2** | Season Spec before a tagged drop | Days, posting cadence, exact `tg-survival-d{N}` on every Telegram post. Without the tag, signups look like generic landing traffic. |
| **PM-LTALK-3** | Pick the 15; tone pass | Affectionate / flattering, not exposé. Claim/Remove path above. |
| **PM-LTALK-4** | Operator log during the season | Completion, in-group forwards, screenshot every inbound “build one for us” + which episode. Manual is enough. |
| **PM-BFST-4** | Daily 9:16 + 15s “your Double’s day” + end-card CTA | Already on `TODO_post_mvp.md`. Vertical + tracked link to Watch. |
| **PM-BFST-5** | B2B line on Watch | *Bring Doubland to your organization* + email. Same capture as landing. |
| **PM-LTALK-1** | Show it to the alumni group | The actual ~300 chat. Product pieces above; this is the drop. |

**Hold until numbers justify:** serialized cliffhangers, Survival “that’s so them” fidelity (**PM-VIL-2**), group influence / vote. Not this cut.
