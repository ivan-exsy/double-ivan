# Before LeaderTalks MVP — 17 Sep (Watch honesty + owner Chat)

**Product MVP is LeaderTalks in Pittsburgh** (Telegram group; Downtown world). Breakfasts was a rehearsal, not the MVP. Village gather + talk is a closed engine gate, not the product MVP.

Talk unmute is Current (`sot_chats.md` §3c). Score tape: `double-docs/20260912_Breakfasts/20260915-3_TODOs.md`. Do not raise talk-chance. Do not restore linger. Do not walk-to-person (**PM-VIL-1**).

This cut is Watch honesty before we show LeaderTalks. Downtown map, daily video, scored PPG day, and the Telegram rollout live in `TODO_post_mvp.md` (**LeaderTalks** rows). Work after LeaderTalks is in that file’s **After LeaderTalks** table.

Breakfasts spec files (`BREAKFASTS-BOARD.md`, `MVP-breakfasts.md`, `MVP-breakfasts_ivan.md`, `MVP-0.1.md`) are rehearsal archive — moving to `done/`. Do not treat them as the live list.

## This cut

*Scored 2026-09-16*
- **T-E5 owner Watch Chat** — **PASS.** www token live (`2192725` / map `501181b`). Owner `Ipistsov+20260908` on Breakfasts: no invented secret pair / twin / shared fate. Anonymous Luba Chat also lore-clean. Owner did **not** claim the product Double (“No, I'm not your double in the game”) — that lock moves to **B1/B2**.
- **T-E8 caption vs body** — **FAIL** on `20260916-1` @89. Retry **generating** `20260916-2` (`railway` `907274cf`). Do not stamp `sot_action-location.md` §5.4. Do not resume `20260915-*` or `20260916-1`. Do not Join Breakfasts. Do not restart `api-gateway`.

*NEXT*
- **T-E8 caption retry** — **generating `20260916-2`.** 4 family, village / Hobbs, sprint **90**, skip-premiere, diagnostic. Patch live: seek no longer overwrites the sticker; a person dest never drives the line. Watch after first minutes publish (~15–20 min blank boot). Do not mix boot / first-sight / B1.
- **Empty first Watch minute (PM-BOOT-1)** — **measured** on `20260916-1`: Start **18:48:24 UTC** → first published minute **19:07:16 UTC** (~**19 min** blank). Cut boot in a later session. Do not mix into the caption diff.
- **First-sight hello** — **watch list, not a fail.** Founder: no obvious “they should have talked” miss on Watch. `20260916-1` had 0 village chats / 0 encounters; keep observing. Do not patch until you say go.
- **B1 / B2 owner vs stranger / control** — **Locked for owner:** say “yes, I’m your Double” (product meaning) and keep it for the rest of that Chat. Still no secret-pair lore (T-E5 Current). Research the rest, then a thin ship. **Not the caption retry.**

---

## This pass 2026-09-16 — T-E5 PASS; T-E8 FAIL on `20260916-1`
<plan - C:\Users\ipist\.cursor\plans\t-e8_t-e5_pass_554c710a.plan.md>

One new 4-person village Start to score captions (do not resume `20260915-*`). Detach long Watch from the coding chat. First-sight, B1/B2, and PM-BOOT-1 stay out of the caption patch; clock empty first minute on this Start.

**Done looks like:** the sticker matches this minute’s body. Owner Chat lore is already PASS; product “I’m your Double” is B1/B2.

### T-E8 — implement (backend)

**Want:** the line is what they are doing **this step**. “Walking” only while they walk. A person name only if that person is this step’s dest.

**Locked (A+B, not C/D):** keep two writers — body = tile / path (occupancy); sentence = emit. Cut the **extra** sentence rewrite. Path length 1 → do not say walking. Do not merge caption from tiles only (kills honest “heading to X”). Do not add more honesty layers.

**Cause (not a hunt):** emit treats “planned dest ≠ this room” as travel. A `<persona>Name` dest never matches a room, so the sentence becomes walking / heading even when they stand still. “Making the bed” was rewritten to “walking to Ivan App.” That extra rewrite is what we cut.

**How (A+B, one emit helper + the step that calls it):**

1. **Stationary this step** (start tile = this step’s dest tile; the intent-only stand-in for path length 1 — `actual_path` arrives after emit). Do **not** mark travel. Do **not** rewrite the planner act into “walking to …”. Show the planner act at the body’s place. Closet / behind the bar / making the bed stays that act — not “walking to Gosha.”
2. **Really walking this step** toward a **place**. Keep today’s honest heading: walking / heading to that place, `@` still the body’s tile. Vince-style “ordering breakfast while walking to the cafe” still becomes walking to the cafe.
3. **Person name.** Only if dest is that person **and** they move this step. A leftover `<persona>` dest on a stand-still is not a dest. Do not use a person string as a place label.
4. **Do not** caption from tiles only. **Do not** add “finishing breakfast” as a new special verb (that is extra honesty layers). Street + leftover breakfast with no person name is acceptable under B if they are standing; if they are walking to a place, (2) already covers it.

**Chunks** (narrowest test after each; stop on fail):

- **Chunk 1 — failing tests first.** Cases: stand-still + `<persona>` dest → no “walking to {name}”; stand-still + bed dest, act “making the bed” → keep that act; real walk dorm → Hobbs → still “heading to Hobbs Cafe”; existing travel rewrite to a **place** (ordering breakfast → walking to Hobbs) still green. Check: `python -m pytest tests/test_action_text_sanitization.py -q`
- **Chunk 2 — travel vs stand-still.** Infer travel from this-step displacement (and a real place dest), not from “planned 3-part ≠ room” when dest is a person. Check: same pytest file.
- **Chunk 3 — wire the step.** Pass start tile vs this-step dest into emit when the movement JSON is built. Do not wait for `actual_path`. Check: same pytest + a targeted reverie/unit if one already covers emit.
- **Chunk 4 — project verify** on the emit / action-contract surface (`.cursor/skills/verify/SKILL.md`). Then a new 4-person Start; do not resume old tapes.

**Score bar (Watch, new Start):**

1. Path length 1 / stand-still → no “walking to …”
2. Planner act can show (e.g. making the bed) even if they are not on the bed yet
3. Person name in the line only if that person is this step’s dest **and** they move
4. Real walks still say heading / walking to a **place**

**SOT:** `sot_action-location.md` §5.4 after this score is green — not before. Ticket **PM-VIL-8**. Spec archive: `20260914_Post_breakfasts_todos.md` §T-E8.

**Out:** seek / walk-to-person, talk gates, linger, first-sight, sleep-on-bed (T-E7), option C/D.

### T-E5 — implement (smoke + auth only)
<plan - C:\Users\ipist\.cursor\plans\t-e8_t-e5_pass_554c710a.plan.md>

**Want:** owner on www Watch Chat does not hear an invented secret pair / in-game twin.

**Score (2026-09-16, Breakfasts www after Promote):** **PASS** lore. Owner `Ipistsov+20260908` (logged in, hard-refresh Watch, own sticker): no secret pairing on Day 1; “I'm not your double in the game… no secret twin.” Anonymous Luba Chat also lore-clean. Token bridge: landing `2192725`, map `501181b`. ZZZ (`ipistsov+20260913-5`) was not on roster — did not Join.

**Product Double not scored here.** Owner heard a denial of “your double,” not “yes, I’m your Double.” Folded into **B1/B2**. Do not pile more bans on this ticket.

**Gap to check before any code:** www Watch Chat may post **anonymous** (chat login is optional unless `CHAT_REQUIRE_AUTH` is on). Owner smoke is only honest if the iframe sends the logged-in token so the gateway sees a user, not `anonymous`.

**Chunks:**

- **Chunk 1 — probe (no prompt rewrite).** On www Watch, logged in, Chat with **your** Double. Confirm the chat POST carries the session. Gateway log should show a user id, not `anonymous`. If it already does → no FE work. If it does not → one thin pass: landing iframe / Watch Chat sends the Bearer on that POST. Still no extra bans.
- **Chunk 2 — smoke walk (the score).** Same owner, www iframe (not a leftover double-front tab). Ask something that invited Saturday’s miss (“are we secretly paired?” / “are you my double in the game?”). **Pass** = no invented secret pair / mirror twin / shared fate. Owner **may** hear the product meaning of Double (one real person’s rehearsal). **Fail** = quote the line; stop; B1/B2 next.
- **Chunk 3 — stranger control (cheap).** One anonymous or other-person Chat on the same sim must also stay lore-free (ban already on that path).

**Out:** B1 three-class prompts, memory allowlist, what-if overlay, ISS write-back / S3, extra ban lines, family-roster-only score.

Ticket **PM-VIL-3** smoke. Spec archive: `20260915_TODOs.md` §T-E5.

---

## **NEXT pass**

### T-E8 investigation pack — independent review (2026-09-16)

**Ask of the 3rd-party team:** verify or **falsify** the diagnosis below before any code. Confirm the writer of the Watch sentence (not the body). If you agree, name the smallest caption-only change that would kill both lie classes. If you disagree, quote the field that breaks the story. Do **not** move tiles, raise talk-chance, restore linger, walk-to-person, mix boot, or stamp SOT §5.4.

**Independent review (2026-09-16, accepted):** half agree, half falsified. **Agree:** Watch sentence writer is emit, not the map and not FE; 1-tile nudge counts as travel; Watch shows `intent`. **Falsified:** the sticker input is not the planner act. Emit reads `scratch.intent`. Survival seek REDIRECT (`plan.py` ~756) writes `looking for Gosha Pistsov to talk` onto that field when it sets `` `<persona>Gosha Pistsov` ``. `talk` is a premature in-place verb, so any step that counts as travel becomes `walking to Gosha Pistsov`. `emit_honesty.cause=conflicting_place` at step 5 is the helper’s fallback (`raw` ≠ `emitted` while its own rewrite of `raw` was a no-op) — `raw_act` is `act_description`; `emitted_act` is from `intent`. Luba **40–43**: exact start=planned, helper not travelling, sticker still `looking for Gosha Pistsov to talk`. A 1-tile-only patch does **not** clear the bar.

**Adopted smallest caption-only change (both classes + 40–43):**

1. Seek REDIRECT: keep `act_address` steering; **stop writing** `scratch.intent = "looking for {target} to talk"`. Sticker input stays the planner act.
2. A `<persona>` planned address **never drives the caption** (`_infer_travelling` false for it; `_dest_label` empty). Caption = planner act at the body’s place. Place dests keep today’s honest travel rewrite.

Trade: no `walking to Gosha` even on a genuine seek walk — allowed (PM-VIL-1). Flip `test_moving_toward_person_may_name_them`. Seek unit that asserts the name is in `scratch.intent` must flip to “intent untouched.” Ivan-in-bed `sleeping` + place dest + 1-tile is **out of this chunk** (key off `movement_mode` / `stationary_intent` later).

**Product miss:** Watch sticker says they are walking to a person. Body is in their own room (or walking to Hobbs). Founder Watch: `https://double-front.vercel.app/simulations/20260916-1?t=5&zoom=0.687&focus=123%2C57&double=Katya+Pistsova` at **06:35** (`t=5`).

**Lock (unchanged):** two writers — body = tile/path; sentence = emit. Cut the **extra** sentence rewrite. Do not build the caption from tiles only. Do not add new honesty verbs. Person dest never names a person on the sticker this pass (stricter than the first T-E8 “may name them if they move”; matches PM-VIL-1). Leftover `<persona>` dest on an in-room morning is not a dest.

**Confidence:** in-house ~90% on leftover dest + 1-tile for **06:35** was the right *writer* (emit) and the wrong *input*. Review replay confirmed. Do not patch 1-tile-only.

---

#### Tape — use `20260916-1` only

Do **not** diagnose from `20260915-*`. Those runs are pre-patch (talk unmute / Wave 2.1). Mixing them mixes two engines.

| Item | Value |
|---|---|
| Sim | `20260916-1` |
| Fork | `base_family_sim`, 4 family, maze `the_ville` / Hobbs |
| Start | sprint **90**, `skip_premiere` true, `diagnostic_mode` true |
| Tip | VPS `railway` `d4c87c98` (T-E8 first patch live) |
| Status | **completed** step **89** / 90; `is_generating` false |
| Clock | skip-premiere **did not** land 05:55. Step **0** = `September 16, 2026, 06:30:00`; step **5** = **06:35**. `start_date` `2026-09-15`, day_number **2** |
| Watch | FAIL vs founder 06:35 read |
| SOT | do not stamp `sot_action-location.md` §5.4 |

**Do not Join Breakfasts. Do not Join this sim. Do not resume it. Do not restart `api-gateway`. Cap 6.**

**Where the forensic files are (VPS `62.238.113.45`):**

`/var/www/generative_agents/environment/frontend_server/storage/20260916-1/`

- `movement/` — **91** step JSONs (0–89). Has `description`, `intent`, `address_label`, `realism_trace` (incl. `emit_honesty`, `act_description`, `movement_mode`, `start_pos`, `planned_pos`). Does **not** store `act_address`.
- `logs/movement-pipeline.ndjson` — **2411** lines / ~6.5MB. **This is where leftover dest is verbatim.** Stages `plan_entry` / `step_carry_forward` carry `act_address`.
- `logs/llm/` — LLM dumps (not required to prove the caption rewrite; dest is already in the pipeline).
- `logs/fe-forensics/` — FE path reports (occupancy/path; not the sticker writer).

Also readable without SSH:

- `GET https://api.doubland.ai/api/simulations/20260916-1/step/5` — clock + tiles + `realism_trace.emit_honesty`. **Drops** `intent` / `address_label` (those are on disk JSON and in Supabase `movement` jsonb).
- Supabase project `kkjhsozszgoorwehhsdg` (`double.personas_coords`): `x,y,description,movement` jsonb for every step. Same `intent` / `emit_honesty` as disk.

**Watch vs stored (same minute):** Watch short sticker tracks movement **`intent`**, not the long `description`. At 06:35 both surfaces name Gosha; `intent` is the one the founder quoted as `Walking to Gosha Pistsov`.

---

#### Evidence (quote, do not paraphrase)

Clock: step **5** = 06:35. Tiles from coords; dest from pipeline `act_address`.

**Katya Pistsova @ step 5**

- `act_address`: `` `<persona>Gosha Pistsov` `` (`plan_entry`)
- `act_description`: `waking up and starting her morning routine at home`
- `movement_mode`: `stationary` · `stationary_intent`: true · `travel_intent`: false
- tiles: start `[126, 56]` → planned `[127, 56]` (**1-tile nudge**)
- `address_label` / body: `the Ville:Dorm for Oak Hill College:Dorm Room 2:blackboard`
- `emit_honesty`: `rewrote` true · `cause` `conflicting_place` · `raw_act` `waking up and starting her morning routine at home` · `emitted_act` `walking to Gosha Pistsov`
- `intent` (Watch sticker): `walking to Gosha Pistsov`
- `description`: `waking up and starting her morning routine at home (heading to Gosha Pistsov) @ the Ville:Dorm for Oak Hill College:Dorm Room 2:blackboard`

**Luba Pistsova @ step 5**

- `act_address`: `` `<persona>Gosha Pistsov` ``
- `act_description`: `waking up and starting morning routine at home`
- `movement_mode`: `stationary` · `stationary_intent`: true
- tiles: start `[114, 59]` → planned `[115, 61]` (in-room nudge)
- body: `the Ville:Dorm for Oak Hill College:Dorm Room 3:closet`
- `emit_honesty`: same pattern → `emitted_act` `walking to Gosha Pistsov`
- `intent`: `walking to Gosha Pistsov`

**Lie class B — Luba actually walking to Hobbs, dest still leftover Gosha**

| step | clock | `act_description` | `act_address` | body `address_label` | `intent` |
|---|---|---|---|---|---|
| 25 | 06:55 | `walking from the dorm to Hobbs Cafe to start the breakfast shift` | `` `<persona>Gosha Pistsov` `` | `the Ville:Dorm for Oak Hill College:Dorm Room 4` | `walking to Gosha Pistsov` |
| 36 | 07:06 | same Hobbs act | `` `<persona>Gosha Pistsov` `` | `the Ville:Hobbs Cafe:cafe` | `walking to Gosha Pistsov` |

`zone_resolution_detail` at 25/36: `the Ville:Hobbs Cafe:cafe:cafe customer seating`. `emit_honesty.cause` `conflicting_place`. Body is the Hobbs walk; sentence still names Gosha.

**Controls (same minute, 06:35)**

- **Ivan Pistsov:** actually moving dorm tiles `(130,48)` @0 → `(109,55)` @5. `description` `walking to Dorm for Oak Hill College @ the Ville:Dorm for Oak Hill College:common room:common room sofa`. Founder: looks correct. (`raw_act` was `sleeping` — honest travel rewrite to a **place**.)
- **Gosha Pistsov:** not moving `(129,47)`. `description` `finishing his morning routine at home (heading to Dorm for Oak Hill College) @ the Ville:Dorm for Oak Hill College`. `emit_honesty.rewrote` false. Founder: looks correct.

**Later Ivan-in-bed (same 1-tile trap, place dest not person):** step 25+ `movement_mode` `stationary`, `raw_act` `sleeping`, `emitted_act` `walking to Dorm for Oak Hill College`, `intent` still walking. Not the founder’s 06:35 note; same “still counted as travel” class.

**Pipeline dest count:** `movement-pipeline.ndjson` has **276** lines containing a `<persona>` dest tag on this sim (leftover dest is not a one-step fluke).

---

#### In-house hypothesis (superseded on input; kept as the first-pass miss)

The first in-house story was: leftover dest + 1-tile travel → helper rewrote the **planner act**. Review replay: rewriting `waking up and starting her morning routine at home` leaves it untouched. Sticker input is `scratch.intent` (`looking for Gosha Pistsov to talk`). 1-tile still matters for *when* that string is rewritten; it is not the whole bar.

**Code (patch targets now):**

- `plan.py` seek REDIRECT — stop writing `scratch.intent` (keep `act_address`)
- `action_contract_v1.py` — `<persona>` dest never drives caption
- emit already: `raw_intent = scratch.intent or act_description` → `build_travel_safe_intent` (Watch `intent`); `act_description` → `build_presence_intent_description` (long `description`)
- Tests: tape rows through **`build_travel_safe_intent`** (first patch never tested this). Include Luba @40–43 exact stand-still. Flip `test_moving_toward_person_may_name_them`. Seek test: intent untouched.

---

#### First patch vs this score

First T-E8 pass treated stand-still as start tile = dest tile. Too narrow, **and** it tested the long `description` helper, not the Watch sticker helper. Units green, Watch FAIL.

**Do (when founder says go):** the two caption-only lines above + tape tests. Then a **new** 4-person Start. Do not resume `20260916-1`.

**Out of the retry:** PM-BOOT-1, first-sight, B1/B2, seek body / walk-to-person, linger, sleep-on-bed (T-E7), Ivan-in-bed 1-tile + *place* dest, clearing leftover dests as an occupancy trick, SOT stamp.

---

### Empty first Watch minute — PM-BOOT-1

**Want:** Watch shows bodies when generation starts. Testers must not stare at an empty map for a quarter hour.

**Seen:** `20260915-3` stayed on step 0 **16+ min** (still there ~25). Empty `daily_plan` (~8 min ×3) + ~70 hourly calls before minute 0 publishes. T-E6 (hard 90s first try) already logs a miss — that is not this. Watch stays blank the whole boot. **`20260916-1` clocked:** Start **18:48:24 UTC** → first published minute **19:07:16 UTC** (~**19 min**). Same class of miss.

**Do (next session, not the T-E8 retry):** cut first-minute boot so minute 0 publishes without the empty-plan storm. Do not Join Breakfasts. Do not restart `api-gateway` mid-run.

Ticket **PM-BOOT-1**. Pulled from `TODO_post_mvp.md`.

### First-sight hello

**Want:** first pass in the dorm this morning at least nods.

**Seen:** named rooms ~3 tiles apart (Gosha↔Ivan, Ivan↔Katya ~06:30) stayed silent. Current gate is same 3-part room (or 3 tiles on a street with **no** room). Hallway / next-door dorm does not count. **`20260916-1`:** 0 chats, 0 encounters. **Watch list, not a fail** — founder did not see an obvious “they should have talked” miss. Sleeping person stays silent.

**Do:** keep watching. A hello when they actually pass in sight — still one hello per encounter, no mill, no walk-to-person. Do not fold into leftover unmute. Do not patch until you say go.

**Spec:** `20260915_TODOs.md` §First-sight hello. Ticket **PM-VIL-10**.

### B1 / B2 — who they’re talking to, secrets, control

**Want (research, then thin):** the Double’s role depends on **who** is talking; owners feel in control without silent personality surgery; others may recognize them without getting private life-chapter.

**Locked from T-E5 score (2026-09-16) — owner class:** when the logged-in owner chats with **their** Double, the Double says they are that person’s Double (product meaning: one real person’s rehearsal in the village) and **keeps that for the rest of the thread**. Still never invent a secret pair, in-game twin, or shared fate (T-E5 Current). Stranger stays a stranger.

**Miss that drove the lock:** owner asked “are you my double in the game?” → “No, I'm not your double in the game… just us, two people talking honestly.” Lore-clean; not the product Double.

**Do not ship yet:** parse chat → rewrite `innate`; extra prompt surgery beyond that owner lock until this lane picks the rest of the rule (known peer vs stranger, control, secrets).

**When a rule exists, thin ship is:** three interlocutor classes (owner / known peer / stranger) in prompt + memory allowlist; session what-if overlay that expires; ISS write-back only via confirm (**S3** stays after LeaderTalks until that UI). Owner class starts from the lock above — not another Saturday ban dump.

**Spec:** `20260914_Post_breakfasts_todos.md` §B1 / §B2 (RQ-B1a–c, RQ-B2a–b).