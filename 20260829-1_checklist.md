# 20260829-1 — Checklist: 1-B score

**Purpose:** Score the **whitelist-veto deletion** on a new skip-Premiere fork. Same bar as 1-A: tiles at **11:00** and **20:00**, competitive day. Do **not** resume `20260828-1`. Do **not** edit `maze_registry.json` on this tip.

**Prior:** `20260828-1` 1-A **FAIL occupancy** — competitive 11:00 **6/14**, day-1 **10/15**, vote **9/15** (14 at 20:15). Honest text **Pass**. Cause: cafe list omits `study` / `play` / `work`, so a correct Hobbs address was relocated. Paper: `20260827_gathering_issue_RCA+recommendations.md` **§12**.

**This tip:** `railway` @ **`273e1f55`** (veto delete `2dea146e` + memory note + deploy-skill fix). 1-A tip was `acf744b8`.

**Score sim:** `20260829-1` (stamp the real id if the fork uses another).  
**Match 1-A exactly:** `soul15_seed_20260224` · `copy_memories=true` · `copy_coords=false` · sprint · **`skip_premiere: true`** · `max_steps: 2400` · **`diagnostic_mode: false`** · `HEADLESS_TAB_REUSE=false` · `TASK_DECOMP_CONTEXTUAL_ENABLED=true`

**Diagnostic stays off.** Occupancy, leave destinations, `action_family`, and resolver tags are already on disk without it. Diagnostic writes movement JSON + LLM capture + FE forensics and filled the box last time we left it on. Turn it on only if this run’s stdout still cannot answer “did search fire?” after the PID is gone — that is a follow-up, not this verdict.

**Score at tiles, not labels.** Day 1 **/15**; day 2 **/14**. Bar **12** then **11**.

**Not this pass:** sit-only `study` widen (32 rooms), `eat`/`work`/`play` list edits, sofa, seek, Shepard’s sleep-over-appointment, day-number restamp. Those wait.

**Do not paste unverified leftovers from the archived score cards as extra 1-B gates.** Those files live in `done/`. Carry only a **regression watch** (already §3). Written-off: 27-1 and 28-1 day-2 votes. Sofa stays second pass. Post-run harvest is **§6**, not a live gate.

**Out (unchanged):** H3 dest-rewrite, fail-closed, longer pins, fire-when-12, restore the lock.

---

## What you would miss if you only did stop → pull → start

1. **Confirm the box is on `273e1f55` after pull**, before fork. A start off `acf744b8` is a second 1-A.
2. **Confirm the cafe list is still** `` `["eat", "social", "serve", "relax"]` `` — no `study` add. Mixing the widen in makes the four-room test useless.
3. **Disable `apt-daily-upgrade` auto-restart of `api-gateway`.** Killed `20260822-1`.
4. **Do not score day-2 vote on `20260828-1` first.** We already have the cause. Stopping it is not a data loss.
5. **First read is where leavers go**, not the 11:00 count. If they still pile into the four `study` rooms, stop — the diagnosis is wrong and more steps will not save it.

---

### When to look — checkpoint ladder

Step 0 = day-2 05:55. One step = one minute. Pace ~50–80 steps/h. Track the **step**, not the wall clock.

| # | Step | Clock | What you read | If it's wrong |
|---|---|---|---|---|
| 1 | **~5** | 06:00 d1 | Cafe block still covers **10:00–11:00** (1-A invitation still in). Zero “premiere day” / “full cohort of fifteen” | **PASS** at step 39. Hard abort only if invitation or premiere string had regressed. Stuck “On day 1” is fine |
| 2 | **~60** | 07:00 d1 | No crash. Persist over-6 = **0** | **Hard abort** — deploy/health, not gather |
| 3 | **240–305** | 10:00–11:00 d1 | **Leave destinations** for anyone who sat then left, then the 11:00 count (bar 12/15) | If leavers still land in classroom / library / Apartment 4 / Dorm Room 2, **diagnosis failed — stop**. Day-1 occupancy can still miss the bar; read the dests first |
| 4 | **785–845** | 19:00–20:00 d1 | Vote dests + 20:00 count (bar 12/15) | Same dest test. Late surge (full at 20:15) without dests in the four rooms = invitation/shape, not veto |
| 5 | **~1445** | 05:55 d2 | Headcount **14**. Premiere string still gone. Day number may still say “day 1” — **known, not a 1-B fail** | Abort only if premiere / “fifteen” returns |
| 6 | **1745** | 11:00 d2 | **The number.** Bar **11/14**. Dest signature again | This is the gate. Shepard **asleep** is an accepted leftover (one body) |
| 7 | **2285** | 20:00 d2 | Day-2 vote, bar **11/14**. Then **stop** | Green needs 6 **and** 7. Do not run to 4000 |

**Three places to walk away:** step 5 (1-A regress), step 60 (box sick), step 305 (four-room dests still firing).

**Live read — CP1 passed at step 39** (2026-08-29 ~21:47 UTC). 15/15 hourly names Hobbs for 10:00–11:00; 15/15 invitation + vote still in the day brief; 0/15 “premiere day” / “full cohort of fifteen”. `currently` still “On day 1” / 15 players (expected). Shepard’s 10:00 hourly is Hobbs this run, not `asleep`. Diana’s 11:00 hourly is `eating lunch at Hobbs Cafe` (still Hobbs). Persist over-6 so far **0**. Next real read: CP2 ~step 60, then dests at 10:00.

---

### 0. Preflight — **done 2026-08-29 ~21:30 UTC**

- [x] `20260828-1` **stopped** at step **2092** (~16:47 day 2). PID `663491` gone. Do not resume.
- [x] VPS pulled to **`273e1f55`** (was `acf744b8`). Restored a local docstring-only dirty `utils.py` so pull could fast-forward.
- [x] `api-gateway` restarted; `/docs` 200
- [x] `apt-daily-upgrade.timer` already **disabled** / inactive
- [x] Env: `HEADLESS_TAB_REUSE=false` · `TASK_DECOMP_CONTEXTUAL_ENABLED=true` · `MAX_TILES_PER_STEP=6` · `SURVIVAL_MODE_ENABLED=true` · `HEADLESS_MOVEMENT_ENABLED=true`
- [x] Hobbs cafe whitelist `` `['eat', 'social', 'serve', 'relax']` `` — no `study`
- [x] Fork + start `20260829-1`: sprint, skip-premiere, 2400, diagnostic **false**
- [x] Clock jumped `2026-08-29 06:30` → **`2026-08-30 05:55`** (engine day 2); `start_date` unchanged; step **0**. Status API may still say Premiere until the first step lands in Supabase — trust the runner log.

**Record:** fork `2026-08-29T21:30:51Z` · UUID `dde796c5-5839-49d0-a48f-160eab41b0d7` · maze `49b8fd0e-4ad3-431e-9f0b-fab9cb571df8` · PID **`808722`** · tip `273e1f55` · log `api_gateway/logs/sims/20260829-1.log`

---

### Clock map (same as 1-A)

| Event | Clock | Step |
|---|---|---|
| Day-1 challenge | 11:00 | **305** |
| Day-1 vote | 20:00 | **845** |
| Day-2 05:55 | — | **1440** |
| Day-2 challenge | 11:00 | **1745** |
| Day-2 vote | 20:00 | **2285** |
| Day-3 replan overwrites day-2 | midnight | 2525 |

**Minimum useful run: 2285.** Then stop.

---

### 1. Did the veto deletion take effect? — read dests before occupancy

Quote paths verbatim. Do not shorten to “college / pub / dorm.”

The **four `study` rooms** (diagnosis fails if leavers still cluster here):

- `the Ville:Oak Hill College:classroom`
- `the Ville:Oak Hill College:library`
- `the Ville:Apartment 4:main room`
- `the Ville:Dorm for Oak Hill College:Dorm Room 2`

| # | Signal | Pass? |
|---|---|---|
| 1 | New leave-writes during 10:00–11:00 are **not** concentrated in those four rooms | [ ] |
| 2 | Cafe→pub tagged `play`, and cafe→home tagged `work`, **drop** vs 1-A (105 / 47+44). Veto also explained those — cafe list has neither `play` nor `work` | [ ] |
| 3 | Inherited / planner rows that name Hobbs in the hourly **keep** a Hobbs dest when `action_family` is `study` | [ ] |
| 4 | No `POST-VALIDATE` / whitelist-cascade reason on those rows (reason `whitelist` should be gone) | [ ] |

**Expected leftovers (not a 1-B fail):**

- Shepard `asleep` across 10:00–11:00
- Cafe → pub tagged `relax` or `eat` (cafe already allows both)
- Dean → Harvey / Owen → Willows **job** snaps
- `currently` still saying “On day 1” on day 2 (headcount may still update)

---

### 2. Occupancy (the gate)

| Fire | Bar | Result |
|---|---|---|
| Day-1 challenge 11:00 | 12/15 | |
| Day-1 vote 20:00 | 12/15 | |
| Day-2 challenge 11:00 | **11/14** | |
| Day-2 vote 20:00 | 11/14 | |

Curve at 10:00 / 10:15 / 10:30 / 11:00 — still write it. Shape should keep moving (no lock freeze). A 10:15 peak then a drop is only a fail if dests are the four rooms again.

**Pass 1-B** = dest signature **and** both competitive fires (day-2 challenge + day-2 vote) at bar. Day-1 can still be thin; it is not the headline.

---

### 3. Hold from 1-A (regression watch)

- [ ] Honest caption: no standing-still “heading to Hobbs” while the body is elsewhere
- [ ] Off-cafe ritual verbs ≈ 0
- [ ] Start-jump persist over-6 = 0
- [ ] Present-only / overlay still matches tiles
- [ ] Lock log lines still **0**

---

### 4. After the score — next steps

- [ ] **SOT after a green 1-B** — only once dests + day-2 occupancy pass. Promote Desired → Current in `double-docs/sot/sot_action-location.md` (whitelist is search, not a relocating gate; soften §3.5 so it is not already written as live law) and `sot_survival.md` (lock already Current; 1-B line becomes Current). Do not rewrite the rest. Do not promote if this score fails.

**If dests left the four rooms and occupancy is green**

1. Do the SOT checkbox above. Then continue.
2. Later data pass — **sit-only `study` (32 rooms)**, plus `eat` on eight home living rooms, `work` on six job sites, `play` on cafe/park/gardens. Not bathrooms. Not 53. Not on a gather-score run if you can avoid it.
3. Second pass: sofa / re-greet on this green tip.
4. Shepard (planner books sleep over a fixed event) and the day-number restamp — separate, small, not occupancy.

**If dests left the four rooms but occupancy still misses**

The veto diagnosis held. The leftovers are doing the damage: Shepard, pub `relax`/`eat`, job snaps. Do **not** restore the lock. Next paper is those leftovers, one at a time, each with its own observable.

**If dests still pile into the four rooms**

The veto was not the writer. Stop. Do not widen lists. Re-open §12.4.

**If the box is wrong (wrong tip, or `study` already on the cafe)**

Not a 1-B score. Tear down and restart.

---

### 5. Start payload (VPS, after pull)

```bash
curl -s -X POST http://127.0.0.1:8001/api/simulations/fork \
  -H "Content-Type: application/json" \
  -d '{"sim_code":"20260829-1","baseline":"soul15_seed_20260224","description":"1-B: whitelist veto deleted","copy_memories":true,"copy_coords":false}'

curl -s -X POST http://127.0.0.1:8001/api/simulations/20260829-1/start \
  -H "Content-Type: application/json" \
  -d '{"action":"start","parameters":{"max_steps":2400,"generation_mode":"sprint","skip_premiere":true,"diagnostic_mode":false}}'
```

---

### 6. Post-run (after this PID is gone)

Do these **once**, from disk / logs. They are not occupancy. Do not open them while `808722` is writing.

| What | Why | Where |
|---|---|---|
| Overlay copy — “you played” / “you were absent” / last-elim in prompts or `dbl_memory` | Present-only on tiles already passed; this is the spoken overlay we never grepped on 27-1 / 28-1 | This run’s storage + [1-A score §6](done/20260828-1_checklist.md) · [Pass 1 §6](done/20260827_checklist.md) |
| Persist + TELEPORT through **2285** | Mid-run persist can still be “so far”; finish the count on a stopped runner | Same bars as [1-A §3](done/20260828-1_checklist.md) |
| Search-fire stdout (`DETERMINISTIC GUARD v2`, cascade Tier-1 / 1b / 2 / 3 / 4) | Sizes whether whitelist **search** still matters after the veto is gone. Was `deferred: PID` on 28-1 | This run’s `api_gateway/logs/sims/20260829-1.log`. Optional: same strings on the stopped 28-1 log. Ask: [datareq 4 Q5](done/20260829_datareq_4_whitelist-strategy.md) |
| `currently` day string on day 2 | Confirm the stuck “On day 1” leftover (not a 1-B fail) | [1-A §5](done/20260828-1_checklist.md) · RCA §12 |
| Sit-and-read widen (32 rooms, not 53) | Search-quality data pass — **after** occupancy is scored, not mixed into it | [datareq 5 decision](done/20260829_datareq_5_whitelist-widen.md) · RCA §12.10 |
| 76-test-suite triage · align `TASK_DECOMP` code default with the box | Hygiene; hides real regressions | [1-A §7–§8](done/20260828-1_checklist.md) |
| Sofa / re-greet | Already **Fail** on Pass 1. Second pass on a green gather tip | [Pass 1 §5](done/20260827_checklist.md) · `20260901_launch.md` |

Trail paper (do not duplicate diagnosis here): `20260827_gathering_issue_RCA+recommendations.md` §12. Raw 1-A rows: `20260829_leave_pack/`.

---

## Decision

_(fill after checkpoint 6 / 7)_
