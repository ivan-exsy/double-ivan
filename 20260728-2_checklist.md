# 20260728-2 — Checklist: place-language re-score + post-chat learning deploy

**Purpose:** After `20260724-2` stops, deploy current `railway` and re-prove what that score left open: **§12C place language** (blocks natural-movement ship) and **Talk / post-chat learning** (assessor + consent auto-learn + leave/idle/reopen).

**Already proven on `20260724-2` (do not re-litigate):** hygiene soak, Chrome recycle, path persist 100%, path playback, walk/walls, Hobbs staff Gap1/2, piano false=0, Survival Day1–3 votes/elims, self-serve quiz→interview→Talk content, Join dormant empty state, bubble minimize + tip→head.

**Prior FAIL / open:**
- **12C** Vince Vale Hobbs vs Johnson Park — RCA `done/20260724-2_rca_12c_place_language.md`; P0+P1 on `railway`
- **Talk End session** empty assessor + FE optimistic skip copy — BE + FE fixes ready, not prod-validated

**BE:** `origin/railway` @ **`b230c4be`** (place-language `87644031` + post-chat assessor/auto-learn/idle sweep)  
**FE (Talk):** `ivan/post-chat-session-end-skip-ux` + `ivan/talk-session-auto-close` — promote when ready (not required for 12C)  
**Posture:** Survival sprint focused on morning commute honesty; self-serve learning is a short signed-in pass (can run while sim is live — do **not** restart `double-api`).

**Score sim:** `20260728-2`  
**Baseline / fork from:** `soul15_seed_20260224`  
**Target steps:** `2600` (min useful call ~1600–1800 through Survival Day 1 ~09:00)  
**Start:** `2026-07-28 06:30` · runner PID `2003296` · UUID `a21ca4f2-a254-45aa-ad20-9bcc8e7ca0a1`  
**Env:** `HEADLESS_MOVEMENT_ENABLED=true` · `HEADLESS_STRICT_ABORT=true` · `INTENT_PERSIST_HARD_FAIL=true`  
**RSS CSV:** `/tmp/reverie_rss_20260728-2.csv` (optional; not the primary gate)

> **Ship rule:** Natural-movement ship needs **§2 (12C) green**. Talk learning ship needs **§3 green** (can land same deploy day).

**Clock note (this run):** day boundary ~**step 1050 @ 00:00**. Survival Day 1 **07:00–09:00 ≈ steps 1470–1590** (checklist table that said 1080–1260 was midnight sleep — ignore for scoring).

**Probe tool:** reuse `generative_agents/scripts/score_sim_probe.py` (do not spawn new `_probe_*` one-offs).

---

### 0. Preflight (before start)

- [x] `20260724-2` fully stopped; prestop/RSS closed if you care about that paper
- [x] VPS `git pull` on `railway` → tip **`b230c4be`** (or newer)
- [x] `sudo systemctl restart double-api` after pull (not while old sim was live)
- [x] Confirm env flags above still set
- [x] Fork + start `20260728-2`; note UUID / PID / max_steps

---

### 1. Smoke only (expect PASS — 10 min)

*Already green on `20260724-2`; one glance after restart.*

- [x] Headless acceptance alive/alive for first ~50–100 steps — no strict abort
- [x] Soft: travel step still has usable `actual_path` (spot-check one commute)

**Result:** PASS @ ~step 110 (and still clean through ~1727). `HEADLESS_STRICT_ABORT=0`.

---

### 2. Place language — **primary gate (§12C)**

**Pass bar:** Morning commute status matches feet; named “walking to Hobbs / Oak Hill” does **not** send body to Johnson Park; no “ordering…” while clearly still travelling across town.

**Human (Day 1 morning, ~07:00–09:00 in-sim = ~1470–1590):**
- [~] Vince Vale (or any commute-heavy Double): status destination = body destination — **soft miss** (see notes)
- [x] No split “Walking to Hobbs” while arriving / lingering at Johnson Park — **classic RCA pattern absent**
- [ ] No premature “Ordering breakfast…” while still blocks away from cafe — **FAIL soft**: “order breakfast…” with path_len=7 ~07:05–07:16 while travelling to Hobbs
- [x] Soft: 1–2 other personas (Diana / Alex) — same honesty check (no Hobbs@Park)

**Analyzer / soak (optional but strong):**
- [x] Zero (or ≪ prior) assigns: `action='walking to Hobbs…' addr=…Johnson Park…` — **0** for Vince Day1 morning
- [x] Soft: soak has `POST-VALIDATE keep named dest` (~48) / no new exercise→Park cascade on named Hobbs travel

**Notes (scored ~step 1727, Survival Day 1 ~11:18):**
1. Classic **Hobbs status @ Johnson Park body** — not observed. `walk to Hobbs` / order / eat assign to Hobbs addr.
2. Soft residual: **06:38** `do light stretching in the main room` → addr **Johnson Park** (status≠feet while crossing town).
3. Soft residual: **07:05–07:16** ordering language while still travelling to cafe.
4. Soft residual: **08:00** `sip coffee…` assigned to **Oak Hill classroom** while moving there.

**RCA dig (2026-07-29):** original findings still hold for the deep mechanism; soft A/B are P0/P1 scope gaps + API raw-act preference. Full write-up: `done/20260724-2_rca_12c_place_language.md` **Addendum — 20260728-2**.

**Root-cause note:** P1 fixed the *named-travel* subclass only; P0 is emit/presentation. True roots are (1) sealing inconsistent act↔address contracts and (2) letting `activity_type` override place claims in the act. Recommended general fix = seal-time invariant + arrival gate + API prefer emit copy — see RCA “Did P0/P1 address the root cause?” / P2a–c.

**Fail →** keep 12C open; do not ship natural movement.

**Result:** **SOFT PASS / yellow** — classic ship-blocker fixed; soft A/B explained in RCA addendum. **Do not stamp full NM ship without a call on soft misses / P2.**

---

### 3. Talk / post-chat learning — **validate today’s BE (+ FE if promoted)**

*Account: `ipistsov@gmail.com` · owned host `owned-c2677e73`. Consent ON at `/onboarding/post-chat-updates`.*

**BE (`railway` @ `b230c4be`):**
- [x] End session after ≥3 user turns / ≥120 chars → **not** empty `assessor_error` (retry/fallback works) — got structured `validation_failed` / `insufficient_turns`, never empty assessor_error
- [ ] Consent ON + proposed patches → response `auto_applied: true`, `apply_status: confirmed`, life-chapter (`soul_seeds`) updated without Confirm — **not proven**: assessor returned JSON missing `status` → `validation_failed` (no patches to auto-apply). Retry blocked by owned-host thread rate limit (2/hour)
- [x] Skip responses include usable `reason` (and preferably `skipped: true`) — `insufficient_turns` + `validation_failed`, both `skipped: true`
- [ ] Soft: leave browser open idle ~16+ min with an unassessed thread → idle sweep assesses (or FE idle close if FE branch live) — not run
- [x] Soft: chat messages still present after skip/error (never wiped) — thread `e58071b2-…` still has 6 msgs / 3 user turns after session/end

**FE (if `ivan/post-chat-session-end-skip-ux` / `ivan/talk-session-auto-close` on the Talk host):**
- [ ] Skip → honest “No update proposals (reason)” — no “suggestions will appear…” CTA — FE branches not scored
- [ ] Leave Talk (`pagehide`) / reopen Talk flushes prior thread; next message uses a **new** `thread_id` after idle close
- [ ] Soft: `auto_applied` copy — “Updated what your Double knows…”

**Out of scope here:** full PM-OWN-1 “what my Double knows” editor; Join punchline world; Light soul.

**Smoke:** `SMOKE_GATEWAY_URL=https://api.ondouble.com:8001 python scripts/smoke_talk_session_end_live.py --live`

**Result:** **PARTIAL / yellow** — empty-assessor bug looks fixed; skip reason OK; messages intact; **auto_apply Path A not green yet** (assessor schema miss). Re-try after thread rate-limit window (~1h) or raise owned-host thread cap for smoke.

---

### 4. Natural-movement spot-check (regression — soft)

- [x] Walk / walls OK (no clip flood) on the morning window you use for §2 — soft PASS (usable paths observed; no abort)
- [ ] Soft: Hobbs counter — non-staff not invading kitchen — not deeply scored this paper

---

### 5. Stop hygiene

- [ ] Status `stopped`/`completed`; runner gone; Chrome drained
- [ ] One-line note: tip deployed + 12C / Talk verdict

---

### Verdict

| Gate | Result | Notes |
|------|--------|-------|
| **0 preflight / deploy** | PASS | tip `b230c4be` |
| **1 smoke** | PASS | no strict abort; paths usable |
| **2 place language (12C)** | SOFT PASS / yellow | Classic Hobbs@Park fixed; residual stretching→Park + ordering-while-travel + coffee@OakHill |
| **3 Talk / post-chat** | PARTIAL / yellow | empty assessor fixed; skip+messages OK; auto_apply not proven (`validation_failed`) |
| **4 NM soft** | soft PASS | paths OK; Hobbs kitchen not deep-scored |
| **Natural movement ship?** | HOLD | needs clean call on §2 soft misses |
| **Talk learning ship?** | HOLD | needs auto_apply green (retry after thread rate limit) |

**Deferred:** Join punchline (§9 prior), Light §12H, deep R4 chat policy, PM-OWN-1 editor UI.
