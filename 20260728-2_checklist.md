# 20260728-1 — Checklist: place-language re-score + post-chat learning deploy

**Purpose:** After `20260724-2` stops, deploy current `railway` and re-prove what that score left open: **§12C place language** (blocks natural-movement ship) and **Talk / post-chat learning** (assessor + consent auto-learn + leave/idle/reopen).

**Already proven on `20260724-2` (do not re-litigate):** hygiene soak, Chrome recycle, path persist 100%, path playback, walk/walls, Hobbs staff Gap1/2, piano false=0, Survival Day1–3 votes/elims, self-serve quiz→interview→Talk content, Join dormant empty state, bubble minimize + tip→head.

**Prior FAIL / open:**
- **12C** Vince Vale Hobbs vs Johnson Park — RCA `20260728_rca_12c_place_language.md`; P0+P1 on `railway`
- **Talk End session** empty assessor + FE optimistic skip copy — BE + FE fixes ready, not prod-validated

**BE:** `origin/railway` @ **`b230c4be`** (place-language `87644031` + post-chat assessor/auto-learn/idle sweep)  
**FE (Talk):** `ivan/post-chat-session-end-skip-ux` + `ivan/talk-session-auto-close` — promote when ready (not required for 12C)  
**Posture:** Survival sprint focused on morning commute honesty; self-serve learning is a short signed-in pass after API restart.

**Score sim:** `20260728-1`  
**Baseline / fork from:** `________` (prefer same Survival cast as `20260724-2` if possible)  
**Target steps:** `________` (≥1 full morning commute Day 1; optional Day 2 challenge)  
**Start:** `________` · runner PID `________` · UUID `________`  
**Env:** `HEADLESS_MOVEMENT_ENABLED=true` · `HEADLESS_STRICT_ABORT=true` · `INTENT_PERSIST_HARD_FAIL=true`  
**RSS CSV:** `/tmp/reverie_rss_20260728-1.csv` (optional; not the primary gate)

> **Ship rule:** Natural-movement ship needs **§2 (12C) green**. Talk learning ship needs **§3 green** (can land same deploy day).

---

### 0. Preflight (before start)

- [ ] `20260724-2` fully stopped; prestop/RSS closed if you care about that paper
- [ ] VPS `git pull` on `railway` → tip **`b230c4be`** (or newer)
- [ ] `sudo systemctl restart double-api` after pull (not while old sim was live)
- [ ] Confirm env flags above still set
- [ ] Fork + start `20260728-1`; note UUID / PID / max_steps

---

### 1. Smoke only (expect PASS — 10 min)

*Already green on `20260724-2`; one glance after restart.*

- [ ] Headless acceptance alive/alive for first ~50–100 steps — no strict abort
- [ ] Soft: travel step still has usable `actual_path` (spot-check one commute)

---

### 2. Place language — **primary gate (§12C)**

**Pass bar:** Morning commute status matches feet; named “walking to Hobbs / Oak Hill” does **not** send body to Johnson Park; no “ordering…” while clearly still travelling across town.

**Human (Day 1 morning, ~07:00–09:00 in-sim):**
- [ ] Vince Vale (or any commute-heavy Double): status destination = body destination
- [ ] No split “Walking to Hobbs” while arriving / lingering at Johnson Park
- [ ] No premature “Ordering breakfast…” while still blocks away from cafe
- [ ] Soft: 1–2 other personas (Diana / Alex) — same honesty check

**Analyzer / soak (optional but strong):**
- [ ] Zero (or ≪ prior) assigns: `action='walking to Hobbs…' addr=…Johnson Park…`
- [ ] Soft: soak has `POST-VALIDATE keep named dest` / no new exercise→Park cascade on named travel

**Fail →** keep 12C open; do not ship natural movement.

---

### 3. Talk / post-chat learning — **validate today’s BE (+ FE if promoted)**

*Account: `ipistsov@gmail.com` · owned host `owned-c2677e73`. Consent ON at `/onboarding/post-chat-updates`.*

**BE (`railway` @ `b230c4be`):**
- [ ] End session after ≥3 user turns / ≥120 chars → **not** empty `assessor_error` (retry/fallback works)
- [ ] Consent ON + proposed patches → response `auto_applied: true`, `apply_status: confirmed`, life-chapter (`soul_seeds`) updated without Confirm
- [ ] Skip responses include usable `reason` (and preferably `skipped: true`)
- [ ] Soft: leave browser open idle ~16+ min with an unassessed thread → idle sweep assesses (or FE idle close if FE branch live)
- [ ] Soft: chat messages still present after skip/error (never wiped)

**FE (if `ivan/post-chat-session-end-skip-ux` / `ivan/talk-session-auto-close` on the Talk host):**
- [ ] Skip → honest “No update proposals (reason)” — no “suggestions will appear…” CTA
- [ ] Leave Talk (`pagehide`) / reopen Talk flushes prior thread; next message uses a **new** `thread_id` after idle close
- [ ] Soft: `auto_applied` copy — “Updated what your Double knows…”

**Out of scope here:** full PM-OWN-1 “what my Double knows” editor; Join punchline world; Light soul.

---

### 4. Natural-movement spot-check (regression — soft)

- [ ] Walk / walls OK (no clip flood) on the morning window you use for §2
- [ ] Soft: Hobbs counter — non-staff not invading kitchen

---

### 5. Stop hygiene

- [ ] Status `stopped`/`completed`; runner gone; Chrome drained
- [ ] One-line note: tip deployed + 12C / Talk verdict

---

### Verdict

| Gate | Result | Notes |
|------|--------|-------|
| **0 preflight / deploy** | | tip `________` (want `b230c4be`+) |
| **1 smoke** | | hygiene/path glance |
| **2 place language (12C)** | | **primary for NM ship** |
| **3 Talk / post-chat** | | assessor + auto-learn (+ FE if live) |
| **4 NM soft** | | |
| **Natural movement ship?** | | needs **§2** green |
| **Talk learning ship?** | | needs **§3** green |

**Deferred:** Join punchline (§9 prior), Light §12H, deep R4 chat policy, PM-OWN-1 editor UI.
